/**
 * Assignment service tests — Phase 02 Plan 01 Wave 0 STUB (covers ASGN-02 query shape).
 *
 * The `findActiveAssignmentForClient` service is implemented in Plan 02-04.
 * This file is intentionally a `describe.skip` placeholder so the test scaffolding
 * exists now and Plan 02-04 only needs to unskip + fill the assertions. Importing the
 * not-yet-created module here would break the suite, so it is described inline.
 */

describe.skip('findActiveAssignmentForClient (implemented in Plan 02-04)', () => {
  test('queries assignments where clientId == uid AND status == "active"', () => {
    // Implemented in Plan 02-04 — see ASGN-02. Stubbed in Wave 0 (Plan 02-01).
    expect(true).toBe(true);
  });
});
