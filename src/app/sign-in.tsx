/**
 * Sign-in placeholder — Phase 01 Plan 02
 *
 * This is a temporary stub to keep the build green while the root layout
 * references the 'sign-in' route. Plan 03 replaces this with the full
 * sign-in form (email/password + forgot password).
 *
 * DO NOT implement the sign-in form here — Plan 03 owns this file.
 */

import { View, Text } from 'react-native';

export default function SignInScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-[#0E0E0E]">
      <Text className="text-white text-lg">Sign In</Text>
    </View>
  );
}
