import { View, Text } from 'react-native';

/** Placeholder — Clients list lands here (filled by Plan 02-03). */
export default function ClientsScreen() {
  return (
    <View style={{ flex: 1, backgroundColor: '#0E0E0E', alignItems: 'center', justifyContent: 'center' }}>
      <Text style={{ color: '#FFFFFF', fontSize: 18 }}>Clients (coming in plan 02-03)</Text>
    </View>
  );
}
