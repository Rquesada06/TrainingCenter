import { View, Text } from 'react-native';

/** Placeholder — Routine builder lands here (filled by Plan 02-04). */
export default function RoutinesScreen() {
  return (
    <View style={{ flex: 1, backgroundColor: '#0E0E0E', alignItems: 'center', justifyContent: 'center' }}>
      <Text style={{ color: '#FFFFFF', fontSize: 18 }}>Routines (coming in plan 02-04)</Text>
    </View>
  );
}
