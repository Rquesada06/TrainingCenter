import { View, Text } from 'react-native';

/** Placeholder — Exercise library lands here (filled by Plan 02-02). */
export default function ExercisesScreen() {
  return (
    <View style={{ flex: 1, backgroundColor: '#0E0E0E', alignItems: 'center', justifyContent: 'center' }}>
      <Text style={{ color: '#FFFFFF', fontSize: 18 }}>Exercises (coming in plan 02-02)</Text>
    </View>
  );
}
