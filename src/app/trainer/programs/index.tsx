import { View, Text } from 'react-native';

/** Placeholder — Program builder lands here (filled by Plan 02-05). */
export default function ProgramsScreen() {
  return (
    <View style={{ flex: 1, backgroundColor: '#0E0E0E', alignItems: 'center', justifyContent: 'center' }}>
      <Text style={{ color: '#FFFFFF', fontSize: 18 }}>Programs (coming in plan 02-05)</Text>
    </View>
  );
}
