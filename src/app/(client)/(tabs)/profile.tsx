/**
 * Client Profile placeholder — Phase 01 Plan 02
 *
 * Intentionally minimal. Real profile content ships in Phase 3.
 * Background: Obsidian Performance base #0E0E0E.
 */

import { View, Text } from 'react-native';

export default function ClientProfileScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-[#0E0E0E]">
      <Text className="text-white text-lg">Client Profile</Text>
    </View>
  );
}
