/**
 * Client Home placeholder — Phase 01 Plan 02
 *
 * Intentionally minimal. Real home content ships in Phase 3.
 * Background: Obsidian Performance base #0E0E0E.
 */

import { View, Text } from 'react-native';

export default function ClientHomeScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-[#0E0E0E]">
      <Text className="text-white text-lg">Client Home</Text>
    </View>
  );
}
