---
phase: 02-trainer-content-creation
plan: 05
type: execute
wave: 4
depends_on: ['02-01', '02-03', '02-04']
files_modified:
  - functions/src/index.ts
  - functions/src/__tests__/createAssignment.test.ts
  - src/firebase/functions.ts
  - src/services/program.service.ts
  - src/services/__tests__/program.service.test.ts
  - src/services/assignment.service.ts
  - src/services/__tests__/assignment.service.test.ts
  - src/hooks/usePrograms.ts
  - src/hooks/useProgram.ts
  - src/hooks/useCreateProgram.ts
  - src/hooks/useUpdateProgram.ts
  - src/hooks/useDeleteProgram.ts
  - src/hooks/useCreateAssignment.ts
  - src/components/programs/ProgramMetaForm.tsx
  - src/components/programs/WeekDayGrid.tsx
  - src/components/programs/DayPickerSheet.tsx
  - src/components/programs/ClientPickerSheet.tsx
  - src/components/programs/AssignProgramModal.tsx
  - src/app/trainer/programs/index.tsx
  - src/app/trainer/programs/new.tsx
  - src/app/trainer/programs/[programId].tsx
autonomous: false
requirements:
  - PROG-01
  - PROG-02
  - PROG-03
  - PROG-04
  - PROG-05
  - PROG-06
  - ASGN-01
  - ASGN-02
  - ASGN-03
  - ASGN-04

must_haves:
  truths:
    - "Trainer can create a program with name, description, and durationWeeks; the weeks array is auto-generated with 7 null-day slots per week"
    - "Trainer can tap any cell in the Week×Day grid and pick a routine (PROG-02) or mark rest (PROG-03); null cells render as REST per PROG-04"
    - "Trainer can edit a program's metadata and grid; can delete a program with confirmation (PROG-05)"
    - "Trainer can view a list of all their programs (PROG-06) sorted by name"
    - "Trainer can tap Assign on a program detail screen, pick a client and a start date, and the system creates an assignment with a full immutable snapshot via the createAssignment Cloud Function (ASGN-01, ASGN-03)"
    - "If the client already has an active assignment, the trainer sees a confirmation modal before overwriting; existing active assignment is marked completed server-side (ASGN-02)"
    - "Start date is stored as YYYY-MM-DD string (ASGN-04 — already a Phase 1 decision, this plan enforces in the schema)"
  artifacts:
    - path: "functions/src/index.ts"
      provides: "createAssignment v1 onCall + existing createClientAccount"
      contains: "export const createAssignment"
    - path: "src/firebase/functions.ts"
      provides: "createAssignmentCallable + existing createClientAccountCallable"
      exports: ["createClientAccountCallable", "createAssignmentCallable"]
    - path: "src/services/program.service.ts"
      provides: "Program CRUD wrappers"
      exports: ["listPrograms", "getProgram", "createProgram", "updateProgram", "deleteProgram"]
    - path: "src/services/assignment.service.ts"
      provides: "createAssignment client caller + re-export findActiveAssignmentForClient from client.service"
      exports: ["callCreateAssignment", "findActiveAssignmentForClient"]
    - path: "src/components/programs/WeekDayGrid.tsx"
      provides: "Scrollable Week×Day 1-7 grid (PROG-02/PROG-03/PROG-04)"
      min_lines: 50
    - path: "src/components/programs/DayPickerSheet.tsx"
      provides: "@gorhom/bottom-sheet picker — pick routine or mark rest"
      min_lines: 40
    - path: "src/components/programs/AssignProgramModal.tsx"
      provides: "Two-step assign flow: client picker + start date + ASGN-02 confirmation"
      min_lines: 60
  key_links:
    - from: "src/services/assignment.service.ts"
      to: "src/firebase/functions.ts (createAssignmentCallable)"
      via: "httpsCallable('createAssignment') invocation"
      pattern: "createAssignmentCallable"
    - from: "functions/src/index.ts (createAssignment)"
      to: "admin.firestore() reads of programs + routines + exercises"
      via: "Server-side snapshot build (ASGN-03)"
      pattern: "buildSnapshot|admin.firestore"
    - from: "src/components/programs/AssignProgramModal.tsx"
      to: "src/hooks/useActiveAssignment.ts (Plan 02-03)"
      via: "findActiveAssignmentForClient for ASGN-02 overwrite check"
      pattern: "findActiveAssignmentForClient"
    - from: "src/components/programs/WeekDayGrid.tsx"
      to: "src/components/programs/DayPickerSheet.tsx"
      via: "Cell tap opens sheet with (weekIndex, dayIndex) context"
      pattern: "DayPickerSheet"
---

<objective>
Final Phase 2 plan — build the program builder and the assignment flow, which together complete the "Trainer can create a program and assign it to a client in under 3 minutes" core value proposition (per PROJECT.md). Covers all six PROG requirements + all four ASGN requirements:

- PROG-01: Create program with name, description, durationWeeks (1-26 cap per Pitfall 6)
- PROG-02: Assign a routine to each day (Week × Day 1-7 grid)
- PROG-03: Mark specific days as rest
- PROG-04: Null days default to rest (UI treats null and 'rest' identically per CONTEXT.md D-2)
- PROG-05: Edit and delete programs
- PROG-06: List all the trainer's programs
- ASGN-01: Assign program to client with start date
- ASGN-02: Warn before overwriting active assignment (uses findActiveAssignmentForClient from Plan 02-03)
- ASGN-03: Server-side snapshot via `createAssignment` Cloud Function (v1 onCall per CONTEXT.md D-Cloud Function — same v1 vs v2 reasoning as createClientAccount Pitfall 5)
- ASGN-04: startDate stored as YYYY-MM-DD string (already a Phase 1 decision per STATE.md; this plan's program schema enforces it)

This plan ships the Cloud Function (deployable to Firebase Functions) AND the client-side trigger + confirmation UX. autonomous=false because it includes a checkpoint:human-verify for the end-to-end assignment flow (Cloud Function emulator + dev client integration).

Output: Programs tab with full CRUD + Week×Day grid editor + assignment flow. After this plan, a real trainer can perform the full content pipeline: create exercises (02-02), build a routine (02-04), assemble a program (02-05), assign to a client (02-05), and the client side becomes Phase 3.
</objective>

<execution_context>
@/Users/FF-rodrigo/Documents/Personal/LauFit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/FF-rodrigo/Documents/Personal/LauFit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-trainer-content-creation/02-CONTEXT.md
@.planning/phases/02-trainer-content-creation/02-RESEARCH.md
@.planning/phases/02-trainer-content-creation/02-01-SUMMARY.md
@.planning/phases/02-trainer-content-creation/02-02-SUMMARY.md
@.planning/phases/02-trainer-content-creation/02-03-SUMMARY.md
@.planning/phases/02-trainer-content-creation/02-04-SUMMARY.md
@CLAUDE.md
@src/types/program.ts
@src/types/assignment.ts
@src/types/routine.ts
@src/validation/program.schema.ts
@src/firebase/firestore.ts
@src/firebase/functions.ts
@src/services/routine.service.ts
@src/services/client.service.ts
@src/hooks/useRoutines.ts
@src/hooks/useClients.ts
@src/hooks/useActiveAssignment.ts
@src/components/ui/TextField.tsx
@src/components/ui/PrimaryButton.tsx
@src/components/clients/ClientListItem.tsx
@src/stores/authStore.ts
@functions/src/index.ts
@firestore.rules
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: createAssignment Cloud Function + client callable + assignment.service + tests</name>
  <files>
    functions/src/index.ts,
    functions/src/__tests__/createAssignment.test.ts,
    src/firebase/functions.ts,
    src/services/assignment.service.ts,
    src/services/__tests__/assignment.service.test.ts,
    src/hooks/useCreateAssignment.ts
  </files>
  <read_first>
    functions/src/index.ts,
    functions/src/__tests__/,
    src/firebase/functions.ts,
    src/services/client.service.ts,
    src/types/assignment.ts,
    src/types/program.ts,
    src/types/routine.ts,
    src/types/exercise.ts,
    .planning/phases/02-trainer-content-creation/02-CONTEXT.md (Decision 4 — Snapshot Implementation; Decision Cloud Function with full signature),
    .planning/phases/02-trainer-content-creation/02-RESEARCH.md (Pattern 7 — createAssignment Cloud Function; Pitfall 5/6 — v1 onCall + snapshot size limit)
  </read_first>
  <behavior>
    `functions/src/__tests__/createAssignment.test.ts` (mirrors existing createClientAccount.test.ts patterns):
    - Test: rejects unauthenticated caller with HttpsError code 'unauthenticated'
    - Test: rejects non-trainer caller (role='client') with HttpsError code 'permission-denied'
    - Test: rejects when program.trainerId !== context.auth.uid with HttpsError code 'not-found'
    - Test: successful call builds snapshot from program → routines → exercises and writes one assignments doc
    - Test: previous active assignment for the same clientId is marked status='completed' in the same batch
    - Test: rejects when snapshot.length > 800_000 bytes with HttpsError code 'failed-precondition' (Pitfall 6 safety)
    - Test: returns `{ assignmentId: string }` on success

    `src/services/__tests__/assignment.service.test.ts` (replaces the Wave 0 stub from Plan 02-01):
    - Test: `callCreateAssignment({ programId, clientId, startDate })` invokes `createAssignmentCallable` and unwraps `result.data.assignmentId`
    - Test: re-exports `findActiveAssignmentForClient` from client.service (single source of truth)
  </behavior>
  <action>
    `functions/src/index.ts` — ADD `createAssignment` at the bottom of the file. KEEP existing `createClientAccount` (Phase 1 deliverable). Follow the v1 onCall pattern per RESEARCH.md Pattern 7 and CONTEXT.md D-Cloud Function. Pitfall 5 from STATE.md confirms v1 (NOT v2) is required for @react-native-firebase/functions.httpsCallable() compatibility.

    Imports (preserve existing): `import * as functions from 'firebase-functions'`, `import * as admin from 'firebase-admin'`.

    Local type copies (mirror src/types/assignment.ts shape — cross-tsconfig imports are not used per the existing createClientAccount pattern):
    - `CreateAssignmentInput { programId: string; clientId: string; startDate: string }`
    - `CreateAssignmentResult { assignmentId: string }`
    - `AssignmentSnapshot { ... }` minimal subset needed at runtime

    `createAssignment` implementation:
    1. Reject if `!context.auth` → `HttpsError('unauthenticated', 'Must be authenticated.')`
    2. Read `users/${context.auth.uid}`; reject if `role !== 'trainer'` → `HttpsError('permission-denied', 'Only trainers can create assignments.')`
    3. Read `programs/${data.programId}`; reject if missing or `program.trainerId !== context.auth.uid` → `HttpsError('not-found', 'Program not found.')`
    4. Validate `data.clientId`: read `users/${data.clientId}`; reject if missing or `role !== 'client'` or `trainerId !== context.auth.uid` → `HttpsError('permission-denied', 'Client not found in your roster.')`
    5. Validate `data.startDate` format: must match `/^\d{4}-\d{2}-\d{2}$/` per ASGN-04 → if not, `HttpsError('invalid-argument', 'startDate must be YYYY-MM-DD.')`
    6. Build snapshot by traversing program.weeks → for each day, if routineId, read `routines/${routineId}` and for each routineExercise, read `exercises/${exerciseId}` AND (if present) `exercises/${alternativeExerciseId}`. Construct `AssignmentSnapshot` matching the exact shape in CONTEXT.md D-4 (name, description, durationWeeks, weeks: [{ days: [{ type, routineId, routine: { name, exercises: [{ exerciseId, name, sets, reps, duration, rest, notes, locationTypes, videoUrl, imageUrl, alternativeExerciseId, alternativeExercise }] }] }] }).
    7. Size safety: `if (JSON.stringify(snapshot).length > 800_000) throw new HttpsError('failed-precondition', 'Program too large for snapshot — split into shorter programs.')` (Pitfall 6).
    8. Query previous active assignments: `await admin.firestore().collection('assignments').where('clientId', '==', data.clientId).where('status', '==', 'active').get()`.
    9. Batch: `const batch = admin.firestore().batch(); const newRef = admin.firestore().collection('assignments').doc(); batch.set(newRef, { trainerId: context.auth.uid, clientId: data.clientId, programId: data.programId, status: 'active', startDate: data.startDate, createdAt: admin.firestore.FieldValue.serverTimestamp(), snapshot }); prevQuery.docs.forEach((d) => batch.update(d.ref, { status: 'completed' })); await batch.commit();`
    10. Return `{ assignmentId: newRef.id }`.

    Performance note: parallelize reads with `Promise.all` for the routines and exercises lookups — collect unique routineIds and exerciseIds first, then `Promise.all(routineIds.map((id) => admin.firestore().doc(...).get()))`.

    `src/firebase/functions.ts` — READ existing exports (createClientAccountCallable from Phase 1). ADD: `export const createAssignmentCallable = functions().httpsCallable('createAssignment')`. Add a TypeScript wrapper if helpful: `export async function callCreateAssignment(input: CreateAssignmentInput): Promise<CreateAssignmentResult> { const result = await createAssignmentCallable(input); return result.data as CreateAssignmentResult; }`.

    `src/services/assignment.service.ts` — Import and re-export `callCreateAssignment` from `@/firebase/functions`. Import and re-export `findActiveAssignmentForClient` from `@/services/client.service`. This consolidates the assignment-related service surface so Plan 02-05 consumers have one import path.

    `src/hooks/useCreateAssignment.ts` — `useMutation({ mutationFn: callCreateAssignment, onSuccess: (_, vars) => { queryClient.invalidateQueries({ queryKey: ['activeAssignment', vars.clientId] }); queryClient.invalidateQueries({ queryKey: ['clients', trainerId] }); } })`.

    `functions/src/__tests__/createAssignment.test.ts` — Mirror the createClientAccount.test.ts test harness. Use firebase-functions-test SDK + admin SDK stubs OR the firebase emulator pattern referenced in VALIDATION.md "Manual-Only Verifications" row for createAssignment. The behavior cases above must be covered with unit-style mocks; full emulator integration test is the checkpoint in Task 4.
  </action>
  <verify>
    <automated>cd functions && npm test -- --testPathPattern=createAssignment && cd .. && npx jest --testPathPattern=assignment.service --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 (root)
    - `cd functions && npx tsc --noEmit` exits 0
    - `cd functions && npm test -- --testPathPattern=createAssignment` passes — 7 test cases green
    - `npx jest --testPathPattern=assignment.service` passes — 2 test cases green
    - `grep -c "export const createAssignment" functions/src/index.ts` == 1
    - `grep -c "export const createClientAccount" functions/src/index.ts` == 1  (preserved)
    - `grep -c "functions.https.onCall" functions/src/index.ts` >= 2  (both Cloud Functions use v1 onCall)
    - `grep -c "createAssignmentCallable" src/firebase/functions.ts` >= 1
    - `grep -c "callCreateAssignment" src/services/assignment.service.ts` >= 1
    - `grep -c "findActiveAssignmentForClient" src/services/assignment.service.ts` >= 1
    - `grep -c "startDate must be YYYY-MM-DD\\|YYYY-MM-DD" functions/src/index.ts` >= 1  (ASGN-04 enforcement)
    - `grep -c "800_000\\|800000" functions/src/index.ts` >= 1  (Pitfall 6 snapshot size guard)
  </acceptance_criteria>
  <done>
    Cloud Function exists, compiles, has unit test coverage; client callable + service wrapper + hook are wired; assignment.service exports the surface that Plan 02-05 consumes for ASGN-01..04.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Program service + hooks + program component primitives (ProgramMetaForm, WeekDayGrid, DayPickerSheet)</name>
  <files>
    src/services/program.service.ts,
    src/services/__tests__/program.service.test.ts,
    src/hooks/usePrograms.ts,
    src/hooks/useProgram.ts,
    src/hooks/useCreateProgram.ts,
    src/hooks/useUpdateProgram.ts,
    src/hooks/useDeleteProgram.ts,
    src/components/programs/ProgramMetaForm.tsx,
    src/components/programs/WeekDayGrid.tsx,
    src/components/programs/DayPickerSheet.tsx
  </files>
  <read_first>
    src/services/routine.service.ts,
    src/firebase/firestore.ts,
    src/types/program.ts,
    src/types/routine.ts,
    src/validation/program.schema.ts,
    src/hooks/useRoutines.ts,
    src/components/ui/TextField.tsx,
    src/components/ui/PrimaryButton.tsx,
    .planning/phases/02-trainer-content-creation/02-CONTEXT.md (Decision 2 — Program Day Model with the durationWeeks generation rule),
    .planning/phases/02-trainer-content-creation/02-RESEARCH.md (Pattern 6 — Program grid; Pitfall 5 — column overflow)
  </read_first>
  <behavior>
    `src/services/__tests__/program.service.test.ts`:
    - Test: listPrograms(uid) calls `programsCollection().where('trainerId', '==', uid).orderBy('name', 'asc').get()`
    - Test: getProgram(id) returns Program or null
    - Test: createProgram seeds `weeks` array of length durationWeeks, each with `days` array of 7 null entries (PROG-04 default)
    - Test: updateProgram preserves existing weeks structure when partial update doesn't include weeks
    - Test: deleteProgram calls .delete()
  </behavior>
  <action>
    `src/services/program.service.ts`:
    - `listPrograms`, `getProgram`, `updateProgram`, `deleteProgram` — mirror exercise.service.ts/routine.service.ts pattern exactly.
    - `createProgram({ trainerId, input }: { trainerId: string; input: CreateProgramInput }): Promise<string>` — REQUIRED behavior: auto-generate `weeks` array per CONTEXT.md D-2 if not provided in input. Implementation: `const weeks = input.weeks ?? Array.from({ length: input.durationWeeks }, () => ({ days: Array(7).fill(null) as ProgramDay[] }));` then `.add({ trainerId, name, description, durationWeeks, weeks, createdAt: serverTimestamp, updatedAt: serverTimestamp })`. The null-day default fulfills PROG-04 (null = unassigned, renders as REST).

    Hooks per TanStack Query v5 pattern (mirror Plan 02-04 Task 1):
    - `usePrograms()`, `useProgram(id)`, `useCreateProgram`, `useUpdateProgram`, `useDeleteProgram` — same shape with `['programs', uid]` and `['program', id]` cache keys.

    Components:

    `src/components/programs/ProgramMetaForm.tsx` — RHF + zodResolver(programSchema). Props: `{ defaultValues?: Partial<ProgramFormValues>; onSubmit: (values: ProgramFormValues) => Promise<void>; submitLabel: string; submitting?: boolean }`. Fields: name (TextField), description (TextField multiline), durationWeeks (TextField number-pad — coerced to int 1-26 per program schema). Per Pitfall 4, explicit `useForm<z.input<typeof programSchema>, any, z.output<typeof programSchema>>` types.

    `src/components/programs/WeekDayGrid.tsx` — Per RESEARCH.md Pattern 6. Props: `{ weeks: ProgramWeek[]; routineNameMap: Record<string, string>; onCellPress: (weekIndex: number, dayIndex: number) => void }`. Renders:
    - Outer horizontal ScrollView (per Pitfall 5 — 7 columns × 320pt iPhone SE overflows without horizontal scroll)
    - Inside: View with flex-direction column
    - Sticky header row: empty cell (week label slot) + 7 cells labeled "D1" through "D7" (text-#888888 text-xs centered)
    - Per-week rows: Text "W${w+1}" (text-white text-xs w-12) + 7 Pressable cells per day. Each cell: bg #1A1A1A border #444 w-14 h-14 m-0.5 items-center justify-center. Content:
      - If day === null OR day === 'rest': Text "REST" (text-#888888 text-xs)
      - If routineId (string): Text `routineNameMap[routineId]?.slice(0, 6) ?? '?'` (text-#00FF66 text-xs) — truncated per Pitfall 5
    - onPress on each cell calls `onCellPress(w, d)`

    `src/components/programs/DayPickerSheet.tsx` — `forwardRef<BottomSheetModal, Props>` that exposes `present(weekIndex, dayIndex)` via useImperativeHandle. Props: `{ onSelect: (weekIndex: number, dayIndex: number, choice: string | 'rest' | null) => void }` (null = unassigned). Uses `useRoutines()` to populate the routine list.

    Sheet layout (snapPoints=['60%']):
    - Header: Text "Week ${w+1}, Day ${d+1}" (text-white text-lg)
    - "Mark as Rest" Pressable (full width, bg #1A1A1A border #444 mb-2) → onSelect(w, d, 'rest')
    - "Unassigned" Pressable → onSelect(w, d, null)
    - BottomSheetFlatList of routines: each row is a Pressable showing routine.name. onPress → onSelect(w, d, routine.id)
    - All onSelect callbacks should also `dismiss()` the sheet
  </action>
  <verify>
    <automated>npx jest --testPathPattern=program.service --passWithNoTests && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `npx jest --testPathPattern=program.service` passes — 5 test cases green
    - `grep -c "Array(7).fill(null)" src/services/program.service.ts` >= 1  (PROG-04 default)
    - `grep -c "where('trainerId', '==', uid)" src/services/program.service.ts` >= 1
    - `grep -c "zodResolver(programSchema)" src/components/programs/ProgramMetaForm.tsx` >= 1
    - `grep -c "ScrollView" src/components/programs/WeekDayGrid.tsx` >= 1  (Pitfall 5 horizontal scroll)
    - `grep -c "horizontal" src/components/programs/WeekDayGrid.tsx` >= 1
    - `grep -c "REST" src/components/programs/WeekDayGrid.tsx` >= 1  (PROG-03/PROG-04)
    - `grep -c "BottomSheetModal\\|BottomSheetFlatList" src/components/programs/DayPickerSheet.tsx` >= 2
    - `grep -c "useRoutines" src/components/programs/DayPickerSheet.tsx` >= 1
    - `grep -c "Mark as Rest" src/components/programs/DayPickerSheet.tsx` >= 1
  </acceptance_criteria>
  <done>
    Program CRUD + hooks compile with unit tests. Three program primitives (meta form, grid, day picker) ready for the screen wiring in Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 3: Programs list, create, edit + AssignProgramModal + ClientPickerSheet</name>
  <files>
    src/components/programs/ClientPickerSheet.tsx,
    src/components/programs/AssignProgramModal.tsx,
    src/app/trainer/programs/index.tsx,
    src/app/trainer/programs/new.tsx,
    src/app/trainer/programs/[programId].tsx
  </files>
  <read_first>
    src/app/trainer/programs/index.tsx,
    src/app/trainer/programs/_layout.tsx,
    src/components/programs/ProgramMetaForm.tsx,
    src/components/programs/WeekDayGrid.tsx,
    src/components/programs/DayPickerSheet.tsx,
    src/components/clients/ClientPhoto.tsx,
    src/hooks/usePrograms.ts,
    src/hooks/useProgram.ts,
    src/hooks/useCreateProgram.ts,
    src/hooks/useUpdateProgram.ts,
    src/hooks/useDeleteProgram.ts,
    src/hooks/useCreateAssignment.ts,
    src/hooks/useClients.ts,
    src/hooks/useRoutines.ts,
    src/services/client.service.ts (findActiveAssignmentForClient)
  </read_first>
  <action>
    `src/components/programs/ClientPickerSheet.tsx` — `forwardRef<BottomSheetModal, Props>` exposing `present()`. Props: `{ onSelect: (client: User) => void }`. Uses `useClients()`. BottomSheetFlatList of client rows (mirror ClientListItem layout: ClientPhoto + name). Tap → onSelect(client) + dismiss().

    `src/components/programs/AssignProgramModal.tsx` — The ASGN-01/ASGN-02 flow component. Props: `{ programId: string; onComplete: () => void; onCancel: () => void }`. Implemented as a Modal (RN `Modal` component or stack screen — keep as Modal for inline UX). Internal state: `selectedClient: User | null`, `startDate: string` (YYYY-MM-DD), `step: 'pickClient' | 'pickDate' | 'confirmOverwrite' | 'submitting' | 'done'`, `existingActiveAssignment: Assignment | null`.

    Flow:
    1. step='pickClient': Render text "Select a client" + Pressable "Choose client" → opens ClientPickerSheet → onSelect sets selectedClient + step='pickDate'.
    2. step='pickDate': Render TextField label="Start date (YYYY-MM-DD)" wired to startDate state with placeholder=today's date. Add a regex validator `/^\d{4}-\d{2}-\d{2}$/` (ASGN-04). PrimaryButton "Continue" enabled when format valid.
    3. On Continue, call `await findActiveAssignmentForClient(selectedClient.uid)`. If non-null → set existingActiveAssignment + step='confirmOverwrite'. If null → step='submitting' + call mutation immediately.
    4. step='confirmOverwrite': Render warning Text "${selectedClient.name} already has an active program: ${existingActiveAssignment.snapshot.name}. Assigning will mark the current program as completed. Continue?" (color #FFD600). PrimaryButton "Overwrite" → step='submitting' + mutation; PrimaryButton variant="outline" label="Cancel" → onCancel.
    5. step='submitting': Render ActivityIndicator + Text "Creating assignment...". Call `useCreateAssignment().mutateAsync({ programId, clientId: selectedClient.uid, startDate })`. On success → step='done' + onComplete (parent dismisses modal). On error → render error message + retry button.

    `src/app/trainer/programs/index.tsx` — Replace placeholder. Imports usePrograms. Standard list pattern:
    - Header "Programs" + "+ Add" → router.push('/trainer/programs/new')
    - FlatList of programs showing name (text-white) + Text "${durationWeeks} weeks" (text-#888888 text-xs)
    - Tap row → router.push(`/trainer/programs/${item.id}`)
    - Empty state Text

    `src/app/trainer/programs/new.tsx` — Two-step create:
    Step A: Render ProgramMetaForm with submitLabel="Create program" → on submit, call useCreateProgram({ trainerId, input }) — gets back the new ID — then `router.replace(\`/trainer/programs/\${id}\`)`. Phase 2 keeps create as meta-only; users fill the grid on the edit screen. Alternative: inline the grid in new.tsx — but the cleaner UX is meta-first then grid-edit.

    `src/app/trainer/programs/[programId].tsx` — Edit + assign. `useLocalSearchParams<{ programId: string }>()`. Read program with `useProgram`. Read routines with `useRoutines()` to build routineNameMap (Record<routineId, name>).

    Layout:
    - SafeAreaView + ScrollView bg #0E0E0E
    - Section 1 — Meta: Text name (text-2xl text-white) + Text description + small "Edit metadata" button (opens an Edit modal or inline ProgramMetaForm)
    - Section 2 — Grid: WeekDayGrid weeks={data.weeks} routineNameMap={routineNameMap} onCellPress={(w, d) => { setActiveCell({ w, d }); dayPickerRef.current?.present(w, d); }}
    - Section 3 — Actions: PrimaryButton label="Assign to Client" onPress={() => setAssignModalVisible(true)} + PrimaryButton variant="outline" label="Delete Program" onPress={confirmDelete}
    - DayPickerSheet ref={dayPickerRef} onSelect={(w, d, choice) => { const newWeeks = [...data.weeks]; newWeeks[w].days[d] = choice; updateMutation.mutate({ id: programId, partial: { weeks: newWeeks } }); }}
    - AssignProgramModal visible={assignModalVisible} programId={programId} onComplete={() => { setAssignModalVisible(false); router.back(); }} onCancel={() => setAssignModalVisible(false)}

    Delete confirmation:
    - Alert.alert('Delete program', 'This cannot be undone. Active assignments will keep their snapshot.', [{ text: 'Cancel', style: 'cancel' }, { text: 'Delete', style: 'destructive', onPress: handleDelete }])

    Update on cell change: optimistic via TanStack Query is nice-to-have but for MVP simplicity, just trigger the mutation and let invalidateQueries refresh.

    Defense-in-depth for cross-trainer: getProgram returns null on permission-denied (catch in the service like Plan 02-02).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx jest --testPathPattern=src --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `grep -c "usePrograms" src/app/trainer/programs/index.tsx` >= 1
    - `grep -c "ProgramMetaForm" src/app/trainer/programs/new.tsx` >= 1
    - `grep -c "WeekDayGrid\\|DayPickerSheet" src/app/trainer/programs/\\[programId\\].tsx` >= 2
    - `grep -c "AssignProgramModal" src/app/trainer/programs/\\[programId\\].tsx` >= 1
    - `grep -c "findActiveAssignmentForClient" src/components/programs/AssignProgramModal.tsx` >= 1  (ASGN-02 overwrite check)
    - `grep -c "useCreateAssignment" src/components/programs/AssignProgramModal.tsx` >= 1
    - `grep -c "YYYY-MM-DD\\|\\\\d{4}-\\\\d{2}-\\\\d{2}" src/components/programs/AssignProgramModal.tsx` >= 1  (ASGN-04)
    - `grep -c "Alert.alert" src/app/trainer/programs/\\[programId\\].tsx` >= 1  (delete confirm)
    - `grep -c "useClients" src/components/programs/ClientPickerSheet.tsx` >= 1
    - `npx jest --testPathPattern=src --passWithNoTests` exits 0
  </acceptance_criteria>
  <done>
    Trainer can create a program, edit its grid by tapping cells and picking routine/rest, delete it with confirmation, and assign it to a client with start date + overwrite warning. Full content pipeline works end-to-end up to assignment submission.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Deploy indexes + Cloud Function, then verify the full Phase 2 user journey on a real device</name>
  <what-built>
    The full Phase 2 content pipeline is now buildable. Before phase verification (`/gsd-verify-work`), the trainer must:
    1. Deploy the new Firestore indexes (3 added in Plan 02-01: exercises/routines/programs by trainerId+name)
    2. Deploy the new Cloud Function `createAssignment`
    3. Run the end-to-end smoke test that validates the "3-minute trainer journey" core value (PROJECT.md)

    Automated coverage from prior tasks:
    - Type contracts compile
    - Schema unit tests green
    - Service + hook unit tests green
    - Cloud Function unit tests green
    - Firestore rules tests green for Phase 2 cross-trainer denial

    Cannot be automated:
    - Index build time on Firebase (can take minutes)
    - Cloud Function deployment success
    - End-to-end UX on real device (5-tab nav → exercise create → routine build with drag → program build with grid → assign with overwrite warning)
    - Snapshot integrity verification (open Firestore console, inspect assignments/{id}/snapshot has full nested data)
  </what-built>
  <how-to-verify>
    Step 1 — Deploy indexes:
    ```
    firebase deploy --only firestore:indexes
    ```
    Watch the console output. Indexes typically take 1-5 minutes to build. You can also check status in the Firebase Console → Firestore → Indexes tab. All 3 new indexes (exercises, routines, programs trainerId+name) should reach "Enabled" status before continuing.

    Step 2 — Deploy Cloud Function:
    ```
    cd functions && npm run build && firebase deploy --only functions:createAssignment
    ```
    Wait for "Deploy complete!" and verify in Firebase Console → Functions that `createAssignment` is listed with v1 runtime.

    Step 3 — Build and launch dev client:
    ```
    npx expo start --dev-client
    ```
    Open the app on a real device with EAS dev client installed.

    Step 4 — End-to-end smoke test (the 3-minute trainer journey):

    Sign in as a trainer (use an existing trainer account from Phase 1, or create one if needed).

    4a. Verify 5-tab shell renders: Clients | Exercises | Routines | Programs | Profile in that order.

    4b. Exercise Tab: Tap +Add. Create 3 exercises:
        - "Back Squat", category=strength, locationTypes=[gym], sets=5, reps=5, rest=90, video URL=https://youtube.com/watch?v=test
        - "Push-ups", category=strength, locationTypes=[home, gym], sets=3, reps=15, rest=60
        - "Bodyweight Squat", category=functional, locationTypes=[home], sets=3, reps=20, rest=45 (will be used as alt for Back Squat)
        Verify all 3 appear in the list sorted alphabetically: Back Squat, Bodyweight Squat, Push-ups.
        Type "squa" in search — verify only Back Squat and Bodyweight Squat appear.
        Tap "gym" filter chip — verify Bodyweight Squat disappears.
        Clear filters.

    4c. Routine Tab: Tap +Add. Name="Full Body A". Tap +Add Exercises, select all 3 exercises, Confirm.
        On Back Squat row, tap "+ Add alternative" → pick Bodyweight Squat → verify "Alt: Bodyweight Squat" appears.
        Add note "Slow eccentric" on Back Squat.
        Long-press the drag handle on Push-ups, drag to first position. Verify smooth no-flicker drag (Task 04-4 verified this for the routine builder).
        Tap "Create routine". Verify the routine appears in the list.

    4d. Program Tab: Tap +Add. Name="Test Program", description="Smoke test", durationWeeks=2. Create.
        On the edit screen, verify the grid renders 2 rows × 7 cells, all showing REST.
        Tap cell (W1, D1) → DayPickerSheet opens → pick "Full Body A" → cell now shows "Full B" (truncated).
        Tap cell (W1, D3) → pick "Full Body A".
        Tap cell (W2, D1) → tap "Mark as Rest" → cell shows REST.

    4e. Assignment flow:
        On the program edit screen, tap "Assign to Client".
        AssignProgramModal opens at step=pickClient. Tap "Choose client" → ClientPickerSheet opens.
        Pick a client. Modal advances to step=pickDate.
        Enter startDate = today's date in YYYY-MM-DD format. Tap Continue.
        - If this client has no existing assignment: modal goes straight to step=submitting then done. Verify a green success state, then modal dismisses.
        - If client has an existing active assignment (e.g., from a prior smoke test): step=confirmOverwrite renders with the warning text mentioning the existing program name. Tap "Overwrite".
        Wait for the Cloud Function call to complete (1-3 seconds). Modal dismisses.

    4f. Verify in Firestore Console:
        Open Firebase Console → Firestore → assignments collection.
        Find the newly created assignment doc.
        Confirm fields: trainerId, clientId, programId, status='active', startDate (YYYY-MM-DD), snapshot.
        Drill into snapshot.weeks[0].days[0].routine.exercises — verify each exercise has full data including alternativeExercise for Back Squat. This proves ASGN-03 (immutable server-built snapshot).
        Confirm any previous active assignment for the same clientId now has status='completed'.

    4g. Verify Clients tab reflects assignment:
        Go back to Clients tab.
        Find the client you just assigned to.
        Verify the row shows the program name in accent green (Plan 02-03 CLNT-02 + active assignment query).
        Tap the client — profile shows Active Program section with the program name and start date.

    Step 5 — Cleanup (optional):
        If you want to re-run the smoke test cleanly, delete the test exercises/routines/programs via the UI or via Firestore Console.

    Reply with one of:
    - "approved" if all 7 sub-steps work
    - Describe specific failures (e.g., "Cloud Function returned permission-denied", "drag flickered on iPhone SE", "snapshot missing alternativeExercise field") for triage
  </how-to-verify>
  <resume-signal>Type "approved" after completing the end-to-end smoke test, or describe issues for triage</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trainer client → createAssignment Cloud Function | Untrusted input (programId, clientId, startDate); CF validates ownership and format |
| Cloud Function → Firestore (admin SDK) | Bypasses rules; snapshot built server-side from authoritative reads |
| trainer client → assignments collection | Read via findActiveAssignmentForClient; rules deny cross-trainer |
| trainer client → programs collection | where(trainerId) + rules; same pattern as exercises |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Elevation of Privilege | Non-trainer calling createAssignment | mitigate | CF step 2: reads users/${context.auth.uid} and rejects if role !== 'trainer'. Tested in Task 1. |
| T-02-XPROG | Tampering | Trainer assigning another trainer's program | mitigate | CF step 3: reads programs/${data.programId} and rejects if program.trainerId !== context.auth.uid. Tested in Task 1. |
| T-02-XCLIENT | Tampering | Trainer assigning a client they don't own | mitigate | CF step 4: reads users/${data.clientId} and rejects if client.trainerId !== context.auth.uid OR role !== 'client'. Tested in Task 1. |
| T-02-SNAP-SPOOF | Tampering | Client sends fabricated snapshot | mitigate | CF accepts only { programId, clientId, startDate } per CONTEXT.md D-CF signature. Snapshot is built server-side from Firestore reads. Client cannot inject snapshot content. |
| T-02-SNAP-SIZE | Denial of Service | Snapshot exceeds 1 MiB Firestore limit | mitigate | CF Task 1 step 7: `if (JSON.stringify(snapshot).length > 800_000) throw HttpsError('failed-precondition')`. Pre-emptive at 800KB to leave headroom. Pitfall 6. |
| T-02-DATE-FORMAT | Tampering | startDate as Timestamp or invalid format breaking Phase 3 calc | mitigate | CF Task 1 step 5: regex `/^\d{4}-\d{2}-\d{2}$/` validates ASGN-04. Phase 1 STATE.md decision reinforced. |
| T-02-OVERWRITE | Tampering | Silent overwrite of active assignment without trainer awareness | mitigate | AssignProgramModal (Task 3) calls findActiveAssignmentForClient BEFORE the CF call; if non-null, confirmOverwrite step requires explicit "Overwrite" button. CF also marks previous active as 'completed' atomically (no orphan active assignments). |
| T-02-INDEX-DOS | Denial of Service | Missing index causes user-visible Firestore error on list queries | mitigate | Indexes for programs (trainerId+name) added in Plan 02-01 + deployed in Task 4. |
| T-02-CF-V2-AUTH | Repudiation | v2 onCall losing context.auth | mitigate | CF written as v1 per Pitfall 5 (STATE.md Phase 1 decision). Same pattern as createClientAccount. |
</threat_model>

<verification>
- Cloud Function createAssignment compiles, has 7 unit test cases, deploys to Firebase
- Snapshot shape exactly matches CONTEXT.md D-4 (verified in Firestore console during smoke test)
- All 10 requirements (PROG-01..06, ASGN-01..04) traceable to specific tasks/files
- ASGN-02 overwrite warning surfaces in real UX (not just code path)
- ASGN-03 snapshot immutability: editing a program after assignment does NOT change the assignment's snapshot (verify by editing program post-assignment + re-inspecting assignment doc)
- ASGN-04 YYYY-MM-DD format enforced client-side (regex) AND server-side (CF rejects non-matching)
</verification>

<success_criteria>
- A trainer can complete the full content pipeline (create exercise → build routine → assemble program → assign to client) in under 3 minutes (PROJECT.md core value)
- The assignments collection contains a real document with a complete nested snapshot
- The client whose ID was used in assignment now appears in the Clients tab with the program name highlighted (per Plan 02-03 CLNT-02 wiring)
- Editing the original program after assignment does NOT change the assignment snapshot (verified manually post-smoke-test)
- All Phase 2 requirements (27 total) traceable to specific tasks across 02-01..02-05
</success_criteria>

<output>
Create `.planning/phases/02-trainer-content-creation/02-05-SUMMARY.md` when done — record: Cloud Function deployment status, indexes deployed (build time), end-to-end smoke test result with assignmentId from Firestore + screenshot description, any deviations from CONTEXT.md decisions.
</output>
