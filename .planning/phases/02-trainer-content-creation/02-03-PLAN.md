---
phase: 02-trainer-content-creation
plan: 03
type: execute
wave: 2
depends_on: ['02-01']
files_modified:
  - src/services/client.service.ts
  - src/services/__tests__/client.service.test.ts
  - src/hooks/useClients.ts
  - src/hooks/useClient.ts
  - src/hooks/useUpdateClient.ts
  - src/hooks/useActiveAssignment.ts
  - src/components/clients/ClientListItem.tsx
  - src/components/clients/ClientPhoto.tsx
  - src/app/trainer/clients/index.tsx
  - src/app/trainer/clients/[clientId].tsx
  - src/app/trainer/clients/add.tsx
  - firestore.rules
  - firestore/__tests__/rules.test.ts
autonomous: true
requirements:
  - CLNT-02
  - CLNT-03
  - CLNT-04
  - CLNT-05

must_haves:
  truths:
    - "Trainer can see a list of their clients showing name, photo (or placeholder), active program name, and a 'No active program' indicator when none assigned"
    - "Trainer can tap a client row to view their profile (active program name, start date placeholder, sessions placeholder for Phase 4)"
    - "Trainer can edit a client's name from the profile screen and the change persists"
    - "Clients whose trainerId does not match the signed-in trainer never appear in the list"
    - "Trainer can tap Add Client, enter name/email/temporary password, and the new client account appears in the Clients list"
  artifacts:
    - path: "src/services/client.service.ts"
      provides: "listClients, getClient, updateClientProfile (name only — photo is Phase 4)"
      exports: ["listClients", "getClient", "updateClientProfile"]
    - path: "src/hooks/useClients.ts"
      provides: "TanStack Query for the trainer's clients with active program join"
      exports: ["useClients"]
    - path: "src/hooks/useActiveAssignment.ts"
      provides: "Query active assignment for a given clientId (used by list rows AND by 02-04 assignment overwrite check)"
      exports: ["useActiveAssignment", "findActiveAssignmentForClient"]
    - path: "src/app/trainer/clients/index.tsx"
      provides: "Client list (CLNT-02, CLNT-05)"
      min_lines: 30
    - path: "src/app/trainer/clients/[clientId].tsx"
      provides: "Client profile + edit name (CLNT-03, CLNT-04)"
      min_lines: 30
    - path: "src/app/trainer/clients/add.tsx"
      provides: "Add client form — calls createClientAccount CF (Phase 1 deliverable, surfaced now)"
      min_lines: 30
    - path: "src/components/clients/ClientPhoto.tsx"
      provides: "expo-image avatar with placeholder fallback"
      exports: ["ClientPhoto"]
    - path: "firestore.rules"
      provides: "Trainer-reads-own-client rule + trainer-updates-client-name rule (CLNT-04 enablement)"
      contains: "isTrainer() && resource.data.trainerId == request.auth.uid"
  key_links:
    - from: "src/hooks/useClients.ts"
      to: "usersCollection() in src/firebase/firestore.ts"
      via: "Firestore .where('role', '==', 'client').where('trainerId', '==', uid).get()"
      pattern: "where\\('trainerId', '==', uid\\)"
    - from: "src/components/clients/ClientListItem.tsx"
      to: "src/hooks/useActiveAssignment.ts"
      via: "Per-row useActiveAssignment(clientId) for the active program label + CLNT-05 indicator"
      pattern: "useActiveAssignment"
    - from: "src/app/trainer/clients/add.tsx"
      to: "src/firebase/functions.ts (createClientAccountCallable)"
      via: "httpsCallable('createClientAccount') from Phase 1"
      pattern: "createClientAccount"
    - from: "src/app/trainer/clients/[clientId].tsx"
      to: "firestore.rules (users update rule for trainers)"
      via: "updateClientProfile() write authorized by trainer-update-client rule"
      pattern: "affectedKeys\\(\\)\\.hasAny\\(\\['role', 'trainerId', 'email'\\]\\)"
---

<objective>
Build the trainer's Clients tab — the primary landing screen per D-1 (CONTEXT.md decision: Clients tab becomes the trainer dashboard). Covers four client-management requirements:

- CLNT-02: Client list with name, photo, active program — sets up the structure (adherence percentage is Phase 4 HIST-04 per CONTEXT.md Out of Scope)
- CLNT-03: Tap a client to view profile (active program + start date; session history is Phase 4 — leave placeholder section)
- CLNT-04: Edit a client's name (photo upload is Phase 4 PROF-02/03 — leave the photo as a read-only ClientPhoto with placeholder). This plan amends `firestore.rules` to allow trainers to update their own clients' name/photoURL while preserving the role-elevation defense.
- CLNT-05: Visual indicator when a client has no active program (yellow dot + "No active program" label)

Also surfaces the Phase-1-deliverable `createClientAccount` Cloud Function in a trainer-facing add screen (Phase 1 deployed the function but did not wire a screen — this plan adds that screen). This is necessary to manually create test clients for verifying ASGN-01 in Plan 02-04.

Wave-2 parallel: This plan depends only on Plan 02-01 (types, nav shell, firebase service refs) — does NOT depend on exercises. Touches `src/app/trainer/clients/`, `src/services/client.service.ts`, `src/hooks/{useClients,useClient,useUpdateClient,useActiveAssignment}.ts`, plus `firestore.rules` and its emulator tests. Zero file overlap with Plan 02-02. Runs in parallel.

Output: Fully functional Clients tab with list, detail, edit-name (server-enforced), and add-client screens. The `useActiveAssignment` hook is also exported here because Plan 02-04 (programs) needs it for the ASGN-02 overwrite warning — defining it once avoids duplication. firestore.rules gains two new authorizations: trainers can READ their own clients' user docs and UPDATE non-privileged fields (name, photoURL) without affecting role/trainerId/email.
</objective>

<execution_context>
@/Users/FF-rodrigo/Documents/Personal/LauFit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/FF-rodrigo/Documents/Personal/LauFit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-trainer-content-creation/02-CONTEXT.md
@.planning/phases/02-trainer-content-creation/02-RESEARCH.md
@.planning/phases/02-trainer-content-creation/02-01-SUMMARY.md
@CLAUDE.md
@src/types/user.ts
@src/types/assignment.ts
@src/firebase/firestore.ts
@src/firebase/functions.ts
@src/services/user.service.ts
@src/stores/authStore.ts
@src/components/ui/TextField.tsx
@src/components/ui/PrimaryButton.tsx
@firestore.rules
@firestore/__tests__/rules.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Client service + clients/active-assignment query hooks + tests</name>
  <files>
    src/services/client.service.ts,
    src/services/__tests__/client.service.test.ts,
    src/hooks/useClients.ts,
    src/hooks/useClient.ts,
    src/hooks/useUpdateClient.ts,
    src/hooks/useActiveAssignment.ts
  </files>
  <read_first>
    src/services/user.service.ts,
    src/firebase/firestore.ts,
    src/types/user.ts,
    src/types/assignment.ts,
    src/stores/authStore.ts,
    .planning/phases/02-trainer-content-creation/02-RESEARCH.md (Pattern 2 — TanStack Query v5 + Firestore; section "Active assignment query in CLNT-02/05"),
    firestore.indexes.json (assignments index: clientId+status)
  </read_first>
  <behavior>
    `src/services/__tests__/client.service.test.ts`:
    - Test: `listClients(trainerId)` calls `usersCollection().where('role', '==', 'client').where('trainerId', '==', trainerId).orderBy('name').get()` and returns `User[]` with role='client' and the requested trainerId
    - Test: `getClient(uid)` returns the User doc or null
    - Test: `updateClientProfile(uid, { name })` calls `.doc(uid).update({ name })` (photo deferred to Phase 4)
    - Test: `findActiveAssignmentForClient(clientId)` calls `assignmentsCollection().where('clientId', '==', clientId).where('status', '==', 'active').limit(1).get()` and returns the assignment or null — used by 02-04 ASGN-02 overwrite check
  </behavior>
  <action>
    `src/services/client.service.ts` — pure async functions:
    - `listClients(trainerId: string): Promise<User[]>` — `usersCollection().where('role', '==', 'client').where('trainerId', '==', trainerId).orderBy('name', 'asc').get()`. Map docs to `{ ...data, uid: d.id }`. NOTE: User type already has `uid` field — ensure mapping is consistent.
    - `getClient(uid: string): Promise<User | null>` — `usersCollection().doc(uid).get()` → `data ? { ...data, uid: d.id } : null`
    - `updateClientProfile(uid: string, partial: { name?: string }): Promise<void>` — `usersCollection().doc(uid).update(partial)`. This call is now authorized by the firestore.rules amendment added in Task 3 of this plan (trainer-updates-client rule). The rule allows the signed-in trainer to update any non-privileged field on their own client's doc — `role`, `trainerId`, and `email` remain locked by the role-elevation defense.
    - `findActiveAssignmentForClient(clientId: string): Promise<Assignment | null>` — `assignmentsCollection().where('clientId', '==', clientId).where('status', '==', 'active').limit(1).get()` → first doc or null. The composite index (clientId ASC + status ASC) is ALREADY in firestore.indexes.json (line 11-18). Used by both CLNT-02 list rows and Plan 02-04 ASGN-02 overwrite check — single source of truth.

    Hooks per TanStack Query v5 single-object API:

    `src/hooks/useClients.ts` — `export function useClients()` reads `trainerId = useAuthStore((s) => s.uid)` (a trainer's uid IS the trainerId). Returns `useQuery({ queryKey: ['clients', trainerId], queryFn: () => listClients(trainerId!), enabled: !!trainerId, staleTime: 30_000 })`.

    `src/hooks/useClient.ts` — `export function useClient(uid: string | undefined)` returns `useQuery({ queryKey: ['client', uid], queryFn: () => getClient(uid!), enabled: !!uid })`.

    `src/hooks/useUpdateClient.ts` — `useMutation({ mutationFn: ({ uid, partial }) => updateClientProfile(uid, partial), onSuccess: (_, { uid }) => { queryClient.invalidateQueries({ queryKey: ['clients', trainerId] }); queryClient.invalidateQueries({ queryKey: ['client', uid] }); } })`.

    `src/hooks/useActiveAssignment.ts` — export both `useActiveAssignment(clientId)` hook AND the underlying `findActiveAssignmentForClient` re-export from the service. The hook: `useQuery({ queryKey: ['activeAssignment', clientId], queryFn: () => findActiveAssignmentForClient(clientId!), enabled: !!clientId, staleTime: 10_000 })`.
  </action>
  <verify>
    <automated>npx jest --testPathPattern=client.service --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `npx jest --testPathPattern=client.service` passes — 4 test cases green
    - `grep -c "where('role', '==', 'client')" src/services/client.service.ts` >= 1
    - `grep -c "where('trainerId', '==', trainerId)" src/services/client.service.ts` >= 1
    - `grep -c "where('clientId', '==', clientId)" src/services/client.service.ts` >= 1
    - `grep -c "where('status', '==', 'active')" src/services/client.service.ts` >= 1
    - `grep -c "findActiveAssignmentForClient" src/services/client.service.ts` >= 1
    - `grep -c "findActiveAssignmentForClient\\|useActiveAssignment" src/hooks/useActiveAssignment.ts` >= 2
  </acceptance_criteria>
  <done>
    Service + hooks compile, listClients filters by role+trainerId, findActiveAssignmentForClient exists and is consumed by both Plan 02-03 (CLNT-02 list rows) and Plan 02-04 (ASGN-02 overwrite check). `updateClientProfile` is authorized by the trainer-update-client rule added in Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 2: ClientPhoto component + ClientListItem with active-program indicator</name>
  <files>
    src/components/clients/ClientPhoto.tsx,
    src/components/clients/ClientListItem.tsx
  </files>
  <read_first>
    src/types/user.ts,
    src/types/assignment.ts,
    src/components/ui/PrimaryButton.tsx,
    src/hooks/useActiveAssignment.ts,
    .planning/phases/02-trainer-content-creation/02-RESEARCH.md (Pattern 8 — expo-image; Open Question 3 — photoURL availability)
  </read_first>
  <action>
    `src/components/clients/ClientPhoto.tsx` — Props: `{ photoURL?: string | null; name: string; size?: number }`. Default size 48.

    Per CONTEXT.md decision "Image: `expo-image` for client profile photos" and RESEARCH.md Pattern 8: import `Image` from `expo-image`. If photoURL is truthy, render `<Image source={photoURL} style={{ width: size, height: size, borderRadius: size / 2 }} contentFit="cover" transition={200} />`. If photoURL is null/undefined (the expected MVP state per Open Question 3), render a circular View bg #1A1A1A border #444 with the first letter of the name centered in #00FF66.

    `src/components/clients/ClientListItem.tsx` — Props: `{ client: User; onPress: () => void }`. Internally calls `useActiveAssignment(client.uid)` to read the active assignment in parallel with the list query. Renders:
    - Pressable row: bg #1A1A1A, border #444, p-4, rounded-lg, mb-2
    - Horizontal flex: ClientPhoto on left (size 48), middle column with name (text-white text-base font-semibold) + active program label (text-#888888 text-xs)
    - CLNT-05 indicator: if `activeAssignment.data === null` after isLoading=false, show a small yellow dot (View bg #FFD600 width 8 height 8 rounded-full) AND text "No active program" (color #FFD600 text-xs)
    - If `activeAssignment.data` is non-null, show `data.snapshot.name` as the program label (color #00FF66 text-xs)
    - While `activeAssignment.isLoading`, show "…" (color #888888) for the program label slot

    Use a small inline component for the "no program" indicator so the visual is unambiguous (per CLNT-05 "visual indicator").
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `grep -c "from 'expo-image'" src/components/clients/ClientPhoto.tsx` == 1
    - `grep -c "useActiveAssignment" src/components/clients/ClientListItem.tsx` >= 1
    - `grep -c "No active program" src/components/clients/ClientListItem.tsx` >= 1  (CLNT-05 indicator)
    - `grep -c "FFD600" src/components/clients/ClientListItem.tsx` >= 1  (yellow indicator)
    - `grep -c "snapshot.name" src/components/clients/ClientListItem.tsx` >= 1  (active program label)
  </acceptance_criteria>
  <done>
    ClientPhoto handles both null and present photoURL with placeholder fallback; ClientListItem queries active assignment per row and shows either the program name (accent green) or "No active program" indicator (yellow) per CLNT-05.
  </done>
</task>

<task type="auto">
  <name>Task 3: Clients list, profile, add-client screens + firestore.rules amendment for CLNT-04</name>
  <files>
    src/app/trainer/clients/index.tsx,
    src/app/trainer/clients/[clientId].tsx,
    src/app/trainer/clients/add.tsx,
    firestore.rules,
    firestore/__tests__/rules.test.ts
  </files>
  <read_first>
    src/app/trainer/clients/index.tsx,
    src/app/trainer/clients/_layout.tsx,
    src/components/clients/ClientListItem.tsx,
    src/components/clients/ClientPhoto.tsx,
    src/components/ui/TextField.tsx,
    src/components/ui/PrimaryButton.tsx,
    src/hooks/useClients.ts,
    src/hooks/useClient.ts,
    src/hooks/useUpdateClient.ts,
    src/hooks/useActiveAssignment.ts,
    src/firebase/functions.ts,
    src/validation/auth.schema.ts (pattern for new client zod schema),
    firestore.rules (Phase 1 users block + role-elevation defense at lines 43-73),
    firestore/__tests__/rules.test.ts (existing harness pattern + Phase 2 cross-trainer block added in 02-01)
  </read_first>
  <action>
    **STEP 3a — firestore.rules amendment (REQUIRED for CLNT-04 to function end-to-end):**

    Edit `firestore.rules`, inside the `match /users/{userId}` block (currently lines 43-73). Preserve all existing rules. Add the following two new rules:

    1. Trainer-reads-client rule (allow READ on /users/{clientId} when the requester is the owning trainer). Add immediately after the existing `allow read: if isSignedIn() && request.auth.uid == userId;` line:

       `allow read: if isTrainer() && resource.data.trainerId == request.auth.uid;`

       Add a comment above it: "// Trainer can read their own client's user doc — needed for CLNT-02 list, CLNT-03 profile, CLNT-04 edit name."

    2. Trainer-updates-client rule (allow UPDATE on /users/{clientId} when the requester is the owning trainer AND no privileged field is in the diff). Add immediately after the existing owner-update rule (currently lines 66-70):

       `allow update: if isTrainer() && resource.data.trainerId == request.auth.uid && !request.resource.data.diff(resource.data).affectedKeys().hasAny(['role', 'trainerId', 'email']);`

       Add a comment above it: "// Trainer can update non-privileged fields (name, photoURL) on their own client's doc. Role/trainerId/email remain locked by the role-elevation defense — diff().affectedKeys().hasAny() denies the entire update if any privileged key changes."

    Do NOT modify the existing owner-update rule, role-elevation defense, create rules, or delete rule. Do NOT relax the `email` lock — email change requires Firebase Auth update too and is out of scope for CLNT-04.

    **STEP 3b — Extend `firestore/__tests__/rules.test.ts`:**

    Add a new `describe('Phase 2 — CLNT-04 trainer reads/updates own client')` block at the end of the file (alongside the cross-trainer denial block added in 02-01). Test cases — use the existing `getAuthedFirestore(uid)` harness, seed two trainers and two clients (one client per trainer) with `withSecurityRulesDisabled`, then assert with rules enabled:

    1. `trainer can read own client's user doc` — trainer A reads /users/clientA → `assertSucceeds`
    2. `trainer cannot read other trainer's client doc` — trainer A reads /users/clientB (trainer B's client) → `assertFails`
    3. `trainer can update own client's name` — trainer A updates /users/clientA `{ name: 'New Name' }` → `assertSucceeds`
    4. `trainer cannot update own client's role` — trainer A attempts `{ name: 'X', role: 'trainer' }` on /users/clientA → `assertFails` (role-elevation defense holds)
    5. `trainer cannot update own client's trainerId` — trainer A attempts `{ trainerId: 'someoneElse' }` on /users/clientA → `assertFails`
    6. `trainer cannot update own client's email` — trainer A attempts `{ email: 'new@example.com' }` on /users/clientA → `assertFails`
    7. `trainer cannot update other trainer's client doc` — trainer A attempts `{ name: 'Hijack' }` on /users/clientB → `assertFails`

    **STEP 3c — UI screens (unchanged from prior revision — now backed by working server-side authorization):**

    `src/app/trainer/clients/index.tsx` — Replace placeholder. Component imports `useClients`. Layout:
    - SafeAreaView + View flex:1 bg #0E0E0E
    - Header: Text "Clients" (text-2xl text-white font-bold) + PrimaryButton label="+ Add Client" onPress=router.push('/trainer/clients/add')
    - FlatList: data=useClients().data ?? [], keyExtractor=item.uid, renderItem=`<ClientListItem client={item} onPress={() => router.push(\`/trainer/clients/\${item.uid}\`)} />`
    - ListEmptyComponent: View centered with Text "No clients yet — tap Add Client to create one." (color #888888)
    - Loading: ActivityIndicator #00FF66 when isLoading

    `src/app/trainer/clients/[clientId].tsx` — `useLocalSearchParams<{ clientId: string }>()`. Two queries: `useClient(clientId)` and `useActiveAssignment(clientId)`. If client is loading show spinner; if null show "Client not found".

    Layout:
    - SafeAreaView + ScrollView bg #0E0E0E px-4
    - Header section: ClientPhoto size 96 centered + Text name (text-2xl text-white text-center) + Text email (text-#888888 text-sm text-center)
    - Edit name section: TextField label="Name" value (local state initialized from `client.name`) + PrimaryButton label="Save Name" onPress=updateClientMutation. On mutation success, show a brief inline confirmation ("Saved" in #00FF66 for 2 seconds). On mutation failure, render the error message below the button (this should now be the rare-error path — the rule allows the update — but still handle network errors gracefully).
    - Active program section: if activeAssignment.data, show: Text "Active Program" (color #888888 text-sm) + Text snapshot.name (color #00FF66 text-lg) + Text "Started: " + startDate (text-white text-sm). If null, show Text "No active program assigned" (color #FFD600).
    - Session history section: placeholder Text "Session history coming in Phase 4." (color #888888 italic). This documents CLNT-03's session list deferral per the Phase 4 mapping.

    `src/app/trainer/clients/add.tsx` — Form to call `createClientAccount` Cloud Function (Phase 1 already deployed). Use RHF + a NEW small zod schema declared inline (or in a new file `src/validation/createClient.schema.ts` if you prefer). Schema: `{ name: z.string().min(1), email: z.email(), temporaryPassword: z.string().min(6) }`.

    Layout: SafeAreaView + ScrollView with TextField for each field + PrimaryButton label="Create Client" onPress=handleSubmit. On submit, call `createClientAccountCallable({ name, email, temporaryPassword })` from `src/firebase/functions.ts`, then `queryClient.invalidateQueries({ queryKey: ['clients', trainerId] })`, then `router.back()`. On error (e.g., already-exists), surface `error.message` in a red text below the form.

    Verify that `src/firebase/functions.ts` already exports `createClientAccountCallable` (Phase 1 Plan 04). If it doesn't, add the export following the pattern shown in RESEARCH.md "Verified: React Native Firebase onCall callable pattern".

    Use `import { useRouter, useLocalSearchParams, Stack } from 'expo-router'`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx jest --testPathPattern=src --passWithNoTests && firebase emulators:exec --only firestore "npx jest --testPathPattern=firestore/__tests__/rules --passWithNoTests"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `grep -c "ClientListItem" src/app/trainer/clients/index.tsx` >= 1
    - `grep -c "useClients" src/app/trainer/clients/index.tsx` >= 1
    - `grep -c "useClient\\|useActiveAssignment\\|useUpdateClient" src/app/trainer/clients/\\[clientId\\].tsx` >= 3
    - `grep -c "ClientPhoto" src/app/trainer/clients/\\[clientId\\].tsx` >= 1
    - `grep -c "createClientAccount" src/app/trainer/clients/add.tsx` >= 1
    - `grep -c "z.email\\|z.string().email" src/app/trainer/clients/add.tsx src/validation/createClient.schema.ts 2>/dev/null` >= 1
    - `npx jest --testPathPattern=src --passWithNoTests` exits 0
    - `grep -vc '^[[:space:]]*//' firestore.rules | head -1` — sanity (file still parses)
    - `grep -c "isTrainer() && resource.data.trainerId == request.auth.uid" firestore.rules` >= 2  (one for read, one for update on users)
    - `grep -c "hasAny(\\['role', 'trainerId', 'email'\\])" firestore.rules` >= 1  (trainer-update privileged-field lock)
    - `grep -c "hasAny(\\['role', 'trainerId'\\])" firestore.rules` >= 1  (existing owner-update role-elevation defense preserved)
    - `firebase emulators:exec --only firestore "npx jest --testPathPattern=firestore/__tests__/rules --passWithNoTests"` exits 0 — all 7 new CLNT-04 cases plus prior cross-trainer cases pass
  </acceptance_criteria>
  <done>
    Trainer can navigate to Clients tab, see their list with name/photo/active-program/no-program-indicator, tap a client to view profile and successfully edit their name (server-side rule allows it; role/trainerId/email remain locked), and tap Add Client to create a new client via the Cloud Function. CLNT-04 ships fully — no Phase 4 deferral, no permission-denied UX path. Plan 02-04 can consume `useActiveAssignment` for the ASGN-02 overwrite check.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trainer client → Firestore users query | Restricted to where(role='client') + where(trainerId=uid); rules enforce trainer-reads-own-client on the server |
| trainer client → Cloud Function createClientAccount | v1 onCall — server-side role check ensures only trainers can create clients (Phase 1 mitigation, reused) |
| trainer client → updateClientProfile | Authorized by new trainer-update-client rule; role/trainerId/email locked by diff().affectedKeys().hasAny() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information Disclosure | listClients query exposing other trainers' clients | mitigate | Service includes `where('role', '==', 'client')` AND `where('trainerId', '==', uid)`. Server-side: new firestore.rules trainer-reads-client rule allows reads ONLY when `resource.data.trainerId == request.auth.uid`. Rules tests in Task 3 prove trainer A cannot read trainer B's client. |
| T-02-04 | Elevation of Privilege | Add client form passing forged trainerId to CF | mitigate | createClientAccount Cloud Function (Phase 1) sets trainerId server-side from context.auth.uid, not from request data. Threat already mitigated upstream. |
| T-02-CLNT-EDIT | Elevation of Privilege | Trainer attempts to escalate their client to trainer role via updateClientProfile | mitigate | New firestore.rules trainer-update rule denies any update whose diff `.affectedKeys().hasAny(['role', 'trainerId', 'email'])`. Tested by rules.test.ts cases 4, 5, 6 in Task 3. Existing owner-update role-elevation defense (lines 66-70) is preserved untouched. |
| T-02-CLNT-XACCESS | Tampering | Trainer A attempts to update Trainer B's client doc | mitigate | New trainer-update rule requires `resource.data.trainerId == request.auth.uid`. Tested by rules.test.ts case 7 in Task 3. |
</threat_model>

<verification>
- Plan 02-01 frontmatter requirements field includes CLNT-05; this plan covers CLNT-02, CLNT-03, CLNT-04, CLNT-05 in screen code AND in rules
- Smoke test: with a trainer account that has 2 clients (one with an active assignment, one without), the list shows the active-program name in green for the first and "No active program" in yellow for the second
- Add Client form successfully calls the Cloud Function and the new client appears in the list after invalidation
- Trainer cannot see clients belonging to a different trainer (rule + query filter — proven by rules tests)
- Edit-name UX writes successfully end-to-end; rules tests confirm role/trainerId/email cannot be tampered with via this path
- firestore.rules deployment: this plan's amended rules must be deployed via `firebase deploy --only firestore:rules` before manual smoke testing on the dev client
</verification>

<success_criteria>
- Clients tab is the default landing on the trainer shell (via 02-01 redirect)
- listClients returns ≤ N clients owned by the signed-in trainer (orderBy name asc)
- CLNT-05 indicator: any client without an active assignment shows the yellow "No active program" badge
- Add Client form creates a new auth account + users doc + appears in the list
- CLNT-04 trainer-edits-client-name works end-to-end with no permission-denied path
- Plan 02-04 (programs) can `import { findActiveAssignmentForClient }` for ASGN-02 overwrite logic
- All 7 new CLNT-04 rules test cases pass under firebase emulator
</success_criteria>

<output>
Create `.planning/phases/02-trainer-content-creation/02-03-SUMMARY.md` when done — record: screens implemented, hook exports, firestore.rules amendments (lines added, comment text), rules test case count (CLNT-04 cases + prior), smoke test results (client count, active-program indicator visible, edit-name round-trip).
</output>
