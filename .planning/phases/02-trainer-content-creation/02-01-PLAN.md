---
phase: 02-trainer-content-creation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - firestore.indexes.json
  - src/types/exercise.ts
  - src/types/routine.ts
  - src/types/program.ts
  - src/types/assignment.ts
  - src/validation/exercise.schema.ts
  - src/validation/routine.schema.ts
  - src/validation/program.schema.ts
  - src/validation/__tests__/exercise.schema.test.ts
  - src/validation/__tests__/routine.schema.test.ts
  - src/validation/__tests__/program.schema.test.ts
  - src/validation/__tests__/exercise.filter.test.ts
  - src/firebase/firestore.ts
  - src/firebase/exerciseFilter.ts
  - src/firebase/__tests__/assignment.service.test.ts
  - src/app/trainer/_layout.tsx
  - src/app/trainer/index.tsx
  - src/app/trainer/clients/_layout.tsx
  - src/app/trainer/clients/index.tsx
  - src/app/trainer/exercises/_layout.tsx
  - src/app/trainer/exercises/index.tsx
  - src/app/trainer/routines/_layout.tsx
  - src/app/trainer/routines/index.tsx
  - src/app/trainer/programs/_layout.tsx
  - src/app/trainer/programs/index.tsx
  - firestore/__tests__/rules.test.ts
autonomous: false
requirements:
  - EXER-06
  - ROUT-07
  - PROG-06
  - CLNT-05
user_setup:
  - service: npm
    why: "Install two new JS-only packages for drag-and-drop and bottom sheet"
    env_vars: []
    dashboard_config: []

must_haves:
  truths:
    - "Trainer shell renders five tabs in order: Clients, Exercises, Routines, Programs, Profile"
    - "All Phase 2 type contracts (Exercise, Routine, Program, Assignment) are exported and importable across the codebase"
    - "Zod v4 schemas for exercise, routine, and program validate required fields and reject invalid input"
    - "Firestore composite indexes for exercises/routines/programs (trainerId ASC, name ASC) are declared in firestore.indexes.json"
    - "Wave 0 test stub files exist and fail-soft (no missing test errors) so subsequent plans have a feedback loop"
  artifacts:
    - path: "src/types/exercise.ts"
      provides: "Exercise + CreateExerciseInput + ExerciseCategory + LocationType types"
      contains: "export interface Exercise"
    - path: "src/types/routine.ts"
      provides: "Routine + RoutineExercise + CreateRoutineInput types"
      contains: "export interface Routine"
    - path: "src/types/program.ts"
      provides: "Program + ProgramWeek + ProgramDay types"
      contains: "export interface Program"
    - path: "src/types/assignment.ts"
      provides: "Assignment + AssignmentSnapshot + CreateAssignmentInput + CreateAssignmentResult types"
      contains: "export interface Assignment"
    - path: "src/validation/exercise.schema.ts"
      provides: "Zod v4 exerciseSchema + ExerciseFormValues type"
      exports: ["exerciseSchema", "ExerciseFormValues", "EXERCISE_CATEGORIES", "LOCATION_TYPES"]
    - path: "src/validation/routine.schema.ts"
      provides: "Zod v4 routineSchema + RoutineFormValues type"
      exports: ["routineSchema", "RoutineFormValues", "routineExerciseSchema"]
    - path: "src/validation/program.schema.ts"
      provides: "Zod v4 programSchema + ProgramFormValues type"
      exports: ["programSchema", "ProgramFormValues"]
    - path: "src/firebase/firestore.ts"
      provides: "Typed collection refs for exercises/routines/programs/assignments"
      exports: ["usersCollection", "exercisesCollection", "routinesCollection", "programsCollection", "assignmentsCollection"]
    - path: "src/firebase/exerciseFilter.ts"
      provides: "Pure client-side filter function for EXER-04/EXER-05 search + category/locationType filter"
      exports: ["filterExercises"]
    - path: "src/app/trainer/_layout.tsx"
      provides: "5-tab Tabs navigator"
      contains: "name=\"exercises\""
    - path: "firestore.indexes.json"
      provides: "3 new composite indexes (exercises/routines/programs by trainerId+name)"
      contains: "exercises"
  key_links:
    - from: "src/app/trainer/_layout.tsx"
      to: "src/app/trainer/{clients,exercises,routines,programs}/"
      via: "expo-router file-based tab registration"
      pattern: "Tabs.Screen name=\"(clients|exercises|routines|programs|profile)\""
    - from: "src/firebase/firestore.ts"
      to: "src/types/{exercise,routine,program,assignment}.ts"
      via: "TypeScript type imports for typed CollectionReference"
      pattern: "CollectionReference<(Exercise|Routine|Program|Assignment)>"
    - from: "src/validation/*.schema.ts"
      to: "src/types/*.ts"
      via: "z.infer<typeof schema> exports matching type contracts"
      pattern: "z.infer<typeof"
---

<objective>
Establish the foundational scaffolding that every subsequent Phase 2 plan depends on:

1. Install two new JS-only libraries (`react-native-reanimated-dnd`, `@gorhom/bottom-sheet`) needed for routine drag-reorder and program day picker.
2. Declare all Phase 2 TypeScript contracts (Exercise, Routine, Program, Assignment) so downstream plans extend, not redefine.
3. Create Zod v4 validation schemas with full test coverage (Wave 0 stubs from VALIDATION.md).
4. Add typed Firestore collection refs (exercises, routines, programs, assignments) following the established `usersCollection` pattern.
5. Add 3 new composite indexes (exercises/routines/programs by trainerId+name) to `firestore.indexes.json`.
6. Replace the current 2-tab trainer shell with a 5-tab shell (Clients | Exercises | Routines | Programs | Profile) and create stack layouts + placeholder index screens for all four new tabs.
7. Extend `firestore/__tests__/rules.test.ts` with Phase 2 cross-trainer denial coverage (EXER-06 + analogous rules for routines/programs/assignments).

Purpose: Wave 1 single source of truth — every Wave 2-4 plan reads these types, schemas, refs, and routes. Locks in Zod v4 patterns (`z.enum([...] as const)`, `z.coerce.number()`, `z.url()`) per CONTEXT.md so downstream plans copy-paste-safe. The package install requires a human checkpoint (must verify packages are legitimate before native module impacts the dev client).

Output: Compilable type system, passing schema tests, declared indexes (deploy in Wave 2), 5-tab nav shell with stubs, updated rules tests covering all Phase 2 collections.
</objective>

<execution_context>
@/Users/FF-rodrigo/Documents/Personal/LauFit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/FF-rodrigo/Documents/Personal/LauFit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-trainer-content-creation/02-CONTEXT.md
@.planning/phases/02-trainer-content-creation/02-RESEARCH.md
@.planning/phases/02-trainer-content-creation/02-VALIDATION.md
@CLAUDE.md
@src/types/user.ts
@src/firebase/firestore.ts
@src/validation/auth.schema.ts
@src/stores/authStore.ts
@src/app/trainer/_layout.tsx
@src/app/trainer/index.tsx
@firestore.rules
@firestore.indexes.json
@firestore/__tests__/rules.test.ts
@package.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify and install new packages (react-native-reanimated-dnd, @gorhom/bottom-sheet)</name>
  <what-built>
    Two new JS-only libraries are needed for Phase 2:
    - `react-native-reanimated-dnd@^2.0.0` — Sortable drag-and-drop for routine builder (ROUT-04). Per D-3 (CONTEXT.md), chosen over `react-native-draggable-flatlist` because installed Reanimated is v4.2.1 and reanimated-dnd is purpose-built for Reanimated v4 + worklets (both already in package.json).
    - `@gorhom/bottom-sheet@^5.2.14` — Bottom sheet for program day picker and exercise selection (per D-Libraries decision in CONTEXT.md). Peer dep `react-native-reanimated >= 3.16.0` satisfied by installed 4.2.1.

    Audit table from RESEARCH.md `## Package Legitimacy Audit`:
    - `react-native-reanimated-dnd` — npm, ~1 yr old (created 2025-05-28), ~28k weekly downloads, source github.com/entropyconquers/react-native-reanimated-dnd, no postinstall script — disposition `[ASSUMED]` (slopcheck unavailable, manual verification required).
    - `@gorhom/bottom-sheet` — npm, 6 years old, ecosystem standard, no postinstall script — disposition `[ASSUMED]` (slopcheck unavailable, manual verification required).

    Both are `[ASSUMED]` per RESEARCH.md and must be human-verified before install. Threat T-02-SC (supply chain) requires blocking-human checkpoint.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/react-native-reanimated-dnd in a browser. Confirm:
       - Maintainer: `entropyconquers`
       - Repository link points to `github.com/entropyconquers/react-native-reanimated-dnd`
       - Latest version is `2.0.0` (or later, do not approve majors past v2)
       - Weekly downloads are in the tens of thousands
       - No suspicious postinstall scripts visible in package.json on the GitHub repo

    2. Open https://www.npmjs.com/package/@gorhom/bottom-sheet in a browser. Confirm:
       - Maintainer: `gorhom`
       - Repository link points to `github.com/gorhom/react-native-bottom-sheet`
       - Latest version is `5.x` (do not approve `6.x` — breaking changes may affect Reanimated v4 compat)
       - Weekly downloads are very high (ecosystem standard)

    3. If both packages pass verification, reply "approved" and Claude will run:
       `npm install react-native-reanimated-dnd@^2.0.0 @gorhom/bottom-sheet@^5.2.14`

    4. If either looks suspicious or has been hijacked, reply with the issue and Claude will halt.
  </how-to-verify>
  <resume-signal>Type "approved" after verifying both npm pages, or describe any concern</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Phase 2 type contracts + Zod v4 schemas + Wave 0 test stubs + Firestore collection refs + indexes</name>
  <files>
    src/types/exercise.ts,
    src/types/routine.ts,
    src/types/program.ts,
    src/types/assignment.ts,
    src/validation/exercise.schema.ts,
    src/validation/routine.schema.ts,
    src/validation/program.schema.ts,
    src/validation/__tests__/exercise.schema.test.ts,
    src/validation/__tests__/routine.schema.test.ts,
    src/validation/__tests__/program.schema.test.ts,
    src/validation/__tests__/exercise.filter.test.ts,
    src/firebase/firestore.ts,
    src/firebase/exerciseFilter.ts,
    src/firebase/__tests__/assignment.service.test.ts,
    firestore.indexes.json,
    firestore/__tests__/rules.test.ts
  </files>
  <read_first>
    src/types/user.ts,
    src/firebase/firestore.ts,
    src/validation/auth.schema.ts,
    firestore.indexes.json,
    firestore/__tests__/rules.test.ts,
    firestore.rules,
    .planning/phases/02-trainer-content-creation/02-CONTEXT.md (sections "Decision 4 — Snapshot Implementation", "Decision 6 — Exercise Sort Order", and "Firestore Collections"),
    .planning/phases/02-trainer-content-creation/02-RESEARCH.md (sections "Pattern 3 — Zod v4 exercise form schema", "Pattern 4 — RHF + Zod field array", "Pitfall 2 — Zod v4 API differences", "Pitfall 7 — Composite index missing"),
    .planning/phases/02-trainer-content-creation/02-VALIDATION.md (Wave 0 Requirements section)
  </read_first>
  <behavior>
    Per VALIDATION.md Wave 0 Requirements — these tests must exist and fail RED initially, then pass GREEN after schema/filter implementation:

    `src/validation/__tests__/exercise.schema.test.ts` (covers EXER-01):
    - Test: rejects missing `name` (empty string fails)
    - Test: rejects invalid `category` value (not in enum) — covers threat T-02-06 input validation
    - Test: rejects empty `locationTypes` array (min 1)
    - Test: rejects negative `defaultSets`, `defaultReps`, `defaultDuration`, `defaultRest`
    - Test: coerces numeric strings to numbers ("5" -> 5 for defaultSets)
    - Test: rejects malformed `videoUrl` like "not-a-url" but accepts empty string and valid `https://youtube.com/watch?v=abc`
    - Test: accepts a minimal valid exercise (name + category + locationTypes only)

    `src/validation/__tests__/routine.schema.test.ts` (covers ROUT-01):
    - Test: rejects routine with empty `exercises` array (min 1)
    - Test: rejects routine entry with non-positive `sets`
    - Test: accepts routine entry with `reps` OR `duration` (either is sufficient)
    - Test: accepts optional `notes` and optional `alternativeExerciseId`

    `src/validation/__tests__/program.schema.test.ts` (covers PROG-01):
    - Test: rejects empty `name`
    - Test: rejects `durationWeeks` < 1
    - Test: rejects `durationWeeks` > 26 (MVP cap per snapshot size pitfall 6)
    - Test: coerces "8" -> 8 for durationWeeks
    - Test: optional `description` accepted as empty string or absent

    `src/validation/__tests__/exercise.filter.test.ts` (covers EXER-04, EXER-05):
    - Test: filters by case-insensitive substring match on `name` (search "squat" matches "Back Squat" and "BACK SQUAT")
    - Test: filters by category exact match
    - Test: filters by locationType exact match (filter "gym" matches exercises with locationTypes including "gym" or "both")
    - Test: combined search + category + locationType filter (AND semantics)
    - Test: empty filter returns all exercises

    `src/firebase/__tests__/assignment.service.test.ts` (covers ASGN-02 query shape — service implementation comes in Plan 02-04, but Wave 0 stub asserts the function signature exists):
    - Stub test that imports `findActiveAssignmentForClient` from `@/services/assignment.service` (will be created in Plan 02-04) — for Wave 0, mark as `.todo` or skip with comment "implemented in Plan 02-04".
    - If symbol not yet importable, the test file should be a `describe.skip` with comment referencing Plan 02-04.

    `firestore/__tests__/rules.test.ts` (extend existing file) — covers EXER-06 and analogous rules per RESEARCH.md security domain:
    - Test: trainer-A cannot read trainer-B's exercise (resource.data.trainerId mismatch denies)
    - Test: trainer cannot create exercise with `trainerId` not equal to their own uid
    - Test: trainer-A cannot read trainer-B's routine
    - Test: trainer-A cannot read trainer-B's program
    - Test: trainer-A cannot read assignment for trainer-B's client (only owning trainer or assigned client can read)
    - Test: client can read assignment where clientId == their uid
    - Test: client cannot create/update assignments (only trainers can)
  </behavior>
  <action>
    CRITICAL: Use the Write tool for every file (never heredoc). Project uses Zod v4.4.3 — per CONTEXT.md and RESEARCH.md Pitfall 2, do NOT use `z.nativeEnum()`. Use `z.enum([...] as const)`. For numeric form fields, use `z.coerce.number()`. For optional URLs, use `z.url().optional().or(z.literal(''))`.

    Type files (extend, do NOT duplicate User from src/types/user.ts):

    `src/types/exercise.ts` — export `ExerciseCategory` as union literal type matching enum `['strength', 'cardio', 'functional', 'hypertrophy', 'HIIT', 'mobility']`. Export `LocationType` as `'gym' | 'home' | 'both'`. Export `Exercise` interface with all fields per CONTEXT.md "Firestore Collections" section: `id, trainerId, name, description?, category, locationTypes: LocationType[], defaultSets?, defaultReps?, defaultDuration?, defaultRest?, videoUrl?, imageUrl?, createdAt, updatedAt`. Export `CreateExerciseInput` (omit `id, trainerId, createdAt, updatedAt`). Use `unknown` for Timestamp fields (follow user.ts precedent).

    `src/types/routine.ts` — export `RoutineExercise` interface: `exerciseId, name (denormalized), sets, reps?, duration?, rest, notes?, alternativeExerciseId?, order: number`. Export `Routine` interface: `id, trainerId, name, exercises: RoutineExercise[], createdAt, updatedAt`. Export `CreateRoutineInput`.

    `src/types/program.ts` — export `ProgramDay` as `string | 'rest' | null` (routineId, literal 'rest', or null = unassigned, per CONTEXT.md decision 2). Export `ProgramWeek` interface: `days: ProgramDay[]` (length exactly 7). Export `Program` interface: `id, trainerId, name, description?, durationWeeks, weeks: ProgramWeek[], createdAt, updatedAt`. Export `CreateProgramInput`.

    `src/types/assignment.ts` — match the exact CONTEXT.md decision 4 snapshot shape:
    - `AssignmentStatus` = `'active' | 'completed'`
    - `AssignmentSnapshotExercise` interface with all fields from CONTEXT.md snapshot shape: `exerciseId, name, sets, reps: number|null, duration: number|null, rest, notes: string|null, locationTypes: LocationType[], videoUrl: string|null, imageUrl: string|null, alternativeExerciseId: string|null, alternativeExercise: AssignmentSnapshotExercise|null`
    - `AssignmentSnapshotDay` interface: `type: 'rest' | 'routine' | null, routineId: string|null, routine: { name, exercises: AssignmentSnapshotExercise[] } | null`
    - `AssignmentSnapshotWeek`: `days: AssignmentSnapshotDay[]`
    - `AssignmentSnapshot` interface: `name, description, durationWeeks, weeks: AssignmentSnapshotWeek[]`
    - `Assignment` interface: `id, trainerId, clientId, programId, status, startDate: string (YYYY-MM-DD), createdAt, snapshot: AssignmentSnapshot`
    - `CreateAssignmentInput`: `{ programId: string, clientId: string, startDate: string }`
    - `CreateAssignmentResult`: `{ assignmentId: string }`

    Schema files (use Zod v4 API per RESEARCH.md Pattern 3, Pattern 4):

    `src/validation/exercise.schema.ts` — export `EXERCISE_CATEGORIES` and `LOCATION_TYPES` const tuples. Export `exerciseSchema = z.object({ ... })` with `name: z.string().min(1, 'Name is required')`, `description: z.string().optional()`, `category: z.enum(EXERCISE_CATEGORIES)`, `locationTypes: z.array(z.enum(LOCATION_TYPES)).min(1, 'Select at least one location')`, `defaultSets: z.coerce.number().int().positive().optional()`, same for `defaultReps`, `defaultDuration: z.coerce.number().int().nonnegative().optional()`, `defaultRest: z.coerce.number().int().nonnegative().optional()`, `videoUrl: z.url().optional().or(z.literal(''))`, `imageUrl: z.url().optional().or(z.literal(''))`. Export `ExerciseFormValues = z.infer<typeof exerciseSchema>`.

    `src/validation/routine.schema.ts` — export `routineExerciseSchema` and `routineSchema` with `exercises: z.array(routineExerciseSchema).min(1, 'Add at least one exercise')`. Fields per RESEARCH.md Pattern 4. `name: z.string().min(1)`. Per-exercise: `exerciseId: z.string().min(1)`, `sets: z.coerce.number().int().positive()`, `reps: z.coerce.number().int().positive().optional()`, `duration: z.coerce.number().int().nonnegative().optional()`, `rest: z.coerce.number().int().nonnegative()`, `notes: z.string().optional()`, `alternativeExerciseId: z.string().optional()`. Export `RoutineFormValues`.

    `src/validation/program.schema.ts` — `name: z.string().min(1)`, `description: z.string().optional()`, `durationWeeks: z.coerce.number().int().min(1).max(26)`. Export `ProgramFormValues`. The max(26) cap is per RESEARCH.md Pitfall 6 snapshot size safety.

    Test files: implement all behavior cases described in `<behavior>` above. Use `safeParse` to assert errors without throwing. For exercise.filter.test.ts, build a fixture array of 4-5 Exercise objects and assert filtered results.

    Filter implementation:

    `src/firebase/exerciseFilter.ts` — export `filterExercises(exercises: Exercise[], filters: { search?: string; category?: ExerciseCategory; locationType?: LocationType }): Exercise[]`. Pure function — case-insensitive substring on `name`; exact match on `category`; `locationType` matches if `exercise.locationTypes` includes the filter or includes 'both'. Combined with AND semantics.

    Firestore service ref additions:

    `src/firebase/firestore.ts` — KEEP existing `usersCollection`. ADD four new typed CollectionReference exports following the exact same pattern: `exercisesCollection(): CollectionReference<Exercise>`, `routinesCollection(): CollectionReference<Routine>`, `programsCollection(): CollectionReference<Program>`, `assignmentsCollection(): CollectionReference<Assignment>`. Import types from `@/types/exercise`, `@/types/routine`, `@/types/program`, `@/types/assignment`.

    Firestore indexes:

    `firestore.indexes.json` — add 4 composite indexes to the `indexes` array per RESEARCH.md Pitfall 7 (covers EXER-06 listing scope, ROUT-07, PROG-06, and listClients query in 02-03):
    - collectionGroup `exercises`, COLLECTION, fields: `trainerId ASCENDING`, `name ASCENDING`
    - collectionGroup `routines`, COLLECTION, fields: `trainerId ASCENDING`, `name ASCENDING`
    - collectionGroup `programs`, COLLECTION, fields: `trainerId ASCENDING`, `name ASCENDING`
    - collectionGroup `users`, COLLECTION, fields: `role ASCENDING`, `trainerId ASCENDING`, `name ASCENDING`  ← required for Plan 02-03 `listClients` query: `.where('role','==','client').where('trainerId','==',uid).orderBy('name')`
    Preserve all 4 existing indexes.

    Firestore rules tests:

    `firestore/__tests__/rules.test.ts` — extend (do NOT replace) the existing file. Add `describe('Phase 2 — cross-trainer denial')` block with the 6+ test cases listed in `<behavior>`. Use existing test harness pattern (initialize emulator, create two trainer contexts via `getAuthedFirestore(uid)`). Reference `firestore.rules` lines 76-108 for the rule semantics being tested.
  </action>
  <verify>
    <automated>npx jest --testPathPattern='(exercise|routine|program|exercise\.filter)\.(schema|filter)' --passWithNoTests</automated>
    <automated>node -e "const i=require('./firestore.indexes.json'); if(i.indexes.length < 8) { console.error('expected >= 8 indexes, got ' + i.indexes.length); process.exit(1); } console.log(i.indexes.length + ' indexes (ok)');"</automated>
    <automated># Requires the Firebase CLI + Firestore emulator to be installed locally (see firebase.json for emulator port config). If unavailable in this environment, run after installing: `npm install -g firebase-tools` and `firebase setup:emulators:firestore`.
firebase emulators:exec --only firestore "npx jest --testPathPattern=rules --passWithNoTests"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 (all new types compile)
    - `npx jest --testPathPattern=src/validation/__tests__` passes — 4 schema test files green
    - `grep -c "export interface Exercise" src/types/exercise.ts` == 1
    - `grep -c "export interface Routine" src/types/routine.ts` == 1
    - `grep -c "export interface Program" src/types/program.ts` == 1
    - `grep -c "export interface Assignment" src/types/assignment.ts` == 1
    - `grep -c "export interface AssignmentSnapshot" src/types/assignment.ts` == 1
    - `grep -c "exercisesCollection" src/firebase/firestore.ts` >= 1
    - `grep -c "routinesCollection" src/firebase/firestore.ts` >= 1
    - `grep -c "programsCollection" src/firebase/firestore.ts` >= 1
    - `grep -c "assignmentsCollection" src/firebase/firestore.ts` >= 1
    - `grep -c "z.nativeEnum" src/validation/exercise.schema.ts src/validation/routine.schema.ts src/validation/program.schema.ts` == 0 (no deprecated v3 API)
    - `grep -c "z.enum(EXERCISE_CATEGORIES)" src/validation/exercise.schema.ts` >= 1
    - `grep -c "z.coerce.number" src/validation/exercise.schema.ts` >= 4
    - `jq '.indexes | length' firestore.indexes.json` == 8 (4 existing + 4 new) AND `node -e "const i=require('./firestore.indexes.json'); if(i.indexes.length < 8) process.exit(1); console.log(i.indexes.length + ' indexes (ok)');"` exits 0
    - `jq '[.indexes[] | select(.collectionGroup == "exercises")] | length' firestore.indexes.json` == 1
    - `jq '[.indexes[] | select(.collectionGroup == "routines")] | length' firestore.indexes.json` == 1
    - `jq '[.indexes[] | select(.collectionGroup == "programs")] | length' firestore.indexes.json` == 1
    - `jq '[.indexes[] | select(.collectionGroup == "users")] | length' firestore.indexes.json` >= 1
    - `firebase emulators:exec --only firestore "npx jest --testPathPattern=rules --passWithNoTests"` exits 0 — Phase 2 cross-trainer denial cases green (requires firebase-tools + firestore emulator installed; see verify-block note)
    - `src/validation/__tests__/exercise.filter.test.ts` exists and all cases pass
  </acceptance_criteria>
  <done>
    All Phase 2 types compile, all schemas validate per behavior matrix, all Wave 0 test stubs exist and run (passing or marked `.todo` for assignment.service stub), 4 new Firestore indexes declared (exercises/routines/programs/users — verified via both jq count and a node-based count check), cross-trainer denial coverage extended in rules tests and proven green under the Firestore emulator.
  </done>
</task>

<task type="auto">
  <name>Task 3: 5-tab trainer nav shell + sub-stack layouts + placeholder index screens</name>
  <files>
    src/app/trainer/_layout.tsx,
    src/app/trainer/index.tsx,
    src/app/trainer/clients/_layout.tsx,
    src/app/trainer/clients/index.tsx,
    src/app/trainer/exercises/_layout.tsx,
    src/app/trainer/exercises/index.tsx,
    src/app/trainer/routines/_layout.tsx,
    src/app/trainer/routines/index.tsx,
    src/app/trainer/programs/_layout.tsx,
    src/app/trainer/programs/index.tsx
  </files>
  <read_first>
    src/app/trainer/_layout.tsx,
    src/app/trainer/index.tsx,
    src/components/ui/PrimaryButton.tsx,
    .planning/phases/02-trainer-content-creation/02-CONTEXT.md (Decision 1 — Trainer Navigation Structure, including the file routing diagram),
    .planning/phases/02-trainer-content-creation/02-RESEARCH.md (Verified expo-router Tab layout pattern)
  </read_first>
  <action>
    Per D-1 (CONTEXT.md): replace the current 2-tab trainer shell (Dashboard + Profile) with 5 tabs in this exact order — Clients | Exercises | Routines | Programs | Profile. The existing `src/app/trainer/index.tsx` placeholder is DELETED by re-routing — instead the Clients tab becomes the trainer's primary landing per D-1. Profile tab stays unchanged (already exists at `src/app/trainer/profile.tsx`).

    Update `src/app/trainer/_layout.tsx` to register five Tabs.Screen entries by name in order: `clients`, `exercises`, `routines`, `programs`, `profile`. Keep existing `screenOptions` (headerShown: false, tabBarStyle bg #0E0E0E, active #00FF66, inactive #888888). Remove the existing `<Tabs.Screen name="index" />` registration.

    Replace `src/app/trainer/index.tsx` with an expo-router redirect: import `Redirect` from `expo-router` and `export default function Index() { return <Redirect href="/trainer/clients" />; }`. This handles deep-link safety if anything still references `/trainer`.

    Create the four sub-stack layouts (`clients/_layout.tsx`, `exercises/_layout.tsx`, `routines/_layout.tsx`, `programs/_layout.tsx`) — each renders `<Stack screenOptions={{ headerShown: false }} />` with no additional config. The screens inside each stack will set their own headers in later plans.

    Create four placeholder index screens (`clients/index.tsx`, `exercises/index.tsx`, `routines/index.tsx`, `programs/index.tsx`) — each renders a centered Text element styled with the Obsidian Performance theme (bg #0E0E0E, text #FFFFFF) showing the tab name + "(coming in plan 02-NN)" placeholder. These are temporary stubs that Wave 2-4 plans will overwrite.

    Use the existing `src/app/trainer/index.tsx` styling pattern (View flex:1, backgroundColor #0E0E0E, Text color #FFFFFF) for consistency. Do NOT add unnecessary state, queries, or business logic — placeholders only.

    Do NOT touch `src/app/trainer/profile.tsx` (Phase 1 deliverable, unchanged).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `grep -c 'name="clients"' src/app/trainer/_layout.tsx` == 1
    - `grep -c 'name="exercises"' src/app/trainer/_layout.tsx` == 1
    - `grep -c 'name="routines"' src/app/trainer/_layout.tsx` == 1
    - `grep -c 'name="programs"' src/app/trainer/_layout.tsx` == 1
    - `grep -c 'name="profile"' src/app/trainer/_layout.tsx` == 1
    - `grep -c 'name="index"' src/app/trainer/_layout.tsx` == 0 (old registration removed)
    - All four `src/app/trainer/{clients,exercises,routines,programs}/_layout.tsx` files exist and export default a Stack component
    - All four `src/app/trainer/{clients,exercises,routines,programs}/index.tsx` files exist and export default a React component
    - `grep -c 'Redirect.*href="/trainer/clients"' src/app/trainer/index.tsx` == 1
    - Build smoke: `npx expo export --platform ios` exits 0 OR (if tooling unavailable in plan-time env) `npx tsc --noEmit` is sufficient
  </acceptance_criteria>
  <done>
    Trainer shell renders five tabs in the correct order; tapping any tab navigates to its placeholder index; old `/trainer` index redirects safely to `/trainer/clients`; TypeScript compiles clean; Wave 2-4 plans can fill the placeholders without touching the shell.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → npm registry | Package install (Task 1) — supply-chain trust boundary for two new libraries |
| client → Firestore | Future Phase 2 queries (no traffic in this plan — indexes declared only) |
| developer → schema files | Locked Zod v4 patterns prevent downstream plans from importing wrong-version APIs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | npm install of react-native-reanimated-dnd + @gorhom/bottom-sheet | mitigate | Blocking-human checkpoint (Task 1) — both packages tagged [ASSUMED] in RESEARCH.md audit (slopcheck unavailable). Human verifies maintainer, repo, version, download count on npmjs.com before approving install. |
| T-02-01 | Information Disclosure | Phase 2 collection rules (exercises/routines/programs/assignments) | mitigate | Cross-trainer denial test cases added to `firestore/__tests__/rules.test.ts` (Task 2) — verifies trainer-A cannot read trainer-B's exercises/routines/programs and that assignment reads are restricted to owning trainer or assigned client. Rules themselves already correct (firestore.rules lines 76-108) — Phase 2 only adds test coverage. |
| T-02-06 | Tampering | Exercise / Routine / Program schema validation | mitigate | Zod v4 schemas with `z.enum([...] as const)` reject invalid category and locationType values; `z.coerce.number().int().positive()` rejects negative/non-integer numerics; tests in Task 2 assert rejection cases. Prevents malformed data reaching Firestore in Wave 2-4 plans. |
| T-02-IDX | Denial of Service | Firestore query without composite index | accept-mitigate | Indexes for exercises/routines/programs (trainerId+name) added in Task 2 — prevents Wave 2-4 list queries from throwing INDEX_REQUIRED errors that would surface as broken UI. Deployment of `firestore.indexes.json` to Firebase is part of Wave 2 (Plan 02-02 first query call). |
| T-02-TYP | Tampering | Type contracts driving downstream plans | mitigate | All types declared exactly once in src/types/* (Task 2) — downstream plans MUST import, not redefine. `<read_first>` in every Phase 2 task references these files. |
</threat_model>

<verification>
- All Phase 2 type contracts (Exercise, Routine, Program, Assignment, AssignmentSnapshot) compile and are importable across the project
- Schema tests are GREEN for exercise, routine, program, and exercise.filter
- Cross-trainer denial rule tests pass under firebase emulator
- 5-tab trainer shell renders the correct tab order and old /trainer index redirects to /trainer/clients
- `firestore.indexes.json` contains the 4 new composite indexes (exercises, routines, programs, users) plus all 4 existing indexes (jq count == 8 AND node-based count >= 8)
- New packages (`react-native-reanimated-dnd`, `@gorhom/bottom-sheet`) appear in `package.json` dependencies
</verification>

<success_criteria>
- Wave 2 plans (02-02 exercises, 02-05a clients) can import `Exercise`, `Routine`, `Program`, `Assignment` types directly with no redefinition
- Wave 2 plans can import `exerciseSchema`, `routineSchema`, `programSchema` and use with `zodResolver(...)` directly
- Wave 2 plans can use `exercisesCollection().where('trainerId', '==', uid).orderBy('name')` without index errors after `firebase deploy --only firestore:indexes`
- Trainer tapping any of the 5 tabs lands on a placeholder screen that does not crash
- All Wave 0 stubs from VALIDATION.md exist; subsequent plans need only fill in implementation, not test scaffolding
</success_criteria>

<output>
Create `.planning/phases/02-trainer-content-creation/02-01-SUMMARY.md` when done — record: tab shell screenshot description, exported type names, schema test pass count, indexes added, rules test cases added, npm packages installed with verified versions.
</output>
