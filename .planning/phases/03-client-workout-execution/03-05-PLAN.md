---
phase: 03-client-workout-execution
plan: 05
type: execute
wave: 4
depends_on: ["03-03", "03-04"]
files_modified: []
autonomous: false
requirements: [WORK-01, WORK-02, WORK-03, WORK-04, WORK-05, WORK-06, WORK-07, WORK-08, WORK-09]
user_setup:
  - service: expo-dev-client
    why: "expo-video is a native module — video playback (WORK-03) and the full flow cannot be verified in a stale build"
    dashboard_config:
      - task: "Rebuild the native dev client after Plan 02 added expo-video: `npx expo run:ios` / `npx expo run:android` (or an EAS dev build), then install on a real device"
        location: "local machine / EAS"
must_haves:
  truths:
    - "Native dev client rebuilt with expo-video so inline video renders without a red screen"
    - "All six home states verified on device against a real assignment"
    - "Gym/home toggle swaps variants and shows mode tags on device"
    - "Force-kill mid-session offers Resume / Start over and restores checks + mode"
    - "Finish writes exactly one Firestore session doc and shows the celebration; re-open shows the done state with no duplicate"
  artifacts: []
  key_links: []
---

<objective>
Final on-device acceptance for Phase 3. Everything in Plans 01–04 is unit-tested where possible, but the five behaviors below require a native dev-client rebuild (expo-video) and a real device + real assignment + device clock to verify. This plan rebuilds the native client and runs the manual UAT script from 03-VALIDATION.md § Manual-Only Verifications, then confirms the full automated suite is green.

Purpose: Close WORK-01..09 with the end-to-end checks that automation cannot reach (video playback, crash-resume, duplicate guard, live state on device).
Output: A verified, shippable Phase 3 (no code changes; UAT sign-off + any defects logged as gap todos).
</objective>

<execution_context>
@/Users/FF-rodrigo/Documents/Personal/LauFit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/FF-rodrigo/Documents/Personal/LauFit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-client-workout-execution/03-VALIDATION.md
@.planning/phases/03-client-workout-execution/03-CONTEXT.md
@.planning/phases/03-client-workout-execution/03-03-SUMMARY.md
@.planning/phases/03-client-workout-execution/03-04-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated suite gate</name>
  <files></files>
  <read_first>
    - 03-VALIDATION.md § Test Infrastructure + Sampling Rate (full-suite command, phase gate)
  </read_first>
  <action>
    Run the full react-native jest project to confirm all Wave 0 unit tests (workoutDayComputer, variantResolver, session.service, sessionStore) plus the pre-existing suite are green before on-device UAT. Run `npx tsc --noEmit` to confirm the whole tree typechecks after Plans 01–04. If anything is red, stop and log it as a gap todo rather than proceeding to manual UAT.
  </action>
  <verify>
    <automated>npx jest --selectProjects react-native</automated>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest --selectProjects react-native` exits 0.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Full automated suite + typecheck green; ready for on-device UAT.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Plans 01–04 delivered the full client workout execution feature. expo-video was installed in Plan 02 but its native module is NOT in the current dev-client binary — a rebuild is required before video (WORK-03) and the full flow can be exercised on device.</what-built>
  <how-to-verify>
    Rebuild the native dev client (this is the one step Claude cannot do — it needs the developer's signing/device):
    1. Run `npx expo run:ios` (or `npx expo run:android`, or trigger an EAS dev build) so the binary includes the expo-video native module.
    2. Install the rebuilt dev client on a real device and launch it.
    3. Confirm the app boots without a red "Native module not found: ExpoVideo" screen.
  </how-to-verify>
  <resume-signal>Type "rebuilt" once the dev client with expo-video is installed and launches cleanly, or describe a build error.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The complete Phase 3 client workout flow: six home states, expandable exercise list with inline video/image, gym/home toggle, completion checkboxes, finish + celebration, crash-safe resume, and the duplicate guard.</what-built>
  <how-to-verify>
    Run the 03-VALIDATION.md § Manual-Only Verifications script on the rebuilt device. Use the existing test client (and assign a program if needed via the trainer flow):
    1. HOME STATES (WORK-01/02): Open the client app. Confirm today's state matches the snapshot — try, across set-ups, each of: no program, program-starts-in-N-days (set a future startDate), rest day (rotating message), active workout, program complete (past program), and already-completed-today.
    2. EXERCISE DETAIL + VIDEO (WORK-03): Start the workout; expand an exercise that has a videoUrl → inline video plays with native controls; expand one with only an imageUrl → image shows. Detail grid shows sets/reps-or-duration/rest; notes show when present.
    3. CHECKBOXES (WORK-04): Mark exercises complete/incomplete; names strike through when checked.
    4. GYM/HOME TOGGLE (WORK-05): Toggle Gym↔Home → exercises with alternatives swap variant; exercises with no valid variant stay and show a "gym only"/"home only" tag. Re-open the app later → the last chosen mode is the default.
    5. CRASH-SAFE RESUME (WORK-08): Check a few exercises, switch mode, force-quit the app mid-session, reopen → "Resume / Start over" prompt; Resume restores the exact checks + mode; Start over clears them.
    6. FINISH + CELEBRATION + DUPLICATE GUARD (WORK-06/07/09): Tap Finish with some unchecked → confirm dialog "Finish with X of Y done?"; confirm → celebration/summary screen; verify exactly ONE new doc appears in the Firestore `sessions` collection for today; tap Back to Home → home shows the "Workout done!" state; confirm you CANNOT start a second session for today (View session opens read-only, no new Start).
  </how-to-verify>
  <resume-signal>Type "approved" if all six checks pass, or list the failing checks (each becomes a gap todo for `/gsd-plan-phase --gaps`).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client app → Firestore | The UAT exercises the real session write + duplicate-guard read against the live rules. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | Elevation of Privilege | Duplicate session (WORK-09) | mitigate | UAT step 6 explicitly verifies exactly one session doc is written and that no second session can be started for the same day — confirms the client-side guard + rules behave end-to-end. |
| T-03-14 | Spoofing | Session ownership | mitigate | UAT confirms the written session carries the authenticated client's `clientId`; `firestore.rules` rejects any other value server-side. |
| T-03-SC | Tampering | npm installs | accept | No installs in this plan; the expo-video legitimacy checkpoint ran in Plan 02. |
</threat_model>

<verification>
- Automated: `npx jest --selectProjects react-native` + `npx tsc --noEmit` green.
- Manual: all six UAT checks in Task 3 pass on a device running the expo-video-rebuilt dev client.
</verification>

<success_criteria>
- Native dev client rebuilt with expo-video; app boots cleanly.
- All six home states, video/image detail, toggle, checkboxes, resume, finish, celebration, and duplicate guard verified on device (WORK-01..09).
- Any failures captured as gap todos for `/gsd-plan-phase --gaps`.
</success_criteria>

<output>
Create `.planning/phases/03-client-workout-execution/03-05-SUMMARY.md` when done.
</output>
