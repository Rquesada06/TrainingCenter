---
phase: 03-client-workout-execution
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - src/components/workout/HomeStateCards.tsx
  - src/app/client/index.tsx
autonomous: true
requirements: [WORK-01, WORK-02]
must_haves:
  truths:
    - "On open, the client Home shows exactly one of six states with no extra navigation (D-06)"
    - "State selection: no_assignment, starts_soon, rest, program_complete, active, already-completed-today"
    - "Stale (yesterday) sessionStore state is cleared on Home mount before resume evaluation"
    - "Active state offers Start Workout, or Continue Workout when an in-progress session exists for today"
    - "Already-completed-today state (WORK-09) offers a read-only View session"
  artifacts:
    - path: "src/components/workout/HomeStateCards.tsx"
      provides: "Six home-state card components per UI-SPEC Screen 1"
      min_lines: 80
    - path: "src/app/client/index.tsx"
      provides: "Stateful client Home wiring queries + computeTodayWorkout → one state card"
  key_links:
    - from: "src/app/client/index.tsx"
      to: "src/lib/workoutDayComputer.ts"
      via: "computeTodayWorkout + localTodayString"
      pattern: "computeTodayWorkout"
    - from: "src/app/client/index.tsx"
      to: "src/hooks/useClientActiveAssignment.ts"
      via: "useClientActiveAssignment + useTodaySession"
      pattern: "useClientActiveAssignment"
    - from: "src/app/client/index.tsx"
      to: "src/stores/sessionStore.ts"
      via: "stale-date clearSession on mount"
      pattern: "clearSession"
---

<objective>
Make the client Home screen (`src/app/client/index.tsx`) show exactly one correct state on open (WORK-01, WORK-02, D-06): No program, Program starts in N days, Rest day (rotating message), Active workout, Program complete, or Workout already completed today. Build the `HomeStateCards` component family to the UI-SPEC Screen 1 contract, wire the two query hooks + `computeTodayWorkout`, and add the stale-date clear so yesterday's partial session never bleeds into today.

Purpose: This is the heart of the phase — the client opens the app and immediately knows what to do, with no navigation. It composes entirely on the Wave 1/2 foundation (pure state machine + hooks + store).
Output: 1 new component file + 1 modified screen.
</objective>

<execution_context>
@/Users/FF-rodrigo/Documents/Personal/LauFit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/FF-rodrigo/Documents/Personal/LauFit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-client-workout-execution/03-CONTEXT.md
@.planning/phases/03-client-workout-execution/03-UI-SPEC.md
@.planning/phases/03-client-workout-execution/03-PATTERNS.md
@.planning/phases/03-client-workout-execution/03-01-SUMMARY.md
@.planning/phases/03-client-workout-execution/03-02-SUMMARY.md
@src/types/assignment.ts
@src/app/client/_layout.tsx
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: HomeStateCards component family</name>
  <files>src/components/workout/HomeStateCards.tsx</files>
  <read_first>
    - 03-UI-SPEC.md Screen 1 (all six state cards 1a–1f: exact icons, headings, body copy, CTAs, card tokens) + Copywriting Contract rows for home states
    - 03-PATTERNS.md § HomeStateCards (analog: ExerciseListItem card container; drop the Pressable for non-interactive states)
    - src/components/exercises/ExerciseListItem.tsx (card container className pattern)
    - src/components/ui/PrimaryButton.tsx (solid + outline variants for the CTAs)
    - src/components/clients/ClientListItem.tsx (NoProgramIndicator — #FFD600 usage reference)
  </read_first>
  <action>
    Create `src/components/workout/HomeStateCards.tsx`. Export a discriminated-union component `HomeStateCard` (or six named components — executor discretion per UI-SPEC) covering exactly the six states. Base card style for every variant: `bg-[#1A1A1A] border border-[#2A2A2A] rounded-xl p-6` (use `border-[#444444]` for the two interactive states 1d active and 1f done, per UI-SPEC). Use Ionicons from `@expo/vector-icons`. Per UI-SPEC Screen 1:
    - 1a no_assignment: `fitness-outline` 48px #444444, heading "No program yet" (20px/600/#FFFFFF), body "Your trainer hasn't assigned a program yet. Check back soon." (16px/400/#888888), no CTA.
    - 1b starts_soon: `calendar-outline` 48px #00FF66, heading "Your program starts in {N} days" (N===1 → "Your program starts tomorrow"), body "{programName} · First workout on {formattedDate}", no CTA. Format the date as a localized weekday+month+day string from the assignment startDate.
    - 1c rest: `moon-outline` 48px #888888, heading "Rest day", body = the rotating message keyed by `parseDateOnly(today).getDay()` from the 7-entry array in UI-SPEC State 1c (D-15), no CTA.
    - 1d active: label "Today's Workout" (14px/400/#888888), heading {routineName} (20px/600/#FFFFFF), body "{N} exercises", CTA PrimaryButton solid — "Start Workout", or "Continue Workout" when an in-progress session for today exists (prop-driven). onPress navigates to `/client/workout/session`.
    - 1e program_complete: `trophy-outline` 48px #00FF66, heading "Program complete", body "You've finished {programName}. Talk to your trainer about what's next.", no CTA.
    - 1f done: `checkmark-circle` 48px #00FF66, heading "Workout done!", body "{completedCount} of {total} exercises completed today.", CTA PrimaryButton variant="outline" — "View session" → navigates to read-only session view (`/client/workout/session?readOnly=true`).
    Props carry the data each card needs (programName, routineName, daysUntilStart, exerciseCount, completedCount, total, hasInProgressSession, onStart, onView). Use NativeWind className styling; system font; the exact copy from the Copywriting Contract. No fenced code, no placeholder text.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "HomeStateCards" || echo "no type errors in HomeStateCards"</automated>
    <automated>grep -c "No program yet\|Rest day\|Program complete\|Workout done" src/components/workout/HomeStateCards.tsx | grep -qv '^0$' && echo copy-present</automated>
  </verify>
  <acceptance_criteria>
    - All six state copy strings from the UI-SPEC Copywriting Contract are present verbatim.
    - The rest-day message array has 7 entries keyed by weekday (D-15).
    - 1d uses PrimaryButton solid; 1f uses PrimaryButton outline.
    - File typechecks.
  </acceptance_criteria>
  <done>HomeStateCards renders all six states per the UI-SPEC with the exact copy, icons, and CTAs.</done>
</task>

<task type="auto">
  <name>Task 2: Wire stateful client Home screen</name>
  <files>src/app/client/index.tsx</files>
  <read_first>
    - src/app/client/index.tsx (current placeholder — full replace)
    - 03-PATTERNS.md § `src/app/client/index.tsx` (loading/error/data pattern + stale-date effect + hydration gate)
    - 03-UI-SPEC.md Screen 1 (layout: SafeAreaView flex-1 bg-[#0E0E0E], ScrollView px-4 py-6; loading spinner; error card; Interaction Contracts § Date Stale-Check)
    - src/app/trainer/exercises/index.tsx (multi-state screen analog: SafeAreaView + ActivityIndicator + conditional render)
    - 03-01-SUMMARY.md, 03-02-SUMMARY.md (the exported computeTodayWorkout/localTodayString + hook signatures + sessionStore selectors)
  </read_first>
  <action>
    Replace `src/app/client/index.tsx` with a stateful Home screen. Import `useClientActiveAssignment`, `useTodaySession`, `useSessionStore`, `computeTodayWorkout`, `localTodayString`, and `HomeStateCard`. Compose:
    - `const { data: assignment, isLoading: aLoading, isError: aError } = useClientActiveAssignment();` and `const { data: todaySession, isLoading: sLoading } = useTodaySession();`
    - Stale-date clear in a mount effect (D-14 / Pitfall 4): read `sessionStore.date` and `clearSession`; `if (sessionDate !== null && sessionDate !== localTodayString()) clearSession();` (empty dep array, runs once on mount).
    - Hydration gate for the in-progress flag: use `useSessionStore.persist.onFinishHydration` + `hasHydrated()` (03-RESEARCH Pattern 5) so the "Continue Workout" vs "Start Workout" decision does not flash.
    - Loading: while `aLoading || sLoading` (or not hydrated) render the centered ActivityIndicator (#00FF66) inside SafeAreaView.
    - Error: if `aError`, render the inline error card (UI-SPEC error state) with a Retry Pressable that calls the query `refetch`.
    - State derivation: if `assignment == null` → no_assignment card; else `const result = computeTodayWorkout(assignment, localTodayString())`; if result.state==='active' AND `todaySession != null` → render the 1f done card (WORK-09 already-completed-today takes precedence over showing a fresh active workout); else map result.state → the matching HomeStateCard (starts_soon/rest/program_complete/active). Pass `hasInProgressSession` = (hydrated && sessionStore.isActive && sessionStore.date===today && sessionStore.clientId===uid) into the active card so it shows "Continue Workout".
    - CTAs use expo-router `router.push('/client/workout/session')` for Start/Continue and `router.push({ pathname: '/client/workout/session', params: { readOnly: 'true' } })` for View session.
    Use `SafeAreaView` from `react-native-safe-area-context`. NativeWind/inline styles per the existing screens.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "client/index" || echo "no type errors in client/index"</automated>
    <automated>grep -q "computeTodayWorkout" src/app/client/index.tsx && grep -q "clearSession" src/app/client/index.tsx && grep -q "useTodaySession" src/app/client/index.tsx && echo wired</automated>
  </verify>
  <acceptance_criteria>
    - Home derives exactly one state via `computeTodayWorkout`; `todaySession != null` on an active day forces the done (1f) state (WORK-09 precedence).
    - Stale-date clear runs on mount before resume evaluation.
    - Hydration gate prevents Start/Continue flash.
    - `npx jest --selectProjects react-native` still green (no regressions).
  </acceptance_criteria>
  <done>Client Home shows the correct one of six states on open with no extra navigation (WORK-01, WORK-02, D-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client app → Firestore | Home issues the active-assignment + today-session reads via the Wave 2 hooks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Information Disclosure | Home reads (assignment + today session) | mitigate | Reads go through `useClientActiveAssignment`/`useTodaySession`, both filtered `clientId == self`; `firestore.rules` enforces ownership server-side. Home renders only the authenticated client's own data. |
| T-03-08 | Tampering | Stale-date resume state | mitigate | Mount-time stale-date clear prevents yesterday's persisted session from offering a "Continue/Resume" against today — a correctness/integrity guard (Pitfall 4). |
| T-03-SC | Tampering | npm installs | accept | No package-manager installs in this plan. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for `HomeStateCards.tsx` and `client/index.tsx`.
- `npx jest --selectProjects react-native` green.
- Manual (Plan 05 UAT): each of the six states renders for the corresponding assignment/day on device.
</verification>

<success_criteria>
- Client Home shows one correct state on open, no navigation required (WORK-01).
- All six explicit states render with UI-SPEC copy and icons (WORK-02, D-06).
- Already-completed-today (WORK-09) is enforced at the Home state layer.
</success_criteria>

<output>
Create `.planning/phases/03-client-workout-execution/03-03-SUMMARY.md` when done.
</output>
