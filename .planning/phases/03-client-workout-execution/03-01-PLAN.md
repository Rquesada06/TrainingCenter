---
phase: 03-client-workout-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/session.ts
  - src/firebase/firestore.ts
  - src/lib/workoutDayComputer.ts
  - src/lib/variantResolver.ts
  - src/lib/__tests__/workoutDayComputer.test.ts
  - src/lib/__tests__/variantResolver.test.ts
autonomous: true
requirements: [WORK-01, WORK-05]
must_haves:
  truths:
    - "computeTodayWorkout returns the correct one of six states for a given assignment + today date"
    - "parseDateOnly / localTodayString use LOCAL midnight, never UTC — no off-by-one in negative-UTC timezones"
    - "resolveVariant returns the correct exercise variant + mode tag for gym/home/both/no-alternative cases"
    - "sessionsCollection() typed ref exists for downstream service writes"
  artifacts:
    - path: "src/types/session.ts"
      provides: "Session document type contract"
      contains: "interface Session"
    - path: "src/lib/workoutDayComputer.ts"
      provides: "Date-only math + computeTodayWorkout state machine (D-01..D-05)"
      exports: ["computeTodayWorkout", "parseDateOnly", "localTodayString", "dayOffset", "WorkoutDayResult"]
      min_lines: 50
    - path: "src/lib/variantResolver.ts"
      provides: "Gym/home variant resolution (D-08, D-10)"
      exports: ["resolveVariant", "WorkoutMode", "ResolvedExercise"]
    - path: "src/firebase/firestore.ts"
      provides: "sessionsCollection typed ref"
      contains: "sessionsCollection"
  key_links:
    - from: "src/firebase/firestore.ts"
      to: "src/types/session.ts"
      via: "import type Session"
      pattern: "CollectionReference<Session>"
    - from: "src/lib/workoutDayComputer.ts"
      to: "src/types/assignment.ts"
      via: "import Assignment + AssignmentSnapshotDay"
      pattern: "import type.*assignment"
---

<objective>
Build the Wave 0 foundation for Phase 3: the Session type contract, the `sessionsCollection()` typed Firestore ref, and the two pure-function modules at the heart of the phase — `workoutDayComputer` (date-only offset math + the six-state home machine, D-01..D-05, WORK-01) and `variantResolver` (gym/home swap, D-08/D-10, WORK-05) — each with its Nyquist unit-test file.

Purpose: Everything downstream (hooks, store, screens) composes on top of these contracts. Getting the date-only math right (no UTC drift) and the state machine correct is the single highest-leverage correctness work in the phase.
Output: 4 new source files, 1 modified (`firestore.ts`), 2 new test files — all unit-tested and green.
</objective>

<execution_context>
@/Users/FF-rodrigo/Documents/Personal/LauFit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/FF-rodrigo/Documents/Personal/LauFit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-client-workout-execution/03-CONTEXT.md
@.planning/phases/03-client-workout-execution/03-RESEARCH.md
@.planning/phases/03-client-workout-execution/03-PATTERNS.md
@.planning/phases/03-client-workout-execution/03-VALIDATION.md
@src/types/assignment.ts
@src/types/exercise.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Session type + sessionsCollection ref + workoutDayComputer with tests</name>
  <files>src/types/session.ts, src/firebase/firestore.ts, src/lib/workoutDayComputer.ts, src/lib/__tests__/workoutDayComputer.test.ts</files>
  <read_first>
    - src/types/assignment.ts (the snapshot the client renders — `Assignment`, `AssignmentSnapshot`, `AssignmentSnapshotDay`; `durationWeeks`, `weeks[].days[]`, `startDate` is YYYY-MM-DD)
    - src/firebase/firestore.ts (existing `assignmentsCollection` typed-ref pattern at the end of the file — copy it exactly)
    - 03-PATTERNS.md § `src/types/session.ts`, § `src/firebase/firestore.ts`, § `src/lib/workoutDayComputer.ts` (the analog: `src/firebase/exerciseFilter.ts` pure-module discipline)
    - 03-RESEARCH.md Pattern 1 (date-only math), Pattern 2 (computeTodayWorkout), Pattern 10 (Session type)
    - src/firebase/exerciseFilter.ts (pure-function module analog — JSDoc header + named exports, no React/Firebase)
  </read_first>
  <behavior>
    - parseDateOnly('2025-06-03') → Date with getFullYear()=2025, getMonth()=5, getDate()=3 (LOCAL midnight, not UTC). Regression guard against `new Date('YYYY-MM-DD')` UTC trap.
    - localTodayString() returns a YYYY-MM-DD string built from getFullYear/getMonth/getDate (NOT toISOString slice).
    - dayOffset(start, today) returns whole-day integer offset via Math.floor of ms diff.
    - computeTodayWorkout — six cases over an Assignment fixture:
      * today < startDate → { state: 'starts_soon', daysUntilStart: N } (D-02)
      * dayOffset >= durationWeeks*7 → { state: 'program_complete' } (D-03, no loop, no hold)
      * day.type === 'rest' → { state: 'rest' } (D-04)
      * day.type === null (unassigned) → { state: 'rest' } (D-04)
      * missing week/day index (out of bounds within range) → { state: 'rest' }
      * day.type === 'routine' with exercises → { state: 'active', weekIndex, dayIndex, day } (D-05)
    - weekIndex = Math.floor(dayOffset/7), dayIndex = dayOffset % 7 (D-01).
  </behavior>
  <action>
    Create `src/types/session.ts` with the exact `Session` interface from 03-PATTERNS.md § session.ts: top-level `id`; fields `clientId`, `trainerId`, `assignmentId`, `date` (YYYY-MM-DD), `weekIndex`, `dayIndex`, `mode: 'gym' | 'home'`, `completedExerciseIds: string[]`, `totalExercises`, `startedAt` (ISO string), `completedAt` (ISO string), `routineName: string | null`. Use `null` not `undefined` (Firestore discipline) and a JSDoc file header matching assignment.ts.

    Modify `src/firebase/firestore.ts`: add `import type { Session } from '@/types/session';` alongside existing type imports, and append `export const sessionsCollection = (): FirebaseFirestoreTypes.CollectionReference<Session> => firestore().collection('sessions') as FirebaseFirestoreTypes.CollectionReference<Session>;` immediately after `assignmentsCollection`. Match the existing ref's exact signature style.

    Create `src/lib/workoutDayComputer.ts` as a pure module (no React, no Firebase) exporting: `parseDateOnly(yyyymmdd: string): Date` using `new Date(y, m-1, d)` local-midnight; `localTodayString(): string` using getFullYear/getMonth+1/getDate with padStart; `dayOffset(startDateStr, todayStr): number` via `Math.floor((today.getTime()-start.getTime())/86400000)`; the `WorkoutDayResult` discriminated union (no_assignment | starts_soon | rest | program_complete | active); and `computeTodayWorkout(assignment: Assignment, todayStr: string): WorkoutDayResult` implementing D-01..D-05 exactly per 03-RESEARCH.md Pattern 2 (offset<0 → starts_soon daysUntilStart=-offset; offset>=durationWeeks*7 → program_complete; resolve `weeks[weekIndex]?.days[dayIndex]`; null/rest/missing → rest; routine → active with weekIndex/dayIndex/day). The `no_assignment` state is produced by the caller when assignment is null — `computeTodayWorkout` takes a non-null Assignment. Import `Assignment` and `AssignmentSnapshotDay` from `@/types/assignment`. NO date library.

    Create `src/lib/__tests__/workoutDayComputer.test.ts` (pure function tests, no mocks) per 03-PATTERNS.md Unit Test § workoutDayComputer and 03-VALIDATION.md Wave 0: a `parseDateOnly` UTC-drift regression test, a `localTodayString` local-date test, and a `computeTodayWorkout` block covering all six behaviors above using inline Assignment fixtures (build minimal `weeks`/`days` arrays sized to `durationWeeks`).
  </action>
  <verify>
    <automated>npx jest workoutDayComputer --passWithNoTests=false</automated>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "session\.ts|firestore\.ts|workoutDayComputer" || echo "no type errors in new files"</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest workoutDayComputer` passes with all six computeTodayWorkout cases + the two date-helper regression tests green.
    - `src/types/session.ts` exports `interface Session` with all 12 fields; `null` (not `undefined`) for nullable fields.
    - `grep -c "sessionsCollection" src/firebase/firestore.ts` ≥ 1 and the ref is typed `CollectionReference<Session>`.
    - `workoutDayComputer.ts` exports `computeTodayWorkout`, `parseDateOnly`, `localTodayString`, `dayOffset`, `WorkoutDayResult`; no import from a date library.
  </acceptance_criteria>
  <done>Session type + sessionsCollection ref exist and typecheck; workoutDayComputer implements D-01..D-05 with all six states; its test file is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: variantResolver pure function with tests</name>
  <files>src/lib/variantResolver.ts, src/lib/__tests__/variantResolver.test.ts</files>
  <read_first>
    - src/types/assignment.ts (`AssignmentSnapshotExercise` — `locationTypes: LocationType[]`, nested `alternativeExercise: AssignmentSnapshotExercise | null`)
    - src/types/exercise.ts (`LocationType` = 'gym' | 'home' | 'both')
    - 03-PATTERNS.md § `src/lib/variantResolver.ts` (analog: exerciseFilter.ts pure module)
    - 03-RESEARCH.md Pattern 8 (resolveVariant logic, D-08 + D-10)
    - src/firebase/exerciseFilter.ts (pure-module analog)
  </read_first>
  <behavior>
    - primary fits mode ('gym'/'home' present OR 'both') → { exercise: primary, modeTag: null } (D-08).
    - primary does NOT fit, alternative fits → { exercise: alternative, modeTag: null } (D-08 swap).
    - neither fits → keep primary, modeTag = 'gym_only' or 'home_only' based on primary.locationTypes (D-10 — never drop trainer work).
    - 'both' locationType counts as fitting either mode.
    - no alternative (null) and primary doesn't fit → primary + correct mode tag.
  </behavior>
  <action>
    Create `src/lib/variantResolver.ts` as a pure module (JSDoc header, no React/Firebase) exporting `WorkoutMode = 'gym' | 'home'`, `ResolvedExercise { exercise: AssignmentSnapshotExercise; modeTag: 'gym_only' | 'home_only' | null }`, and `resolveVariant(primary: AssignmentSnapshotExercise, mode: WorkoutMode): ResolvedExercise` implementing 03-RESEARCH.md Pattern 8 exactly: compute `primaryFits = primary.locationTypes.includes(mode) || primary.locationTypes.includes('both')`; `alt = primary.alternativeExercise`; `altFits` similarly; return primary if primaryFits, else alt if altFits, else primary with a `modeTag` derived from the primary's own location type (gym-capable → 'gym_only', otherwise 'home_only'). Import `AssignmentSnapshotExercise` from `@/types/assignment`.

    Create `src/lib/__tests__/variantResolver.test.ts` (no mocks) covering the full gym/home/both/no-alternative matrix from the behavior block above — at minimum one test per behavior bullet, using inline `AssignmentSnapshotExercise` fixtures (set `alternativeExercise` to a nested fixture or null per case).
  </action>
  <verify>
    <automated>npx jest variantResolver --passWithNoTests=false</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest variantResolver` passes with the gym/home/both/no-alt matrix green.
    - `resolveVariant` never returns `null` for `exercise` (D-10: trainer work is never dropped).
    - When primary fits or alt fits, `modeTag` is `null`; only the no-valid-variant path sets a tag.
  </acceptance_criteria>
  <done>variantResolver implements D-08 + D-10 swap logic; its test file covers all four matrix cases and is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none crossed in this plan) | This plan is pure types + pure functions + one typed Firestore ref declaration. No data crosses a trust boundary at runtime — no reads, no writes, no user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Date-only math (`parseDateOnly`/`dayOffset`) | mitigate | UTC-drift regression test in `workoutDayComputer.test.ts` ensures local-midnight parsing — a wrong workout-day computation is a correctness/integrity bug, guarded by the unit test |
| T-03-02 | Information Disclosure | `sessionsCollection()` typed ref | accept | Ref only declares a typed collection handle; all ownership enforcement is server-side in `firestore.rules` (sessions block: `clientId == request.auth.uid`). No query is issued in this plan. |
| T-03-SC | Tampering | npm installs | accept | No package-manager installs in this plan (expo-video install is deferred to Plan 02 where its legitimacy checkpoint lives). |
</threat_model>

<verification>
- `npx jest --testPathPattern="workoutDayComputer|variantResolver" --passWithNoTests=false` green.
- `npx tsc --noEmit` shows no new errors in `session.ts`, `firestore.ts`, `workoutDayComputer.ts`, `variantResolver.ts`.
- `grep -c "sessionsCollection" src/firebase/firestore.ts` ≥ 1.
</verification>

<success_criteria>
- Session type contract exists and is consumed by `sessionsCollection()`.
- `computeTodayWorkout` returns the correct state for all six cases (WORK-01) with no UTC drift.
- `resolveVariant` returns the correct variant + mode tag for the full gym/home/both/no-alt matrix (WORK-05).
- Both Wave 0 pure-function test files are green.
</success_criteria>

<output>
Create `.planning/phases/03-client-workout-execution/03-01-SUMMARY.md` when done.
</output>
