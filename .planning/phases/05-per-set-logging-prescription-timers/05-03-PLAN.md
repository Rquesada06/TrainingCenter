---
phase: 05-per-set-logging-prescription-timers
plan: 03
type: execute
wave: 1
depends_on: [05-01, 05-02]
files_modified:
  - src/components/workout/SetRow.tsx
  - src/components/workout/RpeStepper.tsx
  - src/app/client/workout/session.tsx
autonomous: true
requirements: [LOG-01, LOG-02, LOG-03, LOG-04]
must_haves:
  truths:
    - "On the workout screen the client sees one row per set (weight / reps / RPE / done) inside the expanded weighted exercise card"
    - "Each set prefills from the trainer target or the client's previous same-exercise session — a prefilled, unconfirmed set requires only the done-check (one tap)"
    - "Checking a set toggles its completed state in live crash-safe state; an exercise counts complete at ≥1 checked set (D-08)"
    - "Finishing builds loggedExercises (null for unlogged) and writes the session in a single Firestore write via the existing withSaveFeedback path; old sessions still load"
  artifacts:
    - path: "src/components/workout/SetRow.tsx"
      provides: "5-column per-set logging row (SET/PESO/REPS/RPE/STATUS)"
      contains: "onToggleDone"
    - path: "src/components/workout/RpeStepper.tsx"
      provides: "compact 1-10 RPE stepper (0.5 step)"
      contains: "Clear"
    - path: "src/app/client/workout/session.tsx"
      provides: "per-set rows in expanded card + buildFinalizedSession on finish"
      contains: "buildFinalizedSession"
  key_links:
    - from: "src/app/client/workout/session.tsx"
      to: "sessionStore loggedSets"
      via: "setSetValue / toggleSet / seedExercise"
      pattern: "setSetValue|toggleSet"
    - from: "src/app/client/workout/session.tsx"
      to: "buildFinalizedSession"
      via: "handleFinish builds the session record from live state"
      pattern: "buildFinalizedSession"
    - from: "session.tsx"
      to: "resolvePrefill"
      via: "seedExercise(prefill) on first expand"
      pattern: "resolvePrefill|seedExercise"
---

<objective>
Replace the per-exercise checkbox with per-set logging on the client workout screen (LOG-01/02/03/04): build `SetRow` (5-column weight/reps/RPE/done grid) and `RpeStepper` (compact 1-10 picker), wire prefill on first expand, write live edits to `sessionStore.loggedSets`, and replace the inline finalize build with `buildFinalizedSession` (single Firestore write through the existing `withSaveFeedback` path). Timed-exercise rendering and the timer bar are Plan 05 — this plan delivers the weighted logging slice end-to-end.

Purpose: This is the core "logged, measurable training" capability and the one-tap speed goal. It consumes the Wave-0 pure libs (prefill, sessionFinalize) and the store extension, so all logic is already tested — this plan is the UI + wiring.
Output: SetRow + RpeStepper components and the modified session screen, with the finalize path emitting loggedExercises and derived completedExerciseIds.
</objective>

<execution_context>
@/Users/FF-rodrigo/Documents/Personal/LauFit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/FF-rodrigo/Documents/Personal/LauFit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-per-set-logging-prescription-timers/05-CONTEXT.md
@.planning/phases/05-per-set-logging-prescription-timers/05-PATTERNS.md
@.planning/phases/05-per-set-logging-prescription-timers/05-UI-SPEC.md
@.planning/phases/05-per-set-logging-prescription-timers/05-02-SUMMARY.md
@src/app/client/workout/session.tsx
@src/components/workout/ExerciseRow.tsx
@src/lib/mutationFeedback.ts
@src/lib/sessionFinalize.ts
@src/lib/prefill.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SetRow + RpeStepper components</name>
  <files>src/components/workout/SetRow.tsx, src/components/workout/RpeStepper.tsx</files>
  <read_first>
    - src/components/workout/ExerciseRow.tsx (analog: expanded detail grid `flexDirection:'row', gap:8` lines ~198-253; checkbox lines ~144-159; #444444 borders)
    - src/components/workout/GymHomeToggle.tsx (analog: compact segmented control with hitSlop + accessibility — chrome for RpeStepper)
    - 05-UI-SPEC.md § A2 (column grid weights SET 0.9 / PESO 2.6 / REPS 2.0 / RPE 2.2 / STATUS 1.6, gap 8; value color rule), § New Components → SetRow + RpeStepper (props), Copywriting (column headers, "Clear", "–" placeholder), Accessibility contracts
    - 05-PATTERNS.md § SetRow.tsx, § RpeStepper.tsx (done-check adapted from ExerciseRow.tsx:144-159, 28×28, #00FF66; drop strikethrough)
  </read_first>
  <behavior>
    - SetRow renders 5 cells (SET/PESO/REPS/RPE/STATUS) on a shared flex template so columns align with the header row.
    - A prefilled, unconfirmed value renders `#888888`; once edited or the set is checked, confirmed values render `#FFFFFF` (value color rule).
    - The done-check is a 28×28 circle: unchecked `border-2 border-[#444444]` transparent; checked `bg-[#00FF66]` with Ionicons `checkmark` 16px `#0E0E0E`; hitSlop to ≥44pt; `accessibilityRole="checkbox"` + `accessibilityState={{ checked }}`.
    - RpeStepper renders `[ − ] {value} [ + ]` + "Clear"; clamps 1.0–10.0, step 0.5 (integer fallback acceptable per D-03); `accessibilityRole="adjustable"`, `accessibilityValue={{ now: rpe }}`; blank shows `–`. NOT a free keypad (D-01).
  </behavior>
  <action>
    Create src/components/workout/SetRow.tsx with props `setNumber, weight: number|null, reps: number|null, rpe: number|null, completed, isPrefilled, onChangeWeight, onChangeReps, onChangeRpe, onToggleDone, readOnly?`. Reuse the ExerciseRow expanded-grid cell style (`bg #0E0E0E`, `borderRadius 8`, `padding 8`) and the column flex weights from UI-SPEC A2. PESO/REPS cells are tappable numeric fields (`keyboardType="number-pad"`, mono 16, focused-cell `border #00FF66`, empty placeholder `–` in `#444444`). RPE cell opens the RpeStepper (not a keypad). Apply the value color rule (`#888888` when `isPrefilled && !completed && !edited`, else `#FFFFFF`). Done-check adapted from ExerciseRow.tsx:144-159 at 28×28. DROP the v1.0 strikethrough.
    Create src/components/workout/RpeStepper.tsx per UI-SPEC: 32×32 `border border-[#444444] rounded-lg` − / + buttons (Ionicons remove/add 16px), value mono 16/600 `#FFFFFF`, "Clear" affordance to return to blank, clamp 1.0–10.0 step 0.5.
    No fenced code in this plan — follow the cited analog lines verbatim for styling.
  </action>
  <acceptance_criteria>
    - `src/components/workout/SetRow.tsx` contains `onToggleDone` and `accessibilityRole="checkbox"`; no `textDecorationLine: 'line-through'`.
    - `src/components/workout/RpeStepper.tsx` contains `Clear` and `accessibilityRole="adjustable"`.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>SetRow renders the aligned 5-column grid with the value-color rule and the 28×28 done-check; RpeStepper is a clamped 0.5-step picker with Clear; both accessible; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire per-set logging + prefill + finalize into session.tsx</name>
  <files>src/app/client/workout/session.tsx</files>
  <read_first>
    - src/app/client/workout/session.tsx (self — `renderItem` lines ~368-391, `handleFinish` lines ~245-303 incl. the inline `sessionRecord` build lines ~253-266 and the `withSaveFeedback` wrapper lines ~268-284)
    - src/lib/sessionFinalize.ts + src/lib/prefill.ts (Wave-0 units to call) + src/stores/sessionStore.ts (setSetValue/toggleSet/seedExercise/loggedSets)
    - src/lib/mutationFeedback.ts (withSaveFeedback — keep the exact wrapper shape, S2)
    - 05-UI-SPEC.md § A1 (collapsed card: complete left-edge at ≥1 set, progress caption), § A2 (expanded weighted layout), § A5 (Finish CTA label "Finish Session" + incomplete-confirm copy), § Interaction Contracts (per-set logging + finish)
    - 05-PATTERNS.md § session.tsx (finalize replacement + render)
  </read_first>
  <behavior>
    - On first expand of a weighted exercise, the screen seeds the store via `seedExercise(resolvePrefill(exercise, priorSessions))` so each set prefills (LOG-03).
    - Editing a weight/reps cell writes through `setSetValue`; tapping the done-check calls `toggleSet`; values persist in live crash-safe state (no Firestore write yet).
    - The collapsed card shows the complete left-edge accent + "{checked}/{sets} sets logged" caption once ≥1 set is checked (D-08).
    - On Finish, `handleFinish` builds the record via `buildFinalizedSession(liveState, resolvedExercises)` (loggedExercises with null for unlogged, derived completedExerciseIds/totalExercises) and writes once via the existing `withSaveFeedback` → `finishMutation.mutateAsync` → `clearSession` → celebration. The incomplete-confirm threshold now uses per-exercise completion derived from sets.
    - Old v1.0 sessions (no loggedExercises) still load the session screen without crashing.
  </behavior>
  <action>
    In src/app/client/workout/session.tsx: in `renderItem` render `SetRow ×sets` (weighted) inside the expanded card, sourcing values from `sessionStore.loggedSets[exerciseId]` and wiring `onChange*`→`setSetValue`, `onToggleDone`→`toggleSet`. Seed prefill on first expand via `seedExercise(resolvePrefill(exercise, priorSessions))` (read prior sessions from the existing history query/hook the screen already has access to; if none is wired, fetch via the existing session-history source — do NOT add a new Firestore read pattern, reuse `useSessionHistory`/`fetchSessionsForAssignment`). Replace the inline `sessionRecord` build (lines ~253-266) with `buildFinalizedSession(...)`, keeping the `withSaveFeedback` wrapper (lines ~268-284) UNCHANGED. Update the collapsed card to the complete left-edge accent + sets-logged caption (UI-SPEC A1). Update the finish CTA label to "Finish Session" and the incomplete-confirm copy per UI-SPEC A5. Null-guard everything for old sessions (no loggedExercises). Timed exercises render the existing v1.0 path for now — Plan 05 adds WorkTimerControl.
  </action>
  <acceptance_criteria>
    - `src/app/client/workout/session.tsx` contains `buildFinalizedSession` and `SetRow` and one of `setSetValue|toggleSet`.
    - The `withSaveFeedback` single-write finalize call is retained (still references `finishMutation` + `clearSession`).
    - `npx jest --selectProjects react-native` exits 0 (full RN suite green after wiring).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx jest --selectProjects react-native -x && npx tsc --noEmit</automated>
  </verify>
  <done>Workout screen logs per-set with prefill into crash-safe state and finalizes with a single write building loggedExercises + derived completedExerciseIds; old sessions safe; suite + tsc green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client UI input → sessionStore live state → SESSIONS finalize write | user-entered weights/reps/RPE flow to a persisted document |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering | finalize payload from live state | mitigate | Finalize routed through `buildFinalizedSession` (null coercion, Wave-0 tested) then `stripUndefinedDeep` in the existing createSession write; no per-field client bypass |
| T-05-06 | Repudiation | session write-once immutability | mitigate | Single create on finish via the existing finishMutation (D-12/D-13); no per-set Firestore updates introduced |
</threat_model>

<verification>
- `npx jest --selectProjects react-native -x` green
- `npx tsc --noEmit` clean
- (manual, Plan 06) on-device per-set logging round-trip
</verification>

<success_criteria>
- SetRow + RpeStepper match the UI-SPEC contract (grid, value-color rule, 28×28 done-check, 0.5 RPE stepper, accessibility)
- Per-set edits + done-checks persist in crash-safe live state; prefill makes an unchanged set one tap
- Finish writes once via buildFinalizedSession + withSaveFeedback; old sessions load
- RN suite + tsc green
</success_criteria>

<output>
Create `.planning/phases/05-per-set-logging-prescription-timers/05-03-SUMMARY.md` when done.
</output>
