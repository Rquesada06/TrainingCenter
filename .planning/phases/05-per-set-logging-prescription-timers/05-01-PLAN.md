---
phase: 05-per-set-logging-prescription-timers
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/types/routine.ts
  - src/types/assignment.ts
  - src/validation/routine.schema.ts
  - src/validation/__tests__/routine.schema.test.ts
  - functions/src/index.ts
  - functions/src/__tests__/createAssignment.test.ts
autonomous: true
requirements: [PRES-01, PRES-02, PRES-03]
must_haves:
  truths:
    - "A new prescription field set by the trainer (rep range, target RPE, timed flag) reaches the client because it is copied into the server-built assignment snapshot"
    - "The routine schema rejects a rep range where min > max with a clear message"
    - "An existing v1.0 assignment with no rep-range/timed fields still validates and renders (additive, back-compatible)"
  artifacts:
    - path: "src/types/routine.ts"
      provides: "repsMin/repsMax/targetRpe/timed on RoutineExercise"
      contains: "timed"
    - path: "src/types/assignment.ts"
      provides: "repsMin/repsMax/targetRpe/timed on AssignmentSnapshotExercise"
      contains: "targetRpe"
    - path: "functions/src/index.ts"
      provides: "buildSnapshotExercise copies new fields (main + alternative branch)"
      contains: "targetRpe"
    - path: "src/validation/routine.schema.ts"
      provides: "rep-range refine + targetRpe bounds + timed boolean"
      contains: "Min must be"
  key_links:
    - from: "functions/src/index.ts buildSnapshotExercise"
      to: "AssignmentSnapshotExercise"
      via: "field copy on snapshot build"
      pattern: "targetRpe"
    - from: "src/validation/routine.schema.ts"
      to: "RoutineExercise"
      via: "zod refine on the exercise object"
      pattern: "repsMin"
---

<objective>
Land the five-tier prescription schema (PRES-01/02/03) as the foundation Wave-0 layer: extend `RoutineExercise` and `AssignmentSnapshotExercise` types, the zod routine schema (rep-range refine + targetRpe bounds + timed flag), AND the server-side `buildSnapshotExercise` Cloud Function (the easy-to-miss tier that actually delivers the fields to the client). Tests are written test-first and must pass.

Purpose: New trainer-prescribed fields are silently invisible to clients unless they traverse all five tiers — type → zod → builder UI → Cloud Function snapshot → snapshot type. This plan owns four of the five tiers (the builder UI is Plan 04); without the Cloud Function tier, Plan 04's builder fields never reach the workout screen.
Output: Updated types, schema with passing refine tests, Cloud Function snapshot propagation with passing tests.
</objective>

<execution_context>
@/Users/FF-rodrigo/Documents/Personal/LauFit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/FF-rodrigo/Documents/Personal/LauFit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-per-set-logging-prescription-timers/05-CONTEXT.md
@.planning/phases/05-per-set-logging-prescription-timers/05-RESEARCH.md
@.planning/phases/05-per-set-logging-prescription-timers/05-PATTERNS.md
@.planning/phases/05-per-set-logging-prescription-timers/05-VALIDATION.md
@src/types/routine.ts
@src/types/assignment.ts
@src/validation/routine.schema.ts
@functions/src/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend RoutineExercise + AssignmentSnapshotExercise types and the routine zod schema</name>
  <files>src/types/routine.ts, src/types/assignment.ts, src/validation/routine.schema.ts, src/validation/__tests__/routine.schema.test.ts</files>
  <read_first>
    - src/types/routine.ts (self — additive; keep legacy `reps?`)
    - src/types/assignment.ts (self — `AssignmentSnapshotExercise` lines 25-38; fields are `T | null`)
    - src/validation/routine.schema.ts (self — analog for zod v4 API: `z.coerce.number()`, `z.enum([...] as const)`)
    - src/validation/__tests__/routine.schema.test.ts (self — test structure to extend)
    - 05-PATTERNS.md § `src/types/routine.ts`, § `src/types/assignment.ts`, § `src/validation/routine.schema.ts` (exact field shapes + refine)
    - 05-RESEARCH.md § "zod v4 prescription + logged-set validators" and Pitfall 5 (null-not-undefined; zod v4 only)
  </read_first>
  <behavior>
    - Test: `{ repsMin: 10, repsMax: 8 }` on an exercise fails validation with message "Min must be ≤ max" (path repsMax) — per PRES-01/D-10.
    - Test: a valid range `{ repsMin: 8, repsMax: 10 }` passes.
    - Test: `targetRpe: 11` fails (out of 1–10); `targetRpe: 8.5` passes (0.5 allowed); blank/omitted targetRpe passes (optional).
    - Test: `timed: true` is accepted; omitting `timed` passes (missing ⇒ weighted, D-11 back-compat).
    - Test: an exercise object with only the legacy `reps` field (no repsMin/repsMax/targetRpe/timed) still validates (v1.0 back-compat).
  </behavior>
  <action>
    Add `repsMin?: number`, `repsMax?: number`, `targetRpe?: number`, `timed?: boolean` to `RoutineExercise` in src/types/routine.ts, keeping legacy `reps?` (D-10 back-compat). Mirror the same four fields as `repsMin: number | null; repsMax: number | null; targetRpe: number | null; timed: boolean;` on `AssignmentSnapshotExercise` in src/types/assignment.ts (snapshot uses `T | null` per the file's null-not-undefined doc convention; `timed` defaults `false` not null since it is always present on new snapshots).
    In src/validation/routine.schema.ts add to the exercise object: `repsMin: z.coerce.number().int().positive().optional()`, `repsMax: z.coerce.number().int().positive().optional()`, `targetRpe: z.coerce.number().min(1).max(10).optional()`, `timed: z.boolean().optional()`. Attach a `.refine(d => d.repsMin == null || d.repsMax == null || d.repsMin <= d.repsMax, { message: 'Min must be ≤ max', path: ['repsMax'] })` to the exercise object. Use zod v4 API only (D-10/D-11; per CLAUDE.md zod ^4.4.3). Do NOT remove legacy `reps`.
    Extend src/validation/__tests__/routine.schema.test.ts with the cases in `<behavior>`, mirroring the existing test structure.
  </action>
  <acceptance_criteria>
    - `src/types/routine.ts` contains `repsMin`, `repsMax`, `targetRpe`, `timed` AND still contains `reps?`.
    - `src/types/assignment.ts` `AssignmentSnapshotExercise` contains `repsMin`, `repsMax`, `targetRpe`, `timed`.
    - `src/validation/routine.schema.ts` contains the string "Min must be ≤ max" and `repsMin`.
    - Command `npx jest src/validation/__tests__/routine.schema.test.ts -x` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx jest src/validation/__tests__/routine.schema.test.ts -x</automated>
  </verify>
  <done>Types extended additively (legacy `reps` retained); routine schema enforces rep range + RPE bounds + timed flag; schema tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Propagate prescription fields through the Cloud Function snapshot builder (TIER 4)</name>
  <files>functions/src/index.ts, functions/src/__tests__/createAssignment.test.ts</files>
  <read_first>
    - functions/src/index.ts — `SnapshotExercise` interface (lines ~160-173) and `buildSnapshotExercise` (main return + alternativeExercise branch); the `reps`/`duration` `?? null` coalescing pattern is the analog
    - functions/src/__tests__/createAssignment.test.ts (self — existing `setPayload.snapshot...` assertion pattern to extend)
    - 05-PATTERNS.md § `functions/src/index.ts → buildSnapshotExercise` (exact copy lines + alt branch + ops note)
    - 05-RESEARCH.md Pitfall 1 (the five-tier trap) + Runtime State Inventory (redeploy + grant-invoker.mjs 403)
  </read_first>
  <behavior>
    - Test: a snapshot built from a routine exercise with `repsMin: 8, repsMax: 10, targetRpe: 8, timed: false` produces a snapshot exercise carrying those exact four fields.
    - Test: the alternative-exercise branch also carries `repsMin/repsMax/targetRpe/timed`.
    - Test: a source exercise missing the new fields defaults safely — `timed ⇒ false`, `repsMin/repsMax/targetRpe ⇒ null` (no undefined).
  </behavior>
  <action>
    In functions/src/index.ts add `repsMin: number | null; repsMax: number | null; targetRpe: number | null; timed: boolean;` to the `SnapshotExercise` interface. In `buildSnapshotExercise`, copy into the MAIN return object (mirroring the existing `reps`/`duration` `?? null` coalescing): `repsMin: (routineEx.repsMin as number | undefined) ?? null`, `repsMax: (routineEx.repsMax as number | undefined) ?? null`, `targetRpe: (routineEx.targetRpe as number | undefined) ?? null`, `timed: (routineEx.timed as boolean | undefined) ?? false`. Add the SAME four fields to the `alternativeExercise` branch return (alternatives default identically: `timed: false`, others `null`). This is TIER 4 — without it the Plan 04 builder fields never reach the client (Pitfall 1).
    Extend functions/src/__tests__/createAssignment.test.ts with the `<behavior>` cases, asserting via the existing `setPayload.snapshot.weeks[...].days[...].routine.exercises[...]` access pattern (and the alternative-exercise sub-object).
    Note for the executor (do NOT auto-run): functions must be redeployed (`firebase deploy --only functions`) and if redeploy returns 403, re-run `scripts/grant-invoker.mjs` (project memory) — this is surfaced as an ops note in the SUMMARY and exercised on-device in Plan 06, not in this autonomous plan.
  </action>
  <acceptance_criteria>
    - `functions/src/index.ts` `SnapshotExercise` and `buildSnapshotExercise` both contain `targetRpe` and `timed` (grep `grep -v '^\s*//' functions/src/index.ts | grep -c targetRpe` ≥ 2 to count interface + return).
    - The alternative-exercise branch in `buildSnapshotExercise` sets `timed`.
    - Command `npx jest functions/src/__tests__/createAssignment.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx jest functions/src/__tests__/createAssignment.test.ts</automated>
  </verify>
  <done>The Cloud Function snapshot carries repsMin/repsMax/targetRpe/timed on both the main and alternative branches with safe defaults; tests green; redeploy ops note recorded in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trainer client → Cloud Function (createAssignment) | trainer-supplied routine fields are copied into the immutable snapshot server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `buildSnapshotExercise` field injection via routine doc | mitigate | Builder reads only the known named fields and coerces types server-side (`as number \| undefined ?? null`, `as boolean ?? false`); no spread of arbitrary client keys into the snapshot |
| T-05-02 | Tampering | routine zod schema bypass (min>max, RPE out of range) | mitigate | `.refine` rep-range + `min(1).max(10)` RPE bounds reject malformed prescriptions before write |
</threat_model>

<verification>
- `npx jest src/validation/__tests__/routine.schema.test.ts -x` green
- `npx jest functions/src/__tests__/createAssignment.test.ts` green
- `npx tsc --noEmit` clean (types compile)
</verification>

<success_criteria>
- RoutineExercise + AssignmentSnapshotExercise + SnapshotExercise all carry repsMin/repsMax/targetRpe/timed (legacy `reps` retained)
- routine schema enforces rep range + RPE bounds + timed flag
- Cloud Function snapshot builder copies the four fields on both main and alternative branches with safe defaults
- All extended/new tests green; tsc clean
</success_criteria>

<output>
Create `.planning/phases/05-per-set-logging-prescription-timers/05-01-SUMMARY.md` when done. Record the functions-redeploy + grant-invoker.mjs ops note for Plan 06.
</output>
