---
phase: 05-per-set-logging-prescription-timers
plan: 02
type: tdd
wave: 0
depends_on: [05-01]
files_modified:
  - src/types/session.ts
  - src/lib/sessionFinalize.ts
  - src/lib/__tests__/sessionFinalize.test.ts
  - src/lib/prefill.ts
  - src/lib/__tests__/prefill.test.ts
  - src/lib/timer.ts
  - src/lib/__tests__/timer.test.ts
  - src/validation/loggedExercise.schema.ts
  - src/validation/__tests__/loggedExercise.schema.test.ts
  - src/stores/sessionStore.ts
  - src/stores/__tests__/sessionStore.test.ts
autonomous: true
requirements: [LOG-01, LOG-02, LOG-03, LOG-04, TIMR-03]
must_haves:
  truths:
    - "completedExerciseIds is derived from loggedExercises — an exercise with ≥1 checked set counts complete (D-08, HIST-04 invariant preserved)"
    - "The finalize payload uses null (never undefined) for unlogged weight/reps/rpe so the field persists through stripUndefinedDeep"
    - "Prefill seeds a set from the client's last session for that exercise, falling back to the trainer target on the first session, and does not crash on old v1.0 sessions with no loggedExercises"
    - "Timer remaining is computed from an absolute endsAt and clamps at 0"
    - "loggedSets live state survives in sessionStore (partialize) and resets on clearSession"
  artifacts:
    - path: "src/lib/sessionFinalize.ts"
      provides: "deriveCompletedExerciseIds + buildFinalizedSession (pure)"
      contains: "deriveCompletedExerciseIds"
    - path: "src/lib/prefill.ts"
      provides: "resolvePrefill (pure, last-session → target fallback)"
      contains: "resolvePrefill"
    - path: "src/lib/timer.ts"
      provides: "remainingMs / addFifteen / isExpired (pure)"
      contains: "remainingMs"
    - path: "src/validation/loggedExercise.schema.ts"
      provides: "loggedSet/loggedExercise zod validators (nullable, not optional)"
      contains: "nullable"
    - path: "src/stores/sessionStore.ts"
      provides: "loggedSets record + setSetValue/toggleSet/seedExercise + partialize"
      contains: "loggedSets"
    - path: "src/types/session.ts"
      provides: "LoggedSet/LoggedExercise + optional Session.loggedExercises"
      contains: "LoggedExercise"
  key_links:
    - from: "src/lib/sessionFinalize.ts"
      to: "completedExerciseIds (HIST-04 / StatusBadge)"
      via: "deriveCompletedExerciseIds(logged)"
      pattern: "completedExerciseIds"
    - from: "src/stores/sessionStore.ts"
      to: "AsyncStorage persist"
      via: "partialize includes loggedSets"
      pattern: "loggedSets"
---

<objective>
Build the four Wave-0 pure units and the crash-safe store extension that the UI waves consume, test-first: `src/lib/sessionFinalize.ts` (LOG-04/D-08, HIST-04 invariant), `src/lib/prefill.ts` (LOG-03/D-09), `src/lib/timer.ts` (TIMR-03), `src/validation/loggedExercise.schema.ts` (LOG-04), and the `loggedSets` extension to `sessionStore` (LOG-01/02). Also add the `LoggedSet`/`LoggedExercise` types + optional `Session.loggedExercises`.

Purpose: These are framework-free, deterministic functions and an established-pattern store extension — they belong test-first so the HIST-04 adherence/StatusBadge contract, the one-tap prefill, the absolute-endsAt timer math, and the null-not-undefined finalize payload are locked before any UI is wired.
Output: Five tested units (three pure libs, one zod validator, one store extension) + additive session types. All tests green.
</objective>

<execution_context>
@/Users/FF-rodrigo/Documents/Personal/LauFit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/FF-rodrigo/Documents/Personal/LauFit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-per-set-logging-prescription-timers/05-CONTEXT.md
@.planning/phases/05-per-set-logging-prescription-timers/05-RESEARCH.md
@.planning/phases/05-per-set-logging-prescription-timers/05-PATTERNS.md
@.planning/phases/05-per-set-logging-prescription-timers/05-VALIDATION.md
@src/types/session.ts
@src/lib/adherence.ts
@src/lib/sessionDetail.ts
@src/lib/workoutDayComputer.ts
@src/validation/routine.schema.ts
@src/stores/sessionStore.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Session types + sessionFinalize.ts + loggedExercise zod schema</name>
  <files>src/types/session.ts, src/lib/sessionFinalize.ts, src/lib/__tests__/sessionFinalize.test.ts, src/validation/loggedExercise.schema.ts, src/validation/__tests__/loggedExercise.schema.test.ts</files>
  <read_first>
    - src/types/session.ts (self — additive; null-not-undefined doc lines 6-9; keep completedExerciseIds/totalExercises)
    - src/lib/adherence.ts + src/lib/__tests__/adherence.test.ts (analog: derivation-from-sessions + test structure + module header citing req IDs)
    - src/lib/sessionDetail.ts (analog: pure resolver, `?? []` null-guard at ~line 43)
    - src/validation/routine.schema.ts (analog: zod v4 API)
    - 05-PATTERNS.md § sessionFinalize.ts, § src/types/session.ts, § loggedExercise.schema.ts (exact shapes)
    - 05-RESEARCH.md Pitfall 4 (completedExerciseIds regression) + Pitfall 5 (null-not-undefined)
  </read_first>
  <behavior>
    - Test (sessionFinalize): `deriveCompletedExerciseIds` includes an exercise with ≥1 set `completed:true`, excludes one with 0 checked sets (D-08).
    - Test: `buildFinalizedSession(liveState, resolvedExercises)` returns `loggedExercises` with `{ weight:null, reps:null, rpe:null, completed:false }` for unlogged sets — and the JSON-serialized payload contains NO `undefined` keys.
    - Test: `totalExercises === resolvedExercises.length`; `completedExerciseIds` equals the derived set.
    - Test: empty/missing inputs do not throw (null-guard like sessionDetail `?? []`).
    - Test (loggedExercise.schema): `loggedSetSchema` accepts `null` weight/reps/rpe; requires `completed:boolean`; rejects `undefined` for the nullable fields.
  </behavior>
  <action>
    In src/types/session.ts add `LoggedSet { setNumber: number; weight: number|null; reps: number|null; rpe: number|null; completed: boolean }` and `LoggedExercise { exerciseId: string; name: string; timed: boolean; sets: LoggedSet[] }` above `Session`, then add `loggedExercises?: LoggedExercise[]` to `Session` (optional — v1.0 back-compat; KEEP `completedExerciseIds`/`totalExercises`).
    Create src/lib/sessionFinalize.ts (pure, no React/Firebase; module header cites LOG-04/D-07/D-08/HIST-04 invariant): export `deriveCompletedExerciseIds(logged: LoggedExercise[]): string[]` = exercises with `sets.some(s => s.completed)` mapped to exerciseId; export `buildFinalizedSession(...)` returning `Omit<Session,'id'>`-shaped data with `loggedExercises`, `completedExerciseIds = deriveCompletedExerciseIds(...)`, `totalExercises = resolvedExercises.length`. Coerce unlogged set fields to `null` explicitly (NOT optional/undefined — stripUndefinedDeep would drop them; Pitfall 5). Null-guard inputs with `?? []`.
    Create src/validation/loggedExercise.schema.ts mirroring routine.schema.ts: `loggedSetSchema` with `setNumber: z.coerce.number().int().positive()`, `weight: z.coerce.number().nonnegative().nullable()`, `reps: z.coerce.number().int().nonnegative().nullable()`, `rpe: z.coerce.number().min(1).max(10).nullable()`, `completed: z.boolean()`; and a `loggedExerciseSchema`. Use `.nullable()` NOT `.optional()` (Pitfall 5). zod v4 API only.
    Create both co-located test files with the `<behavior>` cases.
  </action>
  <acceptance_criteria>
    - `src/types/session.ts` contains `LoggedExercise` and `loggedExercises?` and still contains `completedExerciseIds`.
    - `src/lib/sessionFinalize.ts` contains `deriveCompletedExerciseIds`.
    - `src/validation/loggedExercise.schema.ts` contains `.nullable()` (not `.optional()` for weight/reps/rpe).
    - `npx jest src/lib/__tests__/sessionFinalize.test.ts -x` exits 0.
    - `npx jest src/validation/__tests__/loggedExercise.schema.test.ts -x` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx jest src/lib/__tests__/sessionFinalize.test.ts src/validation/__tests__/loggedExercise.schema.test.ts -x</automated>
  </verify>
  <done>Session types additive; finalize derivation preserves the HIST-04 ≥1-set rule and emits null (not undefined) for unlogged fields; logged-set validator uses nullable; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: prefill.ts + timer.ts pure libs</name>
  <files>src/lib/prefill.ts, src/lib/__tests__/prefill.test.ts, src/lib/timer.ts, src/lib/__tests__/timer.test.ts</files>
  <read_first>
    - src/lib/sessionDetail.ts (analog: `resolveSessionExercises` pure resolver over snapshot + a session field; `?? []` null-guard)
    - src/lib/workoutDayComputer.ts + its test (analog: pure number/date math, small named exports, Math.max clamps)
    - src/types/assignment.ts (AssignmentSnapshotExercise — prefill reads snapshot target: repsMin / legacy reps)
    - 05-PATTERNS.md § prefill.ts, § timer.ts (exact signatures)
    - 05-RESEARCH.md "Timer math (pure)" + D-09/D-02 (last-session → target fallback + carry-down)
  </read_first>
  <behavior>
    - Test (prefill): given a prior Session with a `loggedExercises` entry for the exercise, `resolvePrefill` returns that session's actual weight/reps per set.
    - Test: with no prior session (or only old v1.0 sessions lacking `loggedExercises`), it falls back to the snapshot target (repsMin or legacy reps; `weight: null`) and does NOT crash.
    - Test: carry-down within an exercise — set N+1's seed derives from set N's edited values (D-02).
    - Test (timer): `remainingMs(now+5000, now) === 5000`; `remainingMs` clamps a negative to 0; `addFifteen(endsAt) === endsAt + 15000`; `isExpired` is true at exactly 0 and when negative.
  </behavior>
  <action>
    Create src/lib/timer.ts (pure; header cites TIMR-03): `remainingMs(endsAt, now) = Math.max(0, endsAt - now)`, `addFifteen(endsAt) = endsAt + 15_000`, `isExpired(endsAt, now) = endsAt - now <= 0`, plus `formatMmSs(ms)` if the bar needs it. Mirror workoutDayComputer.ts style.
    Create src/lib/prefill.ts (pure; header cites LOG-03/D-09/D-02): `resolvePrefill(exercise: AssignmentSnapshotExercise, priorSessions: Session[]): LoggedSet[]`. Find the most-recent prior session's `loggedExercises` entry for `exercise.exerciseId` and seed each set from its actual weight/reps; on no match / missing `loggedExercises`, seed from the snapshot target (`repsMin ?? reps`, `weight: null`). Null-guard missing `loggedExercises` exactly like sessionDetail (`?? []`) — never throw on old sessions. Add a pure carry-down helper so set N+1 seeds from set N's values (D-02).
    Create both co-located tests with the `<behavior>` cases (mirror workoutDayComputer.test.ts + sessionDetail.test.ts structure).
  </action>
  <acceptance_criteria>
    - `src/lib/timer.ts` contains `remainingMs`, `addFifteen`, `isExpired`.
    - `src/lib/prefill.ts` contains `resolvePrefill` and null-guards missing `loggedExercises`.
    - `npx jest src/lib/__tests__/timer.test.ts src/lib/__tests__/prefill.test.ts -x` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx jest src/lib/__tests__/timer.test.ts src/lib/__tests__/prefill.test.ts -x</automated>
  </verify>
  <done>Timer math clamps from absolute endsAt; prefill prefers last-session actuals with target fallback and carry-down, safe on old sessions; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend sessionStore with crash-safe loggedSets live state</name>
  <files>src/stores/sessionStore.ts, src/stores/__tests__/sessionStore.test.ts</files>
  <read_first>
    - src/stores/sessionStore.ts (self — `INITIAL` lines ~73-83, `toggleExercise` immutable-update lines ~101-107, `partialize` lines ~117-127, `clearSession` set(INITIAL))
    - src/stores/__tests__/sessionStore.test.ts (self — `jest.mock(...)` before imports lines ~20-24, `setState(INITIAL)` beforeEach reset lines ~36-39)
    - 05-PATTERNS.md § sessionStore.ts + 05-RESEARCH.md "Session Store Extension" (loggedSets shape, partialize, hydration default)
  </read_first>
  <behavior>
    - Test: `setSetValue(exerciseId, setNumber, 'weight', 60)` writes into `loggedSets[exerciseId]`.
    - Test: `toggleSet(exerciseId, setNumber)` flips that set's `completed`.
    - Test: `seedExercise(exerciseId, seeds)` populates `loggedSets[exerciseId]` from a prefill seed array.
    - Test: `clearSession()` empties `loggedSets` back to `{}`.
    - Test: `partialize` output includes `loggedSets` (crash-safe persistence); a hydration blob missing `loggedSets` resolves to `{}` not `undefined`.
  </behavior>
  <action>
    In src/stores/sessionStore.ts add `loggedSets: Record<string, LoggedSet[]>` to `LocalSessionState` and to `INITIAL` (`{}`). Add actions mirroring the immutable-update style of `toggleExercise`: `setSetValue(exerciseId, setNumber, field: 'weight'|'reps'|'rpe', value: number|null)`, `toggleSet(exerciseId, setNumber)` (flips `completed`; carries the set's values down to N+1's prefill per D-02), `seedExercise(exerciseId, seeds: LoggedSet[])`. Add `loggedSets` to `partialize` so it persists (crash-safe). `clearSession` already `set(INITIAL)` → resets it; verify hydration of a pre-Phase-5 blob defaults `loggedSets` to `{}` (not `undefined`). Keep `toggleExercise` only if a v1.0 path still uses it; the per-set flow supersedes per-exercise toggling.
    Extend src/stores/__tests__/sessionStore.test.ts with the `<behavior>` cases, mirroring the existing `beforeEach` reset.
  </action>
  <acceptance_criteria>
    - `src/stores/sessionStore.ts` contains `loggedSets`, `setSetValue`, `toggleSet`, `seedExercise`, and `loggedSets` appears inside the `partialize` block.
    - `npx jest src/stores/__tests__/sessionStore.test.ts -x` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx jest src/stores/__tests__/sessionStore.test.ts -x</automated>
  </verify>
  <done>Store holds per-set live state, persists it via partialize, and resets it on clearSession; hydration defaults to {}; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live session state (Zustand+AsyncStorage) → Firestore SESSIONS write | client-owned per-set logs cross into a persisted document on finalize |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | finalize payload shape (undefined vs null) | mitigate | `buildFinalizedSession` coerces unlogged fields to `null`; loggedExercise zod uses `.nullable()` so `stripUndefinedDeep` cannot silently drop keys |
| T-05-04 | Information disclosure | per-set loads in SESSIONS | accept | Existing per-client SESSIONS rules (clientId == auth.uid) unchanged; this plan adds no new read path |
</threat_model>

<verification>
- `npx jest src/lib/__tests__/sessionFinalize.test.ts src/lib/__tests__/prefill.test.ts src/lib/__tests__/timer.test.ts -x` green
- `npx jest src/validation/__tests__/loggedExercise.schema.test.ts src/stores/__tests__/sessionStore.test.ts -x` green
- `npx tsc --noEmit` clean
</verification>

<success_criteria>
- Three pure libs (sessionFinalize, prefill, timer) + loggedExercise validator + sessionStore extension all implemented test-first with green tests
- Finalize derivation preserves the HIST-04 ≥1-set completion rule and emits null (not undefined)
- Prefill is safe on old v1.0 sessions
- loggedSets is crash-safe (in partialize) and resets on clearSession
</success_criteria>

<output>
Create `.planning/phases/05-per-set-logging-prescription-timers/05-02-SUMMARY.md` when done.
</output>
