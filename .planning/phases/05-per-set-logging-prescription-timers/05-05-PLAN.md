---
phase: 05-per-set-logging-prescription-timers
plan: 05
type: execute
wave: 2
depends_on: [05-02, 05-03]
files_modified:
  - package.json
  - app.config.js
  - assets/audio/alarm.mp3
  - src/hooks/useCountdownTimer.ts
  - src/hooks/__tests__/useCountdownTimer.test.ts
  - src/components/workout/RestTimerBar.tsx
  - src/components/workout/WorkTimerControl.tsx
  - src/app/client/workout/session.tsx
autonomous: false
user_setup:
  - service: expo native modules
    why: "expo-audio (alarm), expo-haptics (vibration), expo-keep-awake (screen-on) are native modules — require a dev-client rebuild on EAS"
    env_vars: []
requirements: [TIMR-01, TIMR-02, TIMR-03, TIMR-04]
must_haves:
  truths:
    - "Checking a set auto-starts a rest-timer countdown from the exercise's rest seconds, shown in an inline bar above the finish CTA with Skip and +15s"
    - "A timed exercise shows a manual Start that counts down from the exercise's duration"
    - "A running timer shows remaining time, keeps the screen awake, and resolves correctly from an absolute endsAt when briefly backgrounded"
    - "A timer reaching zero fires an alarm sound plus vibration exactly once; Skip dismisses with no alarm"
  artifacts:
    - path: "src/hooks/useCountdownTimer.ts"
      provides: "single timer hook (endsAt, tick, keep-awake, fire-once alarm+haptic)"
      contains: "useCountdownTimer"
    - path: "src/components/workout/RestTimerBar.tsx"
      provides: "inline pinned rest-timer bar (presentational)"
      contains: "onAdd15"
    - path: "src/components/workout/WorkTimerControl.tsx"
      provides: "timed-exercise Start pill + running row"
      contains: "onStart"
    - path: "assets/audio/alarm.mp3"
      provides: "bundled alarm asset"
  key_links:
    - from: "src/app/client/workout/session.tsx"
      to: "useCountdownTimer"
      via: "rest auto-start on set check; work manual Start"
      pattern: "useCountdownTimer"
    - from: "src/hooks/useCountdownTimer.ts"
      to: "src/lib/timer.ts"
      via: "remainingMs/addFifteen/isExpired from absolute endsAt"
      pattern: "remainingMs|addFifteen"
---

<objective>
Deliver the rest + work timers (TIMR-01/02/03/04): install the three native modules (`expo-audio`/`expo-haptics`/`expo-keep-awake`) via `npx expo install`, build the single `useCountdownTimer` hook (absolute `endsAt`, 250ms tick, foreground recompute, keep-awake, fire-once alarm+haptic), and the `RestTimerBar` + `WorkTimerControl` presentational components, then wire them into `session.tsx` (rest auto-starts on set check, work is a manual Start on timed exercises). Gate the native install behind a supply-chain checkpoint and the on-device timer behind a dev-client-rebuild checkpoint.

Purpose: This completes the in-gym training loop — the client runs trainer-set rest/work timers with an alarm. The timer math is already Wave-0 tested (`src/lib/timer.ts`); this plan owns the side-effectful hook (the only OS-module-owning unit) and the UI.
Output: Native installs + config, the timer hook with passing fire-once test, two timer components, and session.tsx timer wiring.
</objective>

<execution_context>
@/Users/FF-rodrigo/Documents/Personal/LauFit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/FF-rodrigo/Documents/Personal/LauFit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-per-set-logging-prescription-timers/05-CONTEXT.md
@.planning/phases/05-per-set-logging-prescription-timers/05-RESEARCH.md
@.planning/phases/05-per-set-logging-prescription-timers/05-PATTERNS.md
@.planning/phases/05-per-set-logging-prescription-timers/05-UI-SPEC.md
@.planning/phases/05-per-set-logging-prescription-timers/05-03-SUMMARY.md
@src/lib/timer.ts
@src/app/client/workout/session.tsx
@src/components/workout/ExerciseRow.tsx
@src/components/workout/FinishButton.tsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Supply-chain checkpoint — verify expo-audio / expo-haptics / expo-keep-awake before install</name>
  <what-built>Nothing yet — this gate verifies the three native packages are the official Expo first-party packages before installing. slopcheck was unavailable during research, so all three are tagged [ASSUMED] (05-RESEARCH.md § Package Legitimacy Audit). Legitimacy checkpoints are never auto-approvable.</what-built>
  <how-to-verify>
    1. Confirm each package is published under the official `expo` org and lives in the github.com/expo/expo monorepo:
       - https://www.npmjs.com/package/expo-audio
       - https://www.npmjs.com/package/expo-haptics
       - https://www.npmjs.com/package/expo-keep-awake
    2. Confirm the resolved version (after the install in Task 2) matches the SDK-55 compatibility table — `npx expo install` should pick an SDK-55-compatible version, NOT the npm `latest` 56.x line.
    3. Approve only if all three are first-party Expo packages.
  </how-to-verify>
  <resume-signal>Type "approved" once all three are confirmed Expo first-party, or describe the discrepancy.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install native modules + alarm asset + useCountdownTimer hook</name>
  <files>package.json, app.config.js, assets/audio/alarm.mp3, src/hooks/useCountdownTimer.ts, src/hooks/__tests__/useCountdownTimer.test.ts</files>
  <read_first>
    - 05-RESEARCH.md § Pattern 1 (the authoritative useCountdownTimer skeleton, lines ~144-187) + the expo-audio caveat (verify API against installed node_modules/expo-audio post-install; mark [ASSUMED] until verified) + § Native Rebuild + Supply-Chain
    - 05-PATTERNS.md § useCountdownTimer.ts (subscribe-then-cleanup from session.tsx:114-121; own the three native modules here so they're swappable; firedRef guard; skip suppresses alarm)
    - src/lib/timer.ts (remainingMs/addFifteen/isExpired — the hook composes these, never accumulates ticks)
    - src/stores/__tests__/sessionStore.test.ts (analog: `jest.mock(...)` before imports — mirror for mocking the three expo modules + AppState)
    - app.config.js (self — add any expo-audio config plugin like the expo-video precedent if required by the installed package)
  </read_first>
  <behavior>
    - With fake timers + mocked expo modules: the `onExpire` alarm callback fires exactly ONCE at expiry (firedRef guard).
    - `skip()` before expiry fires NO callback (alarm suppressed).
    - `add15()` extends `endsAt` by 15s.
    - The foreground-recompute path: simulating AppState 'active' recomputes `now` from `Date.now()` (never trusts accumulated ticks, D-06).
    - keep-awake is activated while a timer is running and released on stop/expiry (assert the mocked activate/deactivate calls).
  </behavior>
  <action>
    Run `npx expo install expo-audio expo-haptics expo-keep-awake` (NOT npm — the project is SDK 55; npm latest is SDK 56 and would break the New-Architecture build per 05-RESEARCH.md). Add a short bundled alarm asset at assets/audio/alarm.mp3 (reference via `require()`). If the installed expo-audio requires a config plugin, add it to app.config.js (mirror the expo-video plugin precedent).
    Create src/hooks/useCountdownTimer.ts from the RESEARCH Pattern 1 skeleton: own `endsAt` + a 250ms `setInterval` tick (only while active) + an `AppState 'active'` recompute + `expo-keep-awake` (`activateKeepAwakeAsync`/`deactivateKeepAwake`) while running + a `firedRef` fire-once guard that plays the expo-audio alarm asset and `Haptics.notificationAsync` once at `remainingMs <= 0`. Expose `{ remainingMs, isRunning, start(sec), add15(), skip() }`. Compose `remainingMs`/`addFifteen`/`isExpired` from src/lib/timer.ts. Isolate the expo-audio play call here and mark it `[ASSUMED]` — verify against node_modules/expo-audio after install; release the player on unmount. `skip()` sets the fired guard so NO alarm.
    Create src/hooks/__tests__/useCountdownTimer.test.ts mocking all three expo modules + AppState (mirror the jest.mock-before-imports pattern from sessionStore.test.ts), with the `<behavior>` cases under fake timers.
  </action>
  <acceptance_criteria>
    - `package.json` lists `expo-audio`, `expo-haptics`, `expo-keep-awake`.
    - `assets/audio/alarm.mp3` exists.
    - `src/hooks/useCountdownTimer.ts` contains `useCountdownTimer` and references `remainingMs`/`addFifteen` from src/lib/timer.ts.
    - `npx jest src/hooks/__tests__/useCountdownTimer.test.ts -x` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx jest src/hooks/__tests__/useCountdownTimer.test.ts -x</automated>
  </verify>
  <done>Native modules installed via expo install; alarm asset bundled; timer hook owns endsAt/keep-awake/fire-once alarm composed from the tested pure timer math; hook test green (alarm fires once, skip suppresses).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RestTimerBar + WorkTimerControl components and session.tsx timer wiring</name>
  <files>src/components/workout/RestTimerBar.tsx, src/components/workout/WorkTimerControl.tsx, src/app/client/workout/session.tsx</files>
  <read_first>
    - 05-UI-SPEC.md § A3 (timed exercise: Start pill idle / running row / Done chip), § A4 (inline rest-timer bar: 56px + insets.bottom, progress track, 28/600 mono countdown, final-10s #FFD600 flip, Skip/+15s outline pills), Copywriting ("Rest", "Skip", "+15s", "Start {duration}s", "Done"), Accessibility (`accessibilityRole="timer"` / "button")
    - 05-PATTERNS.md § RestTimerBar.tsx (pinned-bar analog session.tsx:399-411 `position:absolute bottom:0` + `border-t #2A2A2A` + insets.bottom; presentational only — arithmetic lives in the hook), § WorkTimerControl.tsx (FinishButton/PrimaryButton accent pill for idle Start), § session.tsx (mount RestTimerBar above bottom CTA; extend conditional paddingBottom +56 at session.tsx:392-394)
    - src/app/client/workout/session.tsx (self — bottom CTA container lines ~398-427; FlatList contentContainerStyle paddingBottom lines ~392-394; renderItem expanded card from Plan 03)
    - src/hooks/useCountdownTimer.ts (the hook to drive both timers)
  </read_first>
  <behavior>
    - Checking a set done (weighted) auto-starts the rest timer from that exercise's `rest`; the RestTimerBar appears pinned above the finish CTA and the FlatList bottom padding grows by 56px; checking another set restarts it from the new exercise's rest (last-check-wins, one at a time).
    - A timed exercise card shows a manual "Start {duration}s" accent pill (no auto-start); tapping starts the work timer; while running it shows mm:ss + Skip/+15s; on completion the exercise's implicit set marks complete → "Done" chip → contributes to completedExerciseIds.
    - Both timers show remaining derived from the hook, flip to `#FFD600` in the final 10s, fire alarm+haptic once at 0 (rest bar auto-dismisses), Skip dismisses with no alarm, +15s extends.
    - RestTimerBar/WorkTimerControl are presentational — they receive `remainingMs`/`totalMs`/state + callbacks and contain NO endsAt/keep-awake/alarm logic.
  </behavior>
  <action>
    Create src/components/workout/RestTimerBar.tsx (props `remainingMs, totalMs, onSkip, onAdd15`): the pinned bar per UI-SPEC A4 — `bg-[#0E0E0E]`, `border-t border-[#2A2A2A]`, 56px + insets.bottom, 4px progress track (remaining-shrinks `#00FF66`), countdown Display 28/600 mono `#FFFFFF` flipping to `#FFD600` in final 10s, `Skip`/`+15s` outline pills ≥44pt, `accessibilityRole="timer"`. Presentational only.
    Create src/components/workout/WorkTimerControl.tsx (props `durationSec, state: 'idle'|'running'|'done', remainingMs, onStart, onSkip, onAdd15`): idle accent Start pill (`bg-[#00FF66]` `#0E0E0E` "Start {duration}s" + Ionicons play), running row (mm:ss 20/600 mono + Skip/+15s + 4px track), done "✓ Done" chip `#00FF66`.
    In src/app/client/workout/session.tsx: instantiate `useCountdownTimer` for the rest timer; in the set done-check handler (from Plan 03) also call `restTimer.start(exercise.rest)` (auto-start, D-04). Mount `RestTimerBar` above the existing bottom CTA container when `restTimer.isRunning`, and extend the conditional `paddingBottom` (+56) at lines ~392-394. For timed exercises, render `WorkTimerControl` in the expanded card driven by a work-timer `useCountdownTimer` (manual Start, D-05); on its expiry/skip mark the timed exercise's implicit set complete in the store so D-08/completedExerciseIds stays consistent. Route alarm via the hook's `onExpire`.
  </action>
  <acceptance_criteria>
    - `src/components/workout/RestTimerBar.tsx` contains `onAdd15` and `accessibilityRole="timer"`; no `endsAt`/`activateKeepAwake` (presentational).
    - `src/components/workout/WorkTimerControl.tsx` contains `onStart` and the "Start" copy.
    - `src/app/client/workout/session.tsx` references `useCountdownTimer` and mounts `RestTimerBar`.
    - `npx jest --selectProjects react-native -x` exits 0 and `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx jest --selectProjects react-native -x && npx tsc --noEmit</automated>
  </verify>
  <done>Rest timer auto-starts on set check in an inline bar; work timer is a manual Start; both driven by the single hook with the alarm fire-once; components presentational; suite + tsc green.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 4: EAS dev-client rebuild for the three native modules</name>
  <what-built>expo-audio/expo-haptics/expo-keep-awake were installed (Task 2). Native modules do not work in the existing dev client or via JS-only OTA — a dev-client rebuild is required before on-device timer/alarm testing. There is no local Android SDK, so the build runs on EAS.</what-built>
  <how-to-verify>
    1. Trigger an EAS dev-client build (`eas build --profile development --platform android` and/or ios), as done for expo-video (Phase 3) and expo-image-picker (Phase 4).
    2. Install the new dev client on the test device.
    3. Confirm the app launches without a native-module "not found" / TurboModule error.
  </how-to-verify>
  <resume-signal>Type "rebuilt" once the new dev client is installed and launches, or paste the build error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm/expo install → app bundle (native modules) | supply-chain: three native packages added to the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-SC | Tampering | npx expo install of 3 native modules | mitigate | Blocking-human supply-chain checkpoint (Task 1) verifies all three are official Expo first-party packages on npmjs.com before install; slopcheck was unavailable so this manual gate stands in (never auto-approved) |
| T-05-09 | Denial of service | timer double-fire / drift on background | mitigate | Remaining derived from absolute `endsAt` (Wave-0 tested timer.ts) + `firedRef` fire-once guard; foreground recompute; verified in the hook test and on-device (Plan 06) |
</threat_model>

<verification>
- `npx jest src/hooks/__tests__/useCountdownTimer.test.ts -x` green (alarm fires once, skip suppresses)
- `npx jest --selectProjects react-native -x` green; `npx tsc --noEmit` clean
- On-device alarm sound + vibration + keep-awake verified in Plan 06 (manual-only per 05-VALIDATION.md)
</verification>

<success_criteria>
- Three native modules installed via expo install + dev-client rebuilt; supply-chain gate passed
- useCountdownTimer drives both timers from absolute endsAt with keep-awake + fire-once alarm+haptic
- RestTimerBar (auto, inline) + WorkTimerControl (manual Start) wired into session.tsx, presentational
- Hook test + RN suite + tsc green
</success_criteria>

<output>
Create `.planning/phases/05-per-set-logging-prescription-timers/05-05-SUMMARY.md` when done. Note the dev-client rebuild + (if applicable) any expo-audio config-plugin addition.
</output>
