---
phase: 05-per-set-logging-prescription-timers
plan: 06
type: execute
wave: 3
depends_on: [05-01, 05-04, 05-05]
files_modified:
  - firestore.rules
autonomous: false
requirements: [LOG-01, LOG-02, LOG-03, LOG-04, PRES-01, PRES-02, PRES-03, TIMR-01, TIMR-02, TIMR-03, TIMR-04]
must_haves:
  truths:
    - "The SESSIONS create rule permits the new loggedExercises shape while still enforcing client ownership (clientId == auth.uid) and write-once immutability"
    - "The createAssignment Cloud Function is redeployed so new assignments carry the prescription fields to the client"
    - "On a real device: the trainer prescribes rep range + RPE + rest/duration + timed, assigns, and the client sees those targets, logs per-set with prefill, runs rest + work timers, and the alarm sound + vibration fire at zero"
    - "An old v1.0 session (no loggedExercises) still loads in history and counts adherence unchanged"
  artifacts:
    - path: "firestore.rules"
      provides: "verified SESSIONS create/ownership rule (modified-state confirmed)"
      contains: "clientId == request.auth.uid"
  key_links:
    - from: "firestore.rules SESSIONS"
      to: "loggedExercises write on finalize"
      via: "ownership check permits the additive field shape"
      pattern: "clientId == request.auth.uid"
---

<objective>
Close Phase 5: (1) verify `firestore.rules` permits the additive `loggedExercises` SESSIONS shape while preserving client ownership + write-once immutability, (2) redeploy the `createAssignment` Cloud Function (and re-grant invoker if 403) so the new prescription fields reach clients, and (3) run the full on-device UAT for the manual-only behaviors that no unit test can cover — alarm sound, vibration, keep-awake, and the end-to-end trainer-prescribe → client-log → timer round-trip.

Purpose: The timer alarm/haptics/keep-awake and the full per-set logging round-trip are device-level (05-VALIDATION.md Manual-Only Verifications); the prescription propagation only reaches clients after a functions redeploy; and the SESSIONS rule must be confirmed to permit the new field shape. This is the phase gate.
Output: Confirmed/adjusted firestore.rules, redeployed functions, and a signed-off on-device UAT.
</objective>

<execution_context>
@/Users/FF-rodrigo/Documents/Personal/LauFit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/FF-rodrigo/Documents/Personal/LauFit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-per-set-logging-prescription-timers/05-CONTEXT.md
@.planning/phases/05-per-set-logging-prescription-timers/05-RESEARCH.md
@.planning/phases/05-per-set-logging-prescription-timers/05-VALIDATION.md
@.planning/phases/05-per-set-logging-prescription-timers/05-01-SUMMARY.md
@.planning/phases/05-per-set-logging-prescription-timers/05-05-SUMMARY.md
@firestore.rules
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verify firestore.rules SESSIONS create + redeploy rules and functions</name>
  <files>firestore.rules</files>
  <read_first>
    - firestore.rules (self — SESSIONS block lines ~138-143: `allow create, update: if isClient() && request.resource.data.clientId == request.auth.uid`; the rule is field-shape-agnostic, no per-field allowlist)
    - 05-PATTERNS.md § firestore.rules (verify-only: ownership + single-create preserves immutability D-12; file is modified in git status — confirm current state)
    - 05-RESEARCH.md § Security Domain (V4 access control: confirm the additive field is permitted and no new read path leaks) + Runtime Inventory (functions redeploy + grant-invoker.mjs 403)
    - 05-01-SUMMARY.md (the recorded functions-redeploy ops note)
  </read_first>
  <behavior>
    - The SESSIONS create rule still requires `isClient()` and `request.resource.data.clientId == request.auth.uid` — it does not allowlist fields, so the additive `loggedExercises` map is permitted without a rule change.
    - The firestore-rules jest project (if it covers SESSIONS create) stays green: a client can create its own session (now with loggedExercises) and cannot create one for another client.
  </behavior>
  <action>
    Read the current firestore.rules SESSIONS block (modified in git status) and confirm the create/ownership rule permits the additive `loggedExercises` shape while keeping `clientId == request.auth.uid` and preserving the single-create-on-finish immutability (D-12). No rule change is expected; if the current modified state weakened ownership, restore it. Optionally add numeric bounds per RESEARCH Security V5 only if it does not risk the write — ownership + single-write is the load-bearing control.
    Then deploy: `firebase deploy --only firestore:rules` and `firebase deploy --only functions` (the Plan 01 Cloud Function change). If the functions deploy returns 403, re-run `node scripts/grant-invoker.mjs` (project memory) and re-verify. Run the firestore-rules jest project to confirm SESSIONS rules still pass.
  </action>
  <acceptance_criteria>
    - `firestore.rules` SESSIONS block contains `clientId == request.auth.uid` (ownership preserved).
    - `npx jest --selectProjects firestore-rules` exits 0.
    - functions + rules deployed (recorded in SUMMARY); grant-invoker re-run if 403.
    - **Function redeploy PROVEN before UAT (W3):** after the functions deploy, create one assignment and confirm in the Firestore console (or a one-off read) that the written `AssignmentSnapshotExercise` carries the new `targetRpe`/`timed`/`repsMin`/`repsMax` fields — proving Plan 01's `buildSnapshotExercise` change actually shipped, since Task 3's on-device PRES round-trip silently depends on it. If the fields are absent, the deploy did not land — re-deploy before UAT.
  </acceptance_criteria>
  <verify>
    <automated>npx jest --selectProjects firestore-rules</automated>
  </verify>
  <done>SESSIONS rule confirmed to permit the additive loggedExercises shape with ownership + immutability intact; rules + functions redeployed; rules tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Full automated gate (jest + tsc) before on-device UAT</name>
  <files>(no source changes — gate only)</files>
  <read_first>
    - 05-VALIDATION.md § Sampling Rate (phase gate: full suite green + on-device manual check) and Per-Task Verification Map (all Wave 0 commands)
  </read_first>
  <action>
    Run the full automated gate to confirm every Wave-0 + UI plan is green before manual UAT: `npx jest` (both projects) and `npx tsc --noEmit`. If anything is red, stop and route back to the owning plan rather than proceeding to UAT.
  </action>
  <acceptance_criteria>
    - `npx jest` exits 0 (full suite, both projects).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx jest && npx tsc --noEmit</automated>
  </verify>
  <done>Full jest suite + tsc green across both projects — ready for on-device UAT.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: On-device UAT — prescription, per-set logging, timers, alarm, back-compat</name>
  <what-built>The full Phase 5 feature on the rebuilt dev client (Plan 05): trainer prescription (rep range / RPE / rest / duration / timed), client per-set logging with prefill, rest + work timers with alarm + vibration + keep-awake, single finalize write. This checkpoint verifies the manual-only behaviors (alarm sound, vibration, keep-awake, end-to-end round-trip) that no unit test covers (05-VALIDATION.md Manual-Only Verifications).</what-built>
  <how-to-verify>
    1. **Prescription propagation (PRES-01/02/03):** As the trainer, build a routine with a weighted exercise (sets, rep range e.g. 8–10, target RPE, rest e.g. 90s) and a timed exercise (Timed toggle ON, duration e.g. 60s). Assign the program to the test client (NEW assignment, so it uses the redeployed Cloud Function). As the client, confirm the workout card shows "{sets}×{min}–{max} · RPE {tgt}" for weighted and "{duration}s · Timed" for timed.
    2. **Per-set logging + prefill (LOG-01/02/03):** Expand the weighted exercise → confirm one row per set with prefilled muted values; check a set with only the done-check (one tap) and confirm it commits; edit weight/reps via keypad and RPE via the stepper. Finish, reopen the session detail → values persisted. Start the same workout again next day → sets prefill from the last session.
    3. **Rest timer (TIMR-01/03/04):** Check a set done → confirm the inline rest bar auto-starts from the rest seconds, counts down, the final 10s flips to yellow, Skip dismisses with no alarm, +15s extends. Let one run to 0 → confirm alarm sound + vibration fire once.
    4. **Work timer (TIMR-02/03/04):** On the timed exercise tap "Start {duration}s" → counts down; let it reach 0 → alarm + vibration once; the exercise shows "Done".
    5. **Keep-awake (TIMR-03):** Start a timer, leave the screen idle past the device lock timeout → screen stays on while running, releases after stop. Briefly background and foreground the app mid-timer → remaining is correct (resolved from endsAt, no drift/double-fire).
    6. **Back-compat (LOG-04):** Open an old v1.0 session (created before this phase) in history → it loads and shows adherence/StatusBadge unchanged (no crash, no blank).
    7. **Finalize (LOG-04):** Finish a session with some unlogged sets → confirms via the incomplete dialog, saves; per-exercise completion counts an exercise complete at ≥1 checked set (D-08); adherence stays correct.
  </how-to-verify>
  <resume-signal>Type "verified" if all 7 checks pass, or list the failing checks with details.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → SESSIONS Firestore write | the additive loggedExercises field crosses the security-rule boundary |
| trainer client → createAssignment Cloud Function | redeployed function copies prescription into the immutable snapshot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Elevation of privilege | SESSIONS create for another client | mitigate | Rule keeps `clientId == request.auth.uid`; verified in this plan + firestore-rules jest project |
| T-05-11 | Tampering | write-once session immutability | mitigate | Single create on finalize (D-12); rule does not grant broad update beyond ownership; confirmed during rules review |
| T-05-12 | Information disclosure | cross-client read of logged loads | mitigate | SESSIONS read rule unchanged (owning client OR the client's trainer); no new read path added this phase |
</threat_model>

<verification>
- `npx jest --selectProjects firestore-rules` green; `npx jest` (full) green; `npx tsc --noEmit` clean
- Rules + functions redeployed (grant-invoker re-run if 403)
- On-device UAT (7 checks) signed off — covers TIMR-04 alarm/vibration, keep-awake, prescription round-trip, and v1.0 back-compat
</verification>

<success_criteria>
- firestore.rules SESSIONS ownership + immutability confirmed for the new field shape
- Cloud Function redeployed; new assignments carry the prescription
- Full jest suite + tsc green
- On-device UAT passes all 7 checks (prescription, per-set logging + prefill, rest timer, work timer, keep-awake/foreground, back-compat, finalize)
</success_criteria>

<output>
Create `.planning/phases/05-per-set-logging-prescription-timers/05-06-SUMMARY.md` when done. Mark Phase 5 ready for `/gsd-verify-work`.
</output>
