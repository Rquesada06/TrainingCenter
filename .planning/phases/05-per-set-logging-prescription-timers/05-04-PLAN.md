---
phase: 05-per-set-logging-prescription-timers
plan: 04
type: execute
wave: 1
depends_on: [05-01]
files_modified:
  - src/components/routines/RoutineExerciseRow.tsx
  - src/components/routines/RoutineBuilder.tsx
autonomous: true
requirements: [PRES-01, PRES-02, PRES-03]
must_haves:
  truths:
    - "In the routine builder the trainer sets per-exercise targets — sets count, rep range (min–max), and target RPE — plus a rest period, via the builder row"
    - "A Timed toggle switches the visible field set: ON shows Sets + Duration and hides Reps-range/Target-RPE/Rest; OFF shows the weighted fields; hidden fields retain their values (reversible)"
    - "Rep-range validation rejects min > max with 'Min must be ≤ max' at the builder"
    - "New exercises added to a routine seed the new fields with safe defaults (timed:false)"
  artifacts:
    - path: "src/components/routines/RoutineExerciseRow.tsx"
      provides: "repsMin/repsMax/targetRpe fields + Timed toggle + duration switching"
      contains: "repsMin"
    - path: "src/components/routines/RoutineBuilder.tsx"
      provides: "append seed includes timed:false + repsMin/repsMax from defaults"
      contains: "timed"
  key_links:
    - from: "src/components/routines/RoutineExerciseRow.tsx"
      to: "routine.schema.ts (zod refine from Plan 01)"
      via: "RHF Controller fields validated on submit"
      pattern: "repsMin|repsMax|targetRpe"
    - from: "src/components/routines/RoutineExerciseRow.tsx Timed toggle"
      to: "RoutineExercise.timed"
      via: "explicit boolean controller (D-11)"
      pattern: "timed"
---

<objective>
Restructure the trainer routine-builder row to capture the new prescription (PRES-01/02/03): replace single `Reps` with a rep range (min–max), add an optional Target RPE field, and add an explicit Timed toggle (D-11) that switches the visible field set between weighted (Sets / Reps-range / Target-RPE / Rest) and timed (Sets / Duration). Seed the new fields on append in `RoutineBuilder`. This is tier 3 of the five-tier propagation — tiers 1/2/4/5 (types, zod, Cloud Function, snapshot type) landed in Plan 01.

Purpose: This is where the trainer actually enters the targets and timer values. Because Plan 01 already wired the Cloud Function snapshot copy, the fields entered here will reach the client workout screen on the next assignment.
Output: Modified RoutineExerciseRow (new fields + toggle + conditional visibility) and RoutineBuilder (seed defaults).
</objective>

<execution_context>
@/Users/FF-rodrigo/Documents/Personal/LauFit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/FF-rodrigo/Documents/Personal/LauFit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-per-set-logging-prescription-timers/05-CONTEXT.md
@.planning/phases/05-per-set-logging-prescription-timers/05-PATTERNS.md
@.planning/phases/05-per-set-logging-prescription-timers/05-UI-SPEC.md
@src/components/routines/RoutineExerciseRow.tsx
@src/components/routines/RoutineBuilder.tsx
@src/components/workout/GymHomeToggle.tsx
@src/validation/routine.schema.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rep-range + Target RPE + Timed toggle in RoutineExerciseRow</name>
  <files>src/components/routines/RoutineExerciseRow.tsx</files>
  <read_first>
    - src/components/routines/RoutineExerciseRow.tsx (self — RHF Controller + TextField pattern lines ~75-137; numeric Controller convention lines ~78-86: `value={String(field.value ?? '')}`, `onChangeText={(v) => field.onChange(v ? Number(v) : undefined)}`)
    - src/components/workout/GymHomeToggle.tsx (analog: switch visual language + accessibilityRole + hitSlop for the Timed toggle)
    - src/validation/routine.schema.ts (the refine added in Plan 01 — surfaces "Min must be ≤ max" via fieldState.error)
    - 05-UI-SPEC.md § B1 (weighted layout), § B2 (timed layout + Timed badge #FFD600), § New Components → TimedToggle, Copywriting (labels: "Sets", "Reps (min–max)", "Target RPE", "Rest (s)", "Duration (s)", "Timed"; errors "Min must be ≤ max" / "RPE must be 1–10"), Accessibility (switch role)
    - 05-PATTERNS.md § RoutineExerciseRow.tsx (exact Controller names `exercises.${index}.repsMin` etc.; hidden fields RETAIN values)
  </read_first>
  <behavior>
    - With Timed OFF (default): the row shows Sets, Reps (min–max) as two numeric fields with an en-dash separator, Target RPE (optional), and Rest (s); duration is hidden.
    - With Timed ON: the row shows Sets + Duration (s) and a `#FFD600` "Timed" badge; Reps-range / Target-RPE / Rest are hidden but their RHF values are NOT cleared (toggling back restores them — reversible).
    - Submitting with repsMin > repsMax surfaces "Min must be ≤ max" on the repsMax field (from the Plan 01 zod refine).
    - The Timed toggle has `accessibilityRole="switch"` + `accessibilityState={{ checked }}` and a ≥44pt target.
  </behavior>
  <action>
    In src/components/routines/RoutineExerciseRow.tsx add RHF Controllers for `exercises.${index}.repsMin`, `exercises.${index}.repsMax`, `exercises.${index}.targetRpe`, and `exercises.${index}.timed`, following the existing numeric Controller convention verbatim (`value={String(field.value ?? '')}`, `onChangeText={(v) => field.onChange(v ? Number(v) : undefined)}`, `keyboardType="number-pad"`, `error={fieldState.error?.message}`). Render rep range as two fields with an en-dash `–` separator labeled "Reps (min–max)". Add the inline Timed toggle (reuse GymHomeToggle's switch visual language: `bg-[#444444]` off / `bg-[#00FF66]` on, `#FFFFFF` thumb, label "Timed", `accessibilityRole="switch"`). Conditionally render: timed ON → Sets + Duration(s) + Timed badge, hide Reps-range/Target-RPE/Rest; timed OFF → Sets + Reps-range + Target-RPE + Rest, hide Duration. Hidden fields must KEEP their RHF values (do not unregister/clear) so the toggle is reversible without data loss (D-11). Use the exact copy strings from UI-SPEC.
  </action>
  <acceptance_criteria>
    - `src/components/routines/RoutineExerciseRow.tsx` contains `repsMin`, `repsMax`, `targetRpe`, and a `timed` Controller with `accessibilityRole="switch"`.
    - The duration field is rendered only when timed is on (conditional present in source).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Builder row captures rep range + target RPE + rest (weighted) or duration (timed) driven by an explicit reversible Timed toggle; rep-range error surfaces; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seed new prescription defaults on append in RoutineBuilder</name>
  <files>src/components/routines/RoutineBuilder.tsx</files>
  <read_first>
    - src/components/routines/RoutineBuilder.tsx (self — `handleExercisesSelected` `append({...})` seed lines ~90-107)
    - 05-PATTERNS.md § RoutineBuilder.tsx + ExercisePickerSheet.tsx (append seed: repsMin/repsMax from defaults, timed:false, targetRpe:undefined; the append happens in RoutineBuilder, not the picker)
    - src/validation/routine.schema.ts (the seed must satisfy the schema on submit)
  </read_first>
  <behavior>
    - Adding exercises to a routine seeds each new entry with `timed: false`, and `repsMin`/`repsMax` derived from the exercise's default reps (legacy `reps`/`defaultReps`) so the row is valid out of the box.
    - `targetRpe` seeds as undefined/blank (optional).
    - Existing seeded fields (sets, rest, duration) are unchanged.
  </behavior>
  <action>
    In src/components/routines/RoutineBuilder.tsx extend the `append({...})` seed object in `handleExercisesSelected` to include `timed: false`, `repsMin` and `repsMax` seeded from the exercise's default reps (if a `defaultReps`/`reps` value exists, seed both to it; else leave undefined), and `targetRpe: undefined`. Do not change the existing sets/rest/duration/notes seeding. Verify `ExercisePickerSheet.tsx` does not also need touching (the append happens here — only touch the picker if the seed flows through it).
  </action>
  <acceptance_criteria>
    - `src/components/routines/RoutineBuilder.tsx` `append(...)` seed contains `timed` and `repsMin`.
    - `npx jest --selectProjects react-native -x` exits 0.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>npx jest --selectProjects react-native -x && npx tsc --noEmit</automated>
  </verify>
  <done>New routine exercises seed timed:false + rep range from defaults so the builder row is valid immediately; suite + tsc green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trainer form input → routine zod → ROUTINES write | trainer-entered prescription validated before persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering | malformed prescription (min>max, RPE>10) | mitigate | The Plan 01 zod `.refine` + `min(1).max(10)` reject invalid values at submit; the builder surfaces the error on the field |
| T-05-08 | Tampering | timed-state ambiguity inferred from fields | mitigate | `timed` is an explicit persisted boolean (D-11); never inferred from field presence, so client rendering is unambiguous |
</threat_model>

<verification>
- `npx jest --selectProjects react-native -x` green
- `npx tsc --noEmit` clean
- (Plan 01 already green for the zod refine + Cloud Function propagation tests)
</verification>

<success_criteria>
- Builder row captures rep range + target RPE + rest/duration switched by an explicit reversible Timed toggle
- New exercises seed safe defaults (timed:false, rep range from defaults)
- Rep-range validation error surfaces at the field
- RN suite + tsc green
</success_criteria>

<output>
Create `.planning/phases/05-per-set-logging-prescription-timers/05-04-SUMMARY.md` when done.
</output>
