---
phase: 04-history-polish
plan: 04
type: execute
wave: 2
depends_on: ["04-01", "04-03"]
files_modified:
  - src/app/client/history.tsx
  - src/app/client/history/[sessionId].tsx
  - src/app/client/_layout.tsx
  - src/app/trainer/clients/[clientId].tsx
autonomous: true
requirements: [HIST-01, HIST-02, HIST-03]
must_haves:
  truths:
    - "Client can open a paginated, newest-first list of their completed sessions and tap one"
    - "Tapping a session shows which exercises were completed vs skipped"
    - "Trainer sees the same paginated session list inside a specific client's profile"
    - "An empty history shows a purposeful EmptyState, not a blank screen"
  artifacts:
    - path: "src/app/client/history.tsx"
      provides: "client session history list screen (HIST-01)"
      min_lines: 40
    - path: "src/app/client/history/[sessionId].tsx"
      provides: "session detail screen (HIST-02)"
      min_lines: 40
    - path: "src/app/trainer/clients/[clientId].tsx"
      provides: "trainer's inline client history list (HIST-03)"
      contains: "useSessionHistory"
  key_links:
    - from: "src/app/client/history.tsx"
      to: "src/hooks/useSessionHistory.ts"
      via: "useSessionHistory(uid) + fetchNextPage on end"
      pattern: "useSessionHistory"
    - from: "src/app/client/history/[sessionId].tsx"
      to: "src/lib/sessionDetail.ts"
      via: "resolveSessionExercises(day, completedExerciseIds)"
      pattern: "resolveSessionExercises"
    - from: "src/app/trainer/clients/[clientId].tsx"
      to: "src/components/sessions/SessionListItem.tsx"
      via: "renders SessionListItem rows"
      pattern: "SessionListItem"
---

<objective>
Wire the session-history feature end-to-end on both roles: a client History tab + list, a
session-detail screen, and the trainer's inline history list inside a client's profile —
all consuming the Wave 1 hooks/components (useSessionHistory, useAssignment, SessionListItem,
StatusBadge, EmptyState, resolveSessionExercises).

Purpose: Deliver HIST-01 (client paginated history), HIST-02 (session detail), HIST-03
(trainer's view of a client's history) using the same paginated reader for both roles (D-04).
Output: client/history.tsx, client/history/[sessionId].tsx, client/_layout.tsx (History tab),
and the trainer [clientId].tsx history section replacing the placeholder.
</objective>

<execution_context>
@/Users/FF-rodrigo/Documents/Personal/LauFit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/FF-rodrigo/Documents/Personal/LauFit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-history-polish/04-UI-SPEC.md
@.planning/phases/04-history-polish/04-PATTERNS.md
@src/types/session.ts
@src/types/assignment.ts
@src/app/trainer/clients/[clientId].tsx
@src/app/client/_layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Client History tab + paginated list screen (HIST-01)</name>
  <files>src/app/client/history.tsx, src/app/client/_layout.tsx</files>
  <read_first>
    - .planning/phases/04-history-polish/04-UI-SPEC.md § "1. Session History List" (layout, loading/error/empty states, footer copy, onEndReached threshold) + § "Copywriting Contract" (history title "History", "All sessions loaded", empty-state copy)
    - .planning/phases/04-history-polish/04-PATTERNS.md § "src/app/client/history.tsx" (analog: trainer/clients/index.tsx — SafeAreaView + loading + FlatList + ListEmptyComponent; flatten pages; onEndReached guard)
    - .planning/phases/04-history-polish/04-RESEARCH.md § "Pattern 1" (FlatList integration: onEndReached guard !isFetchingNextPage && hasNextPage)
    - src/app/client/_layout.tsx (Tabs — add a History tab between Home and Profile; pushed routes hidden via href:null pattern already used for `workout`)
    - src/hooks/useSessionHistory.ts (from 04-01) ; src/components/sessions/SessionListItem.tsx + src/components/ui/EmptyState.tsx (from 04-03) ; src/stores/authStore.ts (uid)
  </read_first>
  <action>
    Per HIST-01 and D-04/D-12: Create `src/app/client/history.tsx` (default export `ClientHistoryScreen`). Read `uid = useAuthStore((s) => s.uid)`; call `useSessionHistory(uid)`. SafeAreaView bg '#0E0E0E', paddingHorizontal 16. Title "History" (Display 28/600 '#FFFFFF'). Flatten pages: `const sessions = data?.pages.flatMap((p) => p.items) ?? []`. Initial loading (`isLoading`): centered ActivityIndicator color '#00FF66' size large. Error (`isError`): the error block from UI-SPEC § 1 ("Could not load sessions" / "Check your connection and pull to refresh."). FlatList<Session> keyExtractor `(s) => s.id`, renderItem `<SessionListItem session={item} onPress={() => router.push(`/client/history/${item.id}`)} />`, contentContainerStyle paddingBottom 24, showsVerticalScrollIndicator false. `onEndReached={() => { if (!isFetchingNextPage && hasNextPage) fetchNextPage(); }}` onEndReachedThreshold 0.3. ListFooterComponent: isFetchingNextPage → ActivityIndicator '#00FF66' paddingVertical 16; else `!hasNextPage && sessions.length > 0` → Text "All sessions loaded" (Label 14 '#888888' center pv16). ListEmptyComponent (only when not loading): `<EmptyState icon={<Ionicons name="time-outline" size={40} color="#444444" />} title="No sessions yet" message="Your workout history will appear here after you complete your first session." />` (NO CTA — D-12). Then in `src/app/client/_layout.tsx` add a `<Tabs.Screen name="history" options={{ title: 'History', tabBarIcon: tabIcon('time-outline') }} />` between Home and Profile.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - client/history.tsx flattens infinite-query pages and renders SessionListItem rows newest-first
    - onEndReached fetches the next page only when !isFetchingNextPage && hasNextPage
    - Footer shows "All sessions loaded" at the end; ListEmptyComponent is the no-CTA EmptyState
    - A "History" tab appears in the client tab bar
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Client History tab + paginated list render real sessions with pagination, footer, and empty state.</done>
</task>

<task type="auto">
  <name>Task 2: Session detail screen (HIST-02)</name>
  <files>src/app/client/history/[sessionId].tsx</files>
  <read_first>
    - .planning/phases/04-history-polish/04-UI-SPEC.md § "4. Session Detail" (header, EXERCISES card, ExerciseDetailRow icons/colors, exercise-name resolution Option A, loading + empty-exercise edge cases)
    - .planning/phases/04-history-polish/04-PATTERNS.md § "src/app/client/history/[sessionId].tsx" (analog: trainer/clients/[clientId].tsx — push-stack screen: useLocalSearchParams, loading/not-found guards, ScrollView card sections) + § "src/lib/sessionDetail.ts" usage
    - src/hooks/useSessionHistory.ts caches pages, but session detail needs the single session — read it from the history cache or add a small fetch; AND src/hooks/useAssignment.ts (from 04-01) for the snapshot
    - src/lib/sessionDetail.ts (resolveSessionExercises) ; src/components/sessions/StatusBadge.tsx ; src/types/session.ts + src/types/assignment.ts
  </read_first>
  <action>
    Per HIST-02 and D-06 (Option A): Create `src/app/client/history/[sessionId].tsx` (default export `SessionDetailScreen`). `const { sessionId } = useLocalSearchParams<{ sessionId: string }>()`. Obtain the Session: read it from the `['sessionHistory', uid]` infinite-query cache via `useQueryClient` (flatMap pages, find by id); if absent (deep link), fetch the single doc by id (add a `getSession(sessionId)` to session.service if needed, reading sessionsCollection().doc(id).get() with `snap.exists()` METHOD). Then `useAssignment(session?.assignmentId)`. SafeAreaView bg '#0E0E0E', ScrollView paddingHorizontal 16 paddingBottom 40. Header (renders immediately): date (Heading 20/600 '#FFFFFF'), routineName (Body 16/400 '#888888' mt4), `<StatusBadge session={session} />` mt8 alignSelf flex-start, mode "Gym"/"Home" (Label 14/400 '#888888' mt4). EXERCISES card (bg '#1A1A1A' br8 p16): label "EXERCISES" (Label 14/600 '#888888' mb8). While assignment loading → inline ActivityIndicator '#00FF66' in the card. Resolve via `resolveSessionExercises(assignment?.snapshot.weeks[session.weekIndex]?.days[session.dayIndex], session.completedExerciseIds)`. Render ExerciseDetailRow for completed then skipped: row flexDirection row, paddingVertical 10, borderBottomWidth 1 borderBottomColor '#2A2A2A'; Ionicons completed "checkmark-circle" size20 '#00FF66' / skipped "ellipse-outline" size20 '#444444'; Text name (Body 16/400) color completed '#FFFFFF' / skipped '#888888' marginLeft 10. Edge cases: assignment not found → show exercise IDs as fallback (color '#888888'); session.totalExercises === 0 → Text "No exercise data for this session." (Label 14/400 '#888888' center pv16).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Session detail resolves exercise names from the assignment snapshot (Option A) and partitions completed vs skipped
    - Completed rows use checkmark-circle/#00FF66/#FFFFFF; skipped rows use ellipse-outline/#444444/#888888
    - Assignment-loading shows an inline spinner; missing-assignment and zero-exercise edge cases handled per UI-SPEC
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Session detail screen shows which exercises were completed, re-derived from the snapshot.</done>
</task>

<task type="auto">
  <name>Task 3: Trainer client-profile inline history list (HIST-03)</name>
  <files>src/app/trainer/clients/[clientId].tsx</files>
  <read_first>
    - .planning/phases/04-history-polish/04-PATTERNS.md § "src/app/trainer/clients/[clientId].tsx (MODIFY)" (replace the SESSION HISTORY placeholder at lines ~212–227 with the real list; clientId already from useLocalSearchParams) + § "src/app/client/history.tsx" footer/empty pattern
    - .planning/phases/04-history-polish/04-UI-SPEC.md § "1. Session History List" (trainer inline variant: section header "SESSION HISTORY" Label style, no CTA) + empty copy "This client hasn't completed any workouts yet."
    - src/app/trainer/clients/[clientId].tsx (existing — placeholder card to replace; ScrollView context, so use a bounded inline list, not a nested scrolling FlatList)
    - src/hooks/useSessionHistory.ts ; src/components/sessions/SessionListItem.tsx ; src/components/ui/EmptyState.tsx
  </read_first>
  <action>
    Per HIST-03 and D-04: In `src/app/trainer/clients/[clientId].tsx`, replace the SESSION HISTORY placeholder card (lines ~212–227) with a real history section. Keep the "SESSION HISTORY" label (Label 14/600 '#888888'). Call `useSessionHistory(clientId)` (clientId already in scope from useLocalSearchParams). Because this screen is a ScrollView, do NOT nest a scrolling FlatList — instead `.map()` the flattened `data?.pages.flatMap((p) => p.items) ?? []` into `<SessionListItem session={s} onPress={() => router.push(`/client/history/${s.id}`)} />` rows (the session detail route is shared across roles; trainer reads are allowed by the sessions read rule). Add a "Load more" PrimaryButton (variant outline) that calls `fetchNextPage()` shown only when `hasNextPage`, with a spinner when `isFetchingNextPage`. Empty (not loading, zero sessions): `<EmptyState icon={<Ionicons name="time-outline" size={40} color="#444444" />} title="No sessions yet" message="This client hasn't completed any workouts yet." />` (no CTA). Initial loading: inline ActivityIndicator '#00FF66' within the section.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && grep -q "useSessionHistory" "src/app/trainer/clients/[clientId].tsx" && ! grep -q "coming in Phase 4" "src/app/trainer/clients/[clientId].tsx" && echo "history wired, placeholder removed"</automated>
  </verify>
  <acceptance_criteria>
    - The Phase-4 placeholder ("Session history coming in Phase 4.") is removed
    - useSessionHistory(clientId) drives a mapped list of SessionListItem rows (no nested scrolling FlatList)
    - A "Load more" control advances pages; empty + loading states handled with trainer-view copy
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Trainer client profile shows the real paginated session history, reusing the same components as the client view.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client/trainer→Firestore (sessions read) | Both roles query sessions; the sessions read rule is the enforcement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Information Disclosure | trainer history list / shared session-detail route | mitigate | useSessionHistory always passes a clientId; the sessions read rule allows a trainer only where `resource.data.trainerId == request.auth.uid`. A trainer opening a session-detail route for a non-client is denied server-side; the screen renders the not-found/empty fallback. |
| T-04-08 | Information Disclosure | session detail deep link by id | mitigate | getSession reads sessionsCollection().doc(id); the read rule denies sessions whose clientId/trainerId don't match the caller, so a guessed id returns no data. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan (uses existing libs). Supply-chain checkpoint is in 04-02. |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.json` passes
- Placeholder text removed from trainer [clientId].tsx; useSessionHistory present
</verification>

<success_criteria>
- HIST-01/02/03 delivered: client paginated history + detail, trainer inline client history, empty states wired
</success_criteria>

<output>
Create `.planning/phases/04-history-polish/04-04-SUMMARY.md` when done
</output>
