---
phase: 04-history-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/EmptyState.tsx
  - src/components/sessions/StatusBadge.tsx
  - src/components/sessions/SessionListItem.tsx
  - src/components/clients/AdherenceBadge.tsx
autonomous: true
requirements: [HIST-01, HIST-04, PROF-01]
must_haves:
  truths:
    - "An empty list can show a purposeful icon+title+message (with optional CTA) instead of a blank screen"
    - "A session row shows its derived status badge (Completed green / Partial X/Y yellow)"
    - "A client card can show an adherence % colored by threshold"
  artifacts:
    - path: "src/components/ui/EmptyState.tsx"
      provides: "reusable empty-state component (D-11/D-12)"
      exports: ["EmptyState"]
    - path: "src/components/sessions/StatusBadge.tsx"
      provides: "derived Completed/Partial badge (D-05)"
      exports: ["StatusBadge"]
    - path: "src/components/sessions/SessionListItem.tsx"
      provides: "session history row (date + routine + StatusBadge)"
      exports: ["SessionListItem"]
    - path: "src/components/clients/AdherenceBadge.tsx"
      provides: "threshold-colored adherence text (HIST-04)"
      exports: ["AdherenceBadge"]
  key_links:
    - from: "src/components/sessions/SessionListItem.tsx"
      to: "src/components/sessions/StatusBadge.tsx"
      via: "renders StatusBadge from the session"
      pattern: "StatusBadge"
    - from: "src/components/ui/EmptyState.tsx"
      to: "src/components/ui/PrimaryButton.tsx"
      via: "CTA uses PrimaryButton"
      pattern: "PrimaryButton"
---

<objective>
Build the four presentational components Phase 4 introduces, with no data wiring: EmptyState
(empty lists, success criterion 5), StatusBadge (D-05), SessionListItem (history row), and
AdherenceBadge (HIST-04 threshold colors). These are pure UI; the Wave 2 screen plans wire
them to data.

Purpose: Interface-first — the history screens (04-04) and client-list adherence (04-05)
import these components. Building them as isolated, design-contract-exact components keeps the
screen plans focused on data flow.
Output: EmptyState.tsx, StatusBadge.tsx, SessionListItem.tsx, AdherenceBadge.tsx.
</objective>

<execution_context>
@/Users/FF-rodrigo/Documents/Personal/LauFit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/FF-rodrigo/Documents/Personal/LauFit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-history-polish/04-UI-SPEC.md
@.planning/phases/04-history-polish/04-PATTERNS.md
@src/types/session.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: EmptyState component (D-11/D-12)</name>
  <files>src/components/ui/EmptyState.tsx</files>
  <read_first>
    - .planning/phases/04-history-polish/04-UI-SPEC.md § "7. EmptyState Component" (anatomy, props interface, accessibility, CTA uses PrimaryButton) + § "Spacing/Typography/Color" tokens
    - .planning/phases/04-history-polish/04-PATTERNS.md § "src/components/ui/EmptyState.tsx" (analog: PrimaryButton.tsx — same role; reuse PrimaryButton for CTA)
    - src/components/ui/PrimaryButton.tsx (props + token usage; the CTA reuses this, not a custom Pressable)
  </read_first>
  <action>
    Per D-11/D-12 and UI-SPEC § 7: Create `src/components/ui/EmptyState.tsx` exporting `EmptyState` with props `{ icon: React.ReactNode; title: string; message: string; ctaLabel?: string; onCta?: () => void }`. Layout: outer View alignItems/justifyContent center, paddingVertical 48 (2xl token), paddingHorizontal 24 (lg). Icon slot wrapper marginBottom 16 with `accessibilityElementsHidden={true}` (decorative). Title: fontSize 20, fontWeight '600', color '#FFFFFF', textAlign center, marginBottom 8. Message: fontSize 14, fontWeight '400', color '#888888', textAlign center, lineHeight 21. When `ctaLabel && onCta`: render `<PrimaryButton label={ctaLabel} onPress={onCta} />` inside a View marginTop 24. Use only the 4 type sizes (14/16/20/28) and 8-pt spacing per UI-SPEC. Do NOT hardcode a 5th font size.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - EmptyState exports the exact props interface (icon/title/message/ctaLabel?/onCta?)
    - CTA renders via PrimaryButton only when both ctaLabel and onCta are provided
    - Icon wrapper is accessibilityElementsHidden; colors/sizes match UI-SPEC tokens
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>EmptyState renders icon+title+message and an optional PrimaryButton CTA, matching the UI-SPEC anatomy.</done>
</task>

<task type="auto">
  <name>Task 2: StatusBadge + SessionListItem (D-05 + history row)</name>
  <files>src/components/sessions/StatusBadge.tsx, src/components/sessions/SessionListItem.tsx</files>
  <read_first>
    - .planning/phases/04-history-polish/04-UI-SPEC.md § "2. SessionListItem" + § "3. StatusBadge" (variant colors, derivation, date format with T00:00:00 guard, accessibility labels, ≥44dp touch target)
    - .planning/phases/04-history-polish/04-PATTERNS.md § "src/components/sessions/StatusBadge.tsx" + § "src/components/sessions/SessionListItem.tsx" (analog: ClientListItem.tsx — NoProgramIndicator + Pressable row layout)
    - src/components/clients/ClientListItem.tsx (Pressable row structure, props shape, accessibility)
    - src/types/session.ts (completedExerciseIds, totalExercises, date, routineName)
  </read_first>
  <action>
    Per D-05: Create `src/components/sessions/StatusBadge.tsx` exporting `StatusBadge` with props `{ session: Session }`. DERIVE status (never read a status field): isComplete = `session.completedExerciseIds.length === session.totalExercises`. Completed variant: View bg `rgba(0,255,102,0.12)`, borderRadius 4, paddingHorizontal 8, paddingVertical 4, Text "Completed" fontSize 14 fontWeight '600' color '#00FF66'. Partial variant: bg `rgba(255,214,0,0.12)`, Text `Partial ${n}/${total}` color '#FFD600'. accessibilityLabel "Status: Completed" or `Status: Partial, ${n} of ${total} exercises`. Then create `src/components/sessions/SessionListItem.tsx` exporting `SessionListItem` with props `{ session: Session; onPress: () => void }`. Pressable row: bg '#1A1A1A', borderWidth 1, borderColor '#444444', borderRadius 8, paddingVertical 12, paddingHorizontal 16, marginBottom 8, minHeight 44, flexDirection row, alignItems center. Left column flex 1: date Text (Body 16/400 '#FFFFFF') formatted via `new Date(session.date + 'T00:00:00').toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' })`; routineName Text (Label 14/400 '#888888' marginTop 2). Right: `<StatusBadge session={session} />`. accessibilityRole "button", accessibilityLabel `${session.routineName ?? 'Session'} on ${formattedDate}, ${statusLabel}`.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - StatusBadge derives status from completedExerciseIds.length vs totalExercises (no status field read)
    - SessionListItem renders date (T00:00:00-guarded format) + routineName + StatusBadge, minHeight 44, accessibilityRole button
    - Colors and tokens match UI-SPEC § 2/3
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>StatusBadge + SessionListItem render per the UI-SPEC with derived status and the date-format guard.</done>
</task>

<task type="auto">
  <name>Task 3: AdherenceBadge (HIST-04 threshold colors)</name>
  <files>src/components/clients/AdherenceBadge.tsx</files>
  <read_first>
    - .planning/phases/04-history-polish/04-UI-SPEC.md § "5. AdherenceBadge + ClientListItem Modification" (thresholds: null→render nothing, 0–49 #FFD600, 50–79 #FFFFFF, 80–100 #00FF66; text "{N}% adherence"; accessibilityLabel)
    - .planning/phases/04-history-polish/04-PATTERNS.md § "src/components/clients/AdherenceBadge.tsx" (analog: NoProgramIndicator sub-component in ClientListItem.tsx)
    - src/components/clients/ClientListItem.tsx (NoProgramIndicator inline-text row pattern, lines ~29–44)
  </read_first>
  <action>
    Per HIST-04 and UI-SPEC § 5: Create `src/components/clients/AdherenceBadge.tsx` exporting `AdherenceBadge` with props `{ adherence: number | null }`. If adherence is null → `return null` (do not render). Color by threshold: `< 50` → '#FFD600'; `50–79` → '#FFFFFF'; `>= 80` → '#00FF66'. Layout: View flexDirection row, alignItems center, marginTop 2; Text `${adherence}% adherence` fontSize 14 fontWeight '400' with the threshold color (text-only, no background fill). accessibilityLabel `Adherence: ${adherence} percent`.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - AdherenceBadge returns null for null adherence
    - Threshold colors exactly match UI-SPEC (yellow/white/green at &lt;50 / 50–79 / ≥80)
    - Text reads "{N}% adherence"; accessibilityLabel "Adherence: {N} percent"
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>AdherenceBadge renders threshold-colored adherence text and nothing when adherence is null.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure presentational components; no data fetch, no write, no untrusted-input crossing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Tampering | StatusBadge derivation | accept | Status is derived client-side from already-authorized session fields for display; no security decision. Mis-derivation only mislabels a row to the authorized viewer. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (uses existing React Native + Ionicons via callers). Supply-chain checkpoint is in 04-02. |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.json` passes (all four components compile)
</verification>

<success_criteria>
- EmptyState, StatusBadge, SessionListItem, AdherenceBadge implemented exactly to UI-SPEC tokens and accessibility
</success_criteria>

<output>
Create `.planning/phases/04-history-polish/04-03-SUMMARY.md` when done
</output>
