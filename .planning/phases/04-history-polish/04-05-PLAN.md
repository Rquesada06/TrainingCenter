---
phase: 04-history-polish
plan: 05
type: execute
wave: 2
depends_on: ["04-01", "04-03"]
files_modified:
  - src/components/clients/ClientListItem.tsx
  - src/app/trainer/clients/index.tsx
  - src/app/trainer/exercises/index.tsx
  - src/app/trainer/routines/index.tsx
  - src/app/trainer/programs/index.tsx
autonomous: true
requirements: [HIST-04, HIST-01]
must_haves:
  truths:
    - "Each trainer client card shows the client's adherence % (current program), colored by threshold"
    - "Every trainer list (exercises, routines, programs, clients) shows a purposeful EmptyState when empty"
    - "Actionable lists show a CTA; adherence renders nothing when not computable (null)"
  artifacts:
    - path: "src/components/clients/ClientListItem.tsx"
      provides: "adherence badge wired into the client card (HIST-04)"
      contains: "computeAdherence"
    - path: "src/app/trainer/clients/index.tsx"
      provides: "EmptyState wired on the clients list"
      contains: "EmptyState"
  key_links:
    - from: "src/components/clients/ClientListItem.tsx"
      to: "src/lib/adherence.ts"
      via: "computeAdherence(assignment, sessions, today)"
      pattern: "computeAdherence"
    - from: "src/components/clients/ClientListItem.tsx"
      to: "src/services/session.service.ts"
      via: "fetchSessionsForAssignment per-client useQuery"
      pattern: "fetchSessionsForAssignment"
---

<objective>
Wire adherence onto the trainer's client cards (HIST-04) and replace every inline empty
message across the four trainer lists with the reusable EmptyState (success criterion 5,
actionable variant with CTAs).

Purpose: Deliver HIST-04 (adherence % per client) and the actionable half of the empty-state
criterion using the Wave 1 logic (computeAdherence, fetchSessionsForAssignment) and components
(AdherenceBadge, EmptyState). Disjoint file set from 04-04 and 04-06 → runs parallel in Wave 2.
Output: ClientListItem.tsx (adherence), and the four trainer list index screens with EmptyState.
</objective>

<execution_context>
@/Users/FF-rodrigo/Documents/Personal/LauFit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/FF-rodrigo/Documents/Personal/LauFit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-history-polish/04-UI-SPEC.md
@.planning/phases/04-history-polish/04-PATTERNS.md
@src/types/assignment.ts
@src/types/session.ts
@src/components/clients/ClientListItem.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adherence on ClientListItem (HIST-04)</name>
  <files>src/components/clients/ClientListItem.tsx</files>
  <read_first>
    - .planning/phases/04-history-polish/04-UI-SPEC.md § "5. AdherenceBadge + ClientListItem Modification" (placement below programLabel; null → render nothing; per-client lazy useQuery; no per-row spinner)
    - .planning/phases/04-history-polish/04-PATTERNS.md § "src/components/clients/ClientListItem.tsx (MODIFY)" (existing per-row useActiveAssignment; add sessions useQuery + computeAdherence useMemo; import additions)
    - .planning/phases/04-history-polish/04-RESEARCH.md § "Open Questions" #2 (lazy per-client sessions fetch for MVP ~5 clients) + § "Anti-Patterns" (don't compute adherence inside render — useMemo)
    - src/components/clients/ClientListItem.tsx (existing self: useActiveAssignment per row, loading guard, programLabel slot)
    - src/lib/adherence.ts (computeAdherence) ; src/services/session.service.ts (fetchSessionsForAssignment) ; src/components/clients/AdherenceBadge.tsx (from 04-03) ; src/lib/workoutDayComputer.ts (localTodayString)
  </read_first>
  <action>
    Per HIST-04 and D-01/D-03: In `ClientListItem`, after `activeAssignment = useActiveAssignment(client.uid)`, add a per-client sessions query: `const sessionsQ = useQuery({ queryKey: ['adherenceSessions', client.uid, activeAssignment.data?.id], queryFn: () => fetchSessionsForAssignment(client.uid, activeAssignment.data!.id), enabled: !!activeAssignment.data?.id })`. Compute `const adherence = useMemo(() => activeAssignment.data && sessionsQ.data ? computeAdherence(activeAssignment.data, sessionsQ.data, localTodayString()) : null, [activeAssignment.data, sessionsQ.data])`. Render `{adherence !== null && <AdherenceBadge adherence={adherence} />}` directly below the existing programLabel slot. Do NOT add a per-row spinner while sessions load (UI-SPEC: avoid layout shift — render nothing until adherence resolves). Add imports: useQuery, fetchSessionsForAssignment, computeAdherence, localTodayString, AdherenceBadge.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && grep -q "computeAdherence" src/components/clients/ClientListItem.tsx && grep -q "AdherenceBadge" src/components/clients/ClientListItem.tsx && echo "adherence wired"</automated>
  </verify>
  <acceptance_criteria>
    - ClientListItem fetches sessions per client (enabled only when an active assignment exists) and computes adherence in a useMemo (not in render body)
    - AdherenceBadge renders below the program label only when adherence is non-null
    - No per-row spinner is added for the adherence slot
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Each client card shows the threshold-colored adherence % for the current program, or nothing when not computable.</done>
</task>

<task type="auto">
  <name>Task 2: EmptyState on all four trainer lists (success criterion 5 — actionable)</name>
  <files>src/app/trainer/exercises/index.tsx, src/app/trainer/routines/index.tsx, src/app/trainer/programs/index.tsx, src/app/trainer/clients/index.tsx</files>
  <read_first>
    - .planning/phases/04-history-polish/04-UI-SPEC.md § "7. EmptyState Component" → "Per-screen instances" table (exact icon/title/message/CTA per list) + § "Copywriting Contract" (CTA labels)
    - .planning/phases/04-history-polish/04-PATTERNS.md § "src/app/trainer/clients/index.tsx (MODIFY)" (replace inline empty Text with EmptyState; Ionicons import; router.push CTA target)
    - src/app/trainer/clients/index.tsx (existing ListEmptyComponent inline Text + router.push('/trainer/clients/add')) and the analogous ListEmptyComponent in exercises/routines/programs index.tsx — replace each
    - src/components/ui/EmptyState.tsx (from 04-03)
  </read_first>
  <action>
    Per D-11/D-12 and success criterion 5: In each of the four trainer list index screens, replace the current inline-Text `ListEmptyComponent` with `<EmptyState .../>` using the exact values from UI-SPEC § 7 "Per-screen instances": Exercises → icon `barbell-outline`, title "No exercises yet", message "Add exercises to your library to start building routines.", ctaLabel "+ Add Exercise", onCta → push to the new-exercise route; Routines → `list-outline`, "No routines yet", "Create a routine by combining exercises from your library.", "+ New Routine", push to new-routine route; Programs → `calendar-outline`, "No programs yet", "Build a multi-week program by assigning routines to days.", "+ New Program", push to new-program route; Clients → `people-outline`, "No clients yet", "Add your first client to get started.", "+ Add Client", `router.push('/trainer/clients/add')`. Icons: `<Ionicons name="..." size={40} color="#444444" />`. Match each screen's existing add-route path (read the screen's current header "+" button onPress to reuse the exact route). **IMPORTANT — exercises list filter branch:** `src/app/trainer/exercises/index.tsx` already has a TWO-branch ListEmptyComponent ('No exercises match your filters.' when a search/filter is active vs the no-data message). PRESERVE the filter-active branch as a message-only EmptyState (icon `search-outline`, title "No matches", message "No exercises match your filters.", NO CTA) and replace ONLY the no-data branch with the actionable "+ Add Exercise" EmptyState above. Do not show the Add CTA when a filter is active (it would be misleading).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && grep -lq "EmptyState" src/app/trainer/exercises/index.tsx src/app/trainer/routines/index.tsx src/app/trainer/programs/index.tsx src/app/trainer/clients/index.tsx && echo "all four lists use EmptyState"</automated>
  </verify>
  <acceptance_criteria>
    - All four trainer list screens render EmptyState (not inline Text) when empty
    - Each EmptyState uses the exact icon/title/message/CTA from UI-SPEC § 7, with the correct add-route in onCta
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Exercises, routines, programs, and clients lists each show the actionable EmptyState with the correct CTA route.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trainer→Firestore (sessions read for adherence) | Per-client adherence sessions query; sessions read rule enforces trainer scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Information Disclosure | fetchSessionsForAssignment per client | mitigate | Query filters `clientId == client.uid` AND `assignmentId`; the sessions read rule allows the trainer only for their own clients (`resource.data.trainerId == request.auth.uid`). Adherence is computed only over authorized sessions. |
| T-04-10 | Denial of Service | N+1 per-row session queries | accept | MVP trainer (Lau) has ~5 clients; lazy per-row useQuery is acceptable (RESEARCH.md Open Q2). Flagged for optimization if client count grows; not a security issue. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan. Supply-chain checkpoint is in 04-02. |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.json` passes
- ClientListItem contains computeAdherence + AdherenceBadge; all four list screens contain EmptyState
</verification>

<success_criteria>
- HIST-04 adherence shown per client card; actionable EmptyState on all four trainer lists
</success_criteria>

<output>
Create `.planning/phases/04-history-polish/04-05-SUMMARY.md` when done
</output>
