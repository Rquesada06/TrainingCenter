---
phase: 04-history-polish
plan: 07
type: execute
wave: 3
depends_on: ["04-04", "04-05", "04-06"]
files_modified: []
autonomous: false
requirements: [HIST-01, HIST-02, HIST-03, HIST-04, PROF-01, PROF-02, PROF-03]
must_haves:
  truths:
    - "Full jest suite + tsc are green before phase verification"
    - "The dev-client is rebuilt so expo-image-picker's native module is available on device"
    - "On-device: photo capture/upload works and storage.rules enforce own-path only"
    - "On-device: history pagination, session detail, adherence %, and all empty states render correctly"
  artifacts: []
  key_links:
    - from: "app.config.js (expo-image-picker plugin)"
      to: "dev-client binary"
      via: "eas build --profile development"
      pattern: "expo-image-picker"
---

<objective>
End-of-phase gate and on-device UAT for Phase 4. Run the full automated suite, rebuild the
native dev-client for expo-image-picker, then manually verify every behavior that cannot be
unit-tested: photo capture/upload + storage-rule enforcement, history pagination on real data,
session detail, adherence accuracy, and all empty states.

Purpose: The image-picker native module and Firebase Storage rules can only be validated on a
real device with a fresh build and deployed rules (04-VALIDATION.md Manual-Only Verifications).
This plan is the phase's final acceptance gate before /gsd-verify-work.
Output: a green automated suite, a rebuilt dev-client, and confirmed UAT results (no code files).
</objective>

<execution_context>
@/Users/FF-rodrigo/Documents/Personal/LauFit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/FF-rodrigo/Documents/Personal/LauFit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-history-polish/04-VALIDATION.md
@.planning/phases/04-history-polish/04-RESEARCH.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated gate (jest + tsc)</name>
  <files>(none — verification only)</files>
  <read_first>
    - .planning/phases/04-history-polish/04-VALIDATION.md § "Test Infrastructure" + § "Sampling Rate" (full react-native suite before /gsd-verify-work)
  </read_first>
  <action>
    Run the full automated gate: `npx jest --selectProjects react-native` (all suites, including the new adherence, session.service, sessionDetail, and storage.service tests) and `npx tsc --noEmit -p tsconfig.json`. If anything is red, fix-forward in the owning plan's files (do not weaken tests). This must be green before the on-device UAT checkpoint.
  </action>
  <verify>
    <automated>npx jest --selectProjects react-native && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Full react-native jest suite passes (adherence, session.service, sessionDetail, storage.service + all prior phase suites)
    - `npx tsc --noEmit` passes with zero errors
  </acceptance_criteria>
  <done>The entire automated suite and the type-check are green.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild dev-client for expo-image-picker</name>
  <files>(none — native build)</files>
  <read_first>
    - .planning/phases/04-history-polish/04-RESEARCH.md § "Pitfall 4" (native module not found unless the dev-client binary is rebuilt) + § "Environment Availability" (EAS dev-client build)
    - 04-02-SUMMARY.md (confirm expo-image-picker install + app.config.js plugin landed; confirm storage.rules deployed)
  </read_first>
  <action>
    Per D-08 and RESEARCH.md Pitfall 4: Trigger a development dev-client build that includes the newly added expo-image-picker native module — `eas build --profile development --platform ios` (and/or `--platform android` for the test device). If EAS is not authenticated, this surfaces an auth gate → the user authenticates and the build is retried. Install the resulting build on the test device. Also confirm (from 04-02-SUMMARY) that `firebase deploy --only storage` has run so the storage.rules are live; if not, run it now before the UAT checkpoint.
  </action>
  <verify>
    <automated>echo "EAS dev-client build triggered for expo-image-picker; confirm build completion + install in the next checkpoint"</automated>
    <human-check>Confirm the EAS development build completed and is installed on the test device, and that storage.rules are deployed.</human-check>
  </verify>
  <acceptance_criteria>
    - A development dev-client build including expo-image-picker has been produced and installed on the test device
    - storage.rules are confirmed deployed (live)
  </acceptance_criteria>
  <done>The rebuilt dev-client (with expo-image-picker) is on the device and storage.rules are live.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: On-device UAT — photos, history, adherence, empty states</name>
  <action>This is the blocking on-device acceptance gate, not an implementation task. After the dev-client rebuild and storage.rules deploy, pause and walk the user through the how-to-verify checklist on a real device. Capture any failures as gap-closure items; do not mark the phase complete until approved.</action>
  <what-built>The complete Phase 4 feature set on the rebuilt dev-client: profile photo edit (camera + library), session history (client tab + trainer client-profile view), session detail, adherence % on client cards, and EmptyState across all lists. Storage rules are deployed.</what-built>
  <how-to-verify>
    Photo (PROF-01/02/03):
    1. Client → Profile → Change photo → Take photo: confirm the camera opens, you can square-crop, and the avatar updates (cached) after upload.
    2. Client → Profile → Change photo → Choose from library: confirm library opens, square-crop, avatar updates.
    3. Repeat both on Trainer → Profile.
    4. Storage rule: confirm a user can only write their own `users/{uid}/...` path (uploads to others denied) — observe no storage/unauthorized error for your own upload; cross-user write is rejected.
    5. Edit name on both profiles → "Saved" appears and persists after reopening the app.

    History (HIST-01/02/03):
    6. Client → History tab: list shows completed sessions newest-first; with >20 sessions, scrolling loads older pages; footer shows "All sessions loaded" at the end.
    7. Tap a session → detail shows completed (green checkmark) vs skipped (grey outline) exercises with correct names.
    8. Trainer → a client → profile: SESSION HISTORY shows the same paginated list; "Load more" advances; tapping a row opens the detail.

    Adherence (HIST-04):
    9. Trainer client list: each card shows "{N}% adherence" colored by threshold; verify the % equals completed (incl. partials) ÷ scheduled routine-days-due for the current program (not future/rest days).

    Empty states (criterion 5):
    10. Open exercises, routines, programs, clients (with no data) and both history views (no sessions): each shows a purposeful EmptyState — CTA on the four trainer lists, message-only on the two history views.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or list the failures (which will become gap-closure tasks).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device→Firebase Storage (live rules) | First real upload against deployed storage.rules |
| dev→EAS build pipeline | Native dev-client build (supply chain already gated in 04-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | Tampering | live storage.rules enforcement | mitigate | UAT step 4 explicitly verifies own-path-only writes against the deployed rule (T-04-03 validated end-to-end). |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new installs in this plan; the EAS build consumes the already-verified expo-image-picker from 04-02. |
</threat_model>

<verification>
- `npx jest --selectProjects react-native` + `npx tsc --noEmit` green
- Dev-client rebuilt with expo-image-picker; storage.rules deployed
- All on-device UAT checks approved
</verification>

<success_criteria>
- All Phase 4 requirements (HIST-01..04, PROF-01..03) verified on device; empty-state criterion confirmed across all lists
</success_criteria>

<output>
Create `.planning/phases/04-history-polish/04-07-SUMMARY.md` when done
</output>
