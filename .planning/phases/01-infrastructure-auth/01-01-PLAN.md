---
phase: 01-infrastructure-auth
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [AUTH-01, AUTH-03, AUTH-05]
files_modified:
  - package.json
  - app.config.js
  - eas.json
  - firebase.json
  - tsconfig.json
  - babel.config.js
  - metro.config.js
  - tailwind.config.js
  - global.css
  - jest.config.js
  - .gitignore
  - src/types/user.ts
autonomous: true
user_setup:
  - service: firebase
    why: "Firebase project must exist before app can authenticate or read/write Firestore"
    env_vars:
      - name: GOOGLE_SERVICES_JSON
        source: "Firebase Console -> Project Settings -> Your apps -> Android app -> download google-services.json; then upload as EAS secret file"
      - name: GOOGLE_SERVICE_INFO_PLIST
        source: "Firebase Console -> Project Settings -> Your apps -> iOS app -> download GoogleService-Info.plist; then upload as EAS secret file"
    dashboard_config:
      - task: "Create Firebase project, enable Email/Password Authentication, create Firestore database in production mode"
        location: "Firebase Console -> Authentication -> Sign-in method; Firestore Database -> Create database"
      - task: "Register Android app (package com.laufit.dev) and iOS app (bundle com.laufit.dev), download config files"
        location: "Firebase Console -> Project Settings -> Your apps"

must_haves:
  truths:
    - "Project boots as an Expo SDK 55 app with routes resolved from src/app/"
    - "All Phase 1 dependencies install at the pinned versions (tailwindcss is v3, not v4)"
    - "An EAS development build profile exists that produces a dev client (never Expo Go)"
    - "Firebase config files are gitignored and sourced from EAS secret file env vars"
    - "@/* import alias resolves to ./src/* and TypeScript compiles with no errors"
  artifacts:
    - path: "app.config.js"
      provides: "Dynamic Expo config with react-native-firebase plugins, useFrameworks static, env-var googleServicesFile"
      contains: "useFrameworks"
    - path: "eas.json"
      provides: "development build profile with developmentClient: true"
      contains: "developmentClient"
    - path: "tsconfig.json"
      provides: "@/* path alias to ./src/*"
      contains: "@/*"
    - path: "tailwind.config.js"
      provides: "Obsidian Performance theme tokens + nativewind preset"
      contains: "00FF66"
    - path: "src/types/user.ts"
      provides: "UserRole and User type contracts consumed by every later plan"
      exports: ["UserRole", "User"]
  key_links:
    - from: "app.config.js"
      to: "process.env.GOOGLE_SERVICES_JSON"
      via: "googleServicesFile reference"
      pattern: "GOOGLE_SERVICES_JSON"
    - from: "metro.config.js"
      to: "global.css"
      via: "withNativeWind input"
      pattern: "withNativeWind"
---

<objective>
Scaffold the LauFit Expo SDK 55 project with the src/app/ layout, install every Phase 1 dependency at its pinned version, and wire up the build/styling/Firebase configuration so that an EAS development build can boot on a real device with Firebase initialized.

This plan produces NO running auth yet — it produces the buildable, correctly-configured shell that Plans 02, 03, and 04 build inside. It also defines the shared `User`/`UserRole` type contracts (interface-first ordering) that every subsequent plan imports.

Purpose: Establish the foundation. Every other Phase 1 plan depends on this file structure and config existing.
Output: A buildable Expo SDK 55 project with Firebase plugins, NativeWind v4 (Tailwind v3 pinned), EAS dev profile, TypeScript path alias, shared user types, and gitignored Firebase secrets.
</objective>

<execution_context>
@/Users/FF-rodrigo/Documents/Personal/LauFit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/FF-rodrigo/Documents/Personal/LauFit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-auth/01-RESEARCH.md
@.planning/phases/01-infrastructure-auth/01-SKELETON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold SDK 55 project and install pinned dependencies</name>
  <files>package.json, .gitignore</files>
  <read_first>
    01-RESEARCH.md "Standard Stack" tables and "Installation — bootstrap sequence" (lines 99-126);
    01-RESEARCH.md Pitfall 6 (NativeWind / Tailwind v4 incompatibility, lines 850-855)
  </read_first>
  <action>
    Initialize the project with `npx create-expo-app@latest laufit --template default@sdk-55` (this produces the `src/app/` template). If the working directory should be the repo root rather than a `laufit/` subfolder, scaffold into a temp dir and move contents up — preserve the SDK 55 `src/app/` layout either way.

    Install the exact Phase 1 dependency set from the RESEARCH bootstrap sequence:
    - Firebase native: `@react-native-firebase/app`, `@react-native-firebase/auth`, `@react-native-firebase/firestore`, `@react-native-firebase/functions`, `@react-native-firebase/storage` (all ^22.x; if v22 New-Arch compat is unconfirmed at install time per RESEARCH Open Question 1, pin the latest stable major that is SDK-55 compatible and record the chosen version in the SUMMARY).
    - Native build helpers: `expo-build-properties`, `expo-dev-client`, `expo-splash-screen`, `expo-secure-store` (via `npx expo install`).
    - State/forms: `zustand`, `react-hook-form`, `zod`, `@hookform/resolvers`, `@tanstack/react-query`.
    - Styling: `npm install nativewind` then `npm install --save-dev tailwindcss@^3.4.17` — PIN to v3 per D (Tailwind v4 silently breaks NativeWind v4). Do NOT run `npm install tailwindcss` without the version pin.
    - Pin bundled native deps via `npx expo install react-native-reanimated react-native-gesture-handler react-native-safe-area-context`.

    Add `google-services.json` and `GoogleService-Info.plist` to `.gitignore` immediately (before any build), per RESEARCH Pitfall 2. Do NOT commit Firebase config files.

    All packages are pre-approved in the RESEARCH Package Legitimacy Audit (slopcheck PyPI false positives, verified on npm) — no per-package legitimacy checkpoint required.
  </action>
  <verify>
    <automated>npx expo-doctor</automated>
    <automated>node -e "const p=require('./package.json'); const tw=(p.devDependencies||{}).tailwindcss||(p.dependencies||{}).tailwindcss; if(!/\^?3\./.test(tw)) {console.error('tailwindcss must be v3, got '+tw); process.exit(1)} console.log('tailwind pinned ok: '+tw)"</automated>
    <automated>grep -q 'google-services.json' .gitignore && grep -q 'GoogleService-Info.plist' .gitignore && echo "firebase secrets gitignored"</automated>
  </verify>
  <done>SDK 55 project exists with src/app/ layout; all Phase 1 deps installed; tailwindcss pinned to ^3.x; expo-doctor passes; Firebase config files are gitignored.</done>
</task>

<task type="auto">
  <name>Task 2: Configure build, styling, Firebase plugins, and TypeScript paths</name>
  <files>app.config.js, eas.json, firebase.json, tsconfig.json, babel.config.js, metro.config.js, tailwind.config.js, global.css</files>
  <read_first>
    01-RESEARCH.md Pattern 5 (app.config.js, lines 483-519), Pattern 6 (eas.json, lines 521-559),
    Pattern 7 (NativeWind config, lines 561-611); Pitfall 3 (useFrameworks static, lines 813-824);
    Recommended Project Structure (lines 207-256)
  </read_first>
  <action>
    Create `app.config.js` per RESEARCH Pattern 5: dev/prod variant via `APP_VARIANT`, `scheme: 'laufit'`, Android package `com.laufit.dev`/`com.laufit`, iOS bundle `com.laufit.dev`/`com.laufit`, `googleServicesFile` referencing `process.env.GOOGLE_SERVICES_JSON` / `process.env.GOOGLE_SERVICE_INFO_PLIST` with local-file fallbacks. Plugins array MUST include `expo-router`, `@react-native-firebase/app`, `@react-native-firebase/auth`, and `expo-build-properties` with `ios: { useFrameworks: 'static' }` (mandatory for firebase-ios-sdk, per Pitfall 3). Delete any static `app.json` `expo` block that would conflict with `app.config.js`.

    Create `eas.json` per RESEARCH Pattern 6: `development` profile with `developmentClient: true` + `distribution: internal` + `env.APP_VARIANT=development` + Android `buildType: apk`; `development-simulator` extending development with iOS `simulator: true`; `preview` and `production` profiles. NEVER configure an Expo Go path.

    Create `firebase.json` declaring `firestore.rules` -> `firestore.rules`, `firestore.indexes` -> `firestore.indexes.json`, and a `functions` source of `functions` (the rules/indexes/functions files themselves are authored in Plan 04; this file just wires the deploy targets).

    Configure `tsconfig.json` so `compilerOptions.paths` maps `@/*` to `./src/*` and `baseUrl` is `.` (per RESEARCH: SDK 55 src/app template resolves @/* to ./src/*).

    Configure NativeWind per RESEARCH Pattern 7: `tailwind.config.js` with `content: ['./src/**/*.{js,jsx,ts,tsx}']`, `presets: [require('nativewind/preset')]`, and `theme.extend.colors` for the Obsidian Performance system (base `#0E0E0E`, accent `#00FF66`, surface `#1A1A1A`, muted `#444444`, text.primary `#FFFFFF`, text.secondary `#888888`) plus `fontFamily.sans: ['HankenGrotesk-Regular']`. Create `metro.config.js` wrapping `getDefaultConfig` with `withNativeWind(config, { input: './global.css' })`. Update `babel.config.js` to `babel-preset-expo` with `jsxImportSource: 'nativewind'` plus the `nativewind/babel` preset. Create `global.css` with the three `@tailwind` directives (base, components, utilities).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>grep -q 'useFrameworks' app.config.js && grep -q 'GOOGLE_SERVICES_JSON' app.config.js && echo "app.config firebase+frameworks ok"</automated>
    <automated>grep -q 'developmentClient' eas.json && echo "eas dev client profile ok"</automated>
    <automated>grep -q '00FF66' tailwind.config.js && grep -q 'withNativeWind' metro.config.js && echo "nativewind theme ok"</automated>
    <automated>grep -q '@/\*' tsconfig.json && echo "path alias ok"</automated>
  </verify>
  <done>tsc --noEmit passes; app.config.js has firebase plugins + useFrameworks static + env-var googleServicesFile; eas.json has developmentClient dev profile; firebase.json wires rules/indexes/functions; NativeWind theme + metro + babel configured; @/* alias resolves.</done>
</task>

<task type="auto">
  <name>Task 3: Define shared user type contracts and Jest test infrastructure</name>
  <files>src/types/user.ts, jest.config.js</files>
  <read_first>
    01-RESEARCH.md Pattern 2/3 (authStore shape: uid, role, trainerId, isLoaded, lines 327-395);
    Pattern 4 (createClientAccount USERS doc fields, lines 453-462);
    01-VALIDATION.md Test Infrastructure + Wave 0 Requirements (lines 18-24, 55-61)
  </read_first>
  <action>
    Create `src/types/user.ts` exporting the contracts every later plan consumes (interface-first ordering — define these now so Plans 02-04 import rather than redefine):
    - `export type UserRole = 'trainer' | 'client';`
    - `export interface User` with fields matching the Firestore USERS doc written by createClientAccount and read by the auth listener: `uid: string`, `role: UserRole`, `trainerId: string | null`, `name: string`, `email: string`, `createdAt?: unknown` (Firestore server timestamp; refine in Plan 04 if needed).
    - `export interface CreateClientAccountInput { name: string; email: string; temporaryPassword: string; }` and `export interface CreateClientAccountResult { uid: string; }` — the Cloud Function contract consumed by Plan 04's client caller.

    Configure `jest.config.js` for the `jest-expo` preset (per 01-VALIDATION Wave 0). Ensure `@testing-library/react-native` is installed as a devDependency (install if the SDK 55 template did not include it). Configure `moduleNameMapper` so `@/` resolves to `<rootDir>/src/` in tests, matching the tsconfig alias.

    Do NOT write authStore tests here — those belong to Plan 02 which owns authStore. This task only stands up the Jest harness and the type contracts.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/types/user.ts','utf8');['UserRole','User','CreateClientAccountInput','CreateClientAccountResult'].forEach(t=>{if(!s.includes(t)){console.error('missing export '+t);process.exit(1)}});console.log('user contracts ok')"</automated>
    <automated>npx jest --listTests 2>&1 | head -5 && echo "jest config loads"</automated>
  </verify>
  <done>src/types/user.ts exports UserRole, User, CreateClientAccountInput, CreateClientAccountResult; jest.config.js uses jest-expo preset with @/ mapped to src/; tsc passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → EAS build env | Gitignored Firebase config files must reach the build via secret file vars, not source control |
| device → Firebase | Firebase initialized from native config files; absence causes runtime crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | google-services.json / GoogleService-Info.plist | mitigate | Gitignored at scaffold (Task 1); referenced via EAS secret file env vars in app.config.js (Task 2). Firebase security enforced at rules layer (Plan 04), not key secrecy. |
| T-01-02 | Tampering | npm dependency install | accept | All packages pre-verified in RESEARCH Package Legitimacy Audit against npm registry; slopcheck PyPI verdicts are cross-ecosystem false positives. No [ASSUMED]/[SUS] packages. |
| T-01-03 | Denial of Service (build) | iOS native build | mitigate | useFrameworks: "static" set in expo-build-properties (Task 2) — without it iOS build fails with linker errors (Pitfall 3). |
| T-01-SC | Tampering | npm installs | mitigate | slopcheck run during research; legitimacy table present and complete; no blocking checkpoint needed (zero [ASSUMED]/[SUS]). |
</threat_model>

<verification>
- `npx expo-doctor` passes (no SDK 55 dependency mismatches).
- `npx tsc --noEmit` passes with the @/* alias resolving.
- `tailwindcss` is pinned to a v3.x range in package.json.
- `app.config.js` contains `useFrameworks` and env-var-based `googleServicesFile`.
- `eas.json` development profile sets `developmentClient: true`.
- Firebase config files are listed in `.gitignore`.
</verification>

<success_criteria>
- A developer can run `eas build --profile development` and produce a dev client (no Expo Go path exists).
- The project compiles, expo-doctor is clean, and the shared user type contracts exist for Plans 02-04 to import.
- NativeWind v4 + Tailwind v3 styling pipeline is wired to global.css.
</success_criteria>

<output>
Create `.planning/phases/01-infrastructure-auth/01-01-SUMMARY.md` when done. Record the exact react-native-firebase major version chosen and confirm tailwindcss pinned version.
</output>
