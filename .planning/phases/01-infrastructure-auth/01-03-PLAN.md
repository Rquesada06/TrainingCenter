---
phase: 01-infrastructure-auth
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
requirements: [AUTH-01, AUTH-02, AUTH-04]
files_modified:
  - src/app/sign-in.tsx
  - src/validation/auth.schema.ts
  - src/components/ui/TextField.tsx
  - src/components/ui/PrimaryButton.tsx
  - src/firebase/__tests__/auth.service.test.ts
autonomous: true

must_haves:
  truths:
    - "A user can enter email + password and submit to sign in (AUTH-01, AUTH-02)"
    - "Invalid email / empty password are caught by zod before any Firebase call"
    - "Wrong credentials and network errors surface a readable error message (not a silent failure)"
    - "A user can trigger a password reset email from the sign-in screen (AUTH-04)"
    - "The screen renders the Obsidian Performance theme: #0E0E0E background, #00FF66 primary CTA"
  artifacts:
    - path: "src/app/sign-in.tsx"
      provides: "Public login screen with RHF + zod, sign-in and reset actions"
      contains: "useForm"
    - path: "src/validation/auth.schema.ts"
      provides: "zod sign-in schema (email, password)"
      exports: ["signInSchema", "SignInValues"]
    - path: "src/components/ui/PrimaryButton.tsx"
      provides: "Obsidian Performance accent CTA button (#00FF66)"
  key_links:
    - from: "src/app/sign-in.tsx"
      to: "src/firebase/auth.ts signIn"
      via: "onSubmit handler"
      pattern: "signIn\\("
    - from: "src/app/sign-in.tsx"
      to: "src/firebase/auth.ts sendPasswordReset"
      via: "reset button handler"
      pattern: "sendPasswordReset"
    - from: "src/app/sign-in.tsx"
      to: "src/validation/auth.schema.ts"
      via: "zodResolver(signInSchema)"
      pattern: "zodResolver"
---

<objective>
Build the public sign-in screen — the single real UI interaction in the walking skeleton. It validates input with zod, calls the Plan 02 `signIn` helper (which triggers the auth listener → role routing), surfaces errors, and offers a password-reset action. Styled with the Obsidian Performance theme.

After this plan, signing in with real credentials on a dev build moves the user from sign-in to their role shell — closing the end-to-end auth loop for AUTH-01/AUTH-02. Password reset closes AUTH-04.

Purpose: Deliver the user-facing auth capability that makes the skeleton demonstrable.
Output: A themed sign-in screen with form validation, sign-in + reset actions, error states, reusable UI primitives, and a unit test for the reset helper.
</objective>

<execution_context>
@/Users/FF-rodrigo/Documents/Personal/LauFit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/FF-rodrigo/Documents/Personal/LauFit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-auth/01-RESEARCH.md
@.planning/phases/01-infrastructure-auth/01-SKELETON.md
@src/firebase/auth.ts
@src/types/user.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: zod sign-in schema + sendPasswordReset unit test + UI primitives</name>
  <files>src/validation/auth.schema.ts, src/components/ui/TextField.tsx, src/components/ui/PrimaryButton.tsx, src/firebase/__tests__/auth.service.test.ts</files>
  <read_first>
    01-RESEARCH.md Security Domain V5 Input Validation (lines 980); Pattern 7 theme tokens (lines 561-590);
    01-VALIDATION.md auth.service test row (line 45 / Test Map AUTH-04)
  </read_first>
  <behavior>
    - Test: signInSchema rejects an invalid email string with a field error.
    - Test: signInSchema rejects an empty password with a field error.
    - Test: signInSchema accepts a valid email + non-empty password.
    - Test: sendPasswordReset('user@example.com') calls auth().sendPasswordResetEmail with that email and NO second argument (no actionCodeSettings) — AUTH-04.
  </behavior>
  <action>
    Create `src/validation/auth.schema.ts`: `export const signInSchema = z.object({ email: z.string().email(), password: z.string().min(1) })` and `export type SignInValues = z.infer<typeof signInSchema>`.

    Create `src/firebase/__tests__/auth.service.test.ts` mocking `@react-native-firebase/auth`, asserting `sendPasswordReset` (from `src/firebase/auth.ts`, owned by Plan 02) calls `sendPasswordResetEmail` with exactly the email and no actionCodeSettings argument (AUTH-04). Also assert the three schema behaviors above (schema validation needs no mocks).

    Create reusable Obsidian Performance UI primitives:
    - `src/components/ui/TextField.tsx`: a controlled text input (label + error text) styled with NativeWind — surface `#1A1A1A` field on `#0E0E0E`, white text, secondary `#888888` label, red-toned error text. Props: `label`, `value`, `onChangeText`, `error?`, plus passthrough TextInput props (secureTextEntry, autoCapitalize, keyboardType).
    - `src/components/ui/PrimaryButton.tsx`: the accent CTA — `#00FF66` background, dark `#0E0E0E` label text, `loading` and `disabled` states (dim + spinner). Props: `label`, `onPress`, `loading?`, `disabled?`.
  </action>
  <verify>
    <automated>npx jest --testPathPattern="auth.service"</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>node -e "const fs=require('fs');const b=fs.readFileSync('src/components/ui/PrimaryButton.tsx','utf8');if(!/00FF66/.test(b)){console.error('PrimaryButton must use accent #00FF66');process.exit(1)}console.log('accent ok')"</automated>
  </verify>
  <done>auth.service test green (3 schema + 1 reset behaviors); tsc passes; signInSchema + SignInValues exported; TextField and PrimaryButton exist with Obsidian Performance theming (accent #00FF66).</done>
</task>

<task type="auto">
  <name>Task 2: Sign-in screen with RHF, sign-in + reset actions, and error states</name>
  <files>src/app/sign-in.tsx</files>
  <read_first>
    01-RESEARCH.md Pattern 1 system diagram sign-in branch (lines 190-205);
    01-RESEARCH.md "Don't Hand-Roll" password reset row (line 772); Pattern 7 theme (lines 561-590);
    src/firebase/auth.ts (signIn, sendPasswordReset signatures from Plan 02)
  </read_first>
  <action>
    Replace the Plan 02 placeholder `src/app/sign-in.tsx` with the real screen. Use `useForm<SignInValues>({ resolver: zodResolver(signInSchema) })` with `Controller`-wrapped `TextField`s for email (keyboardType email-address, autoCapitalize none) and password (secureTextEntry). Render on a `#0E0E0E` background with the LauFit/Obsidian Performance styling and Hanken Grotesk font family.

    Primary CTA (`PrimaryButton`, accent #00FF66): on submit, call `signIn(values.email, values.password)` from `@/firebase/auth`. On success do nothing navigation-wise — the Plan 02 auth listener fires onAuthStateChanged → authStore updates → Stack.Protected re-evaluates and routes the user to their role shell (AUTH-01/AUTH-02). Show the button `loading` state while the promise is pending.

    Error states: catch the signIn rejection and map Firebase error codes to readable messages — `auth/invalid-credential` / `auth/wrong-password` / `auth/user-not-found` → "Incorrect email or password."; `auth/network-request-failed` → "Network error. Check your connection and try again."; fallback → a generic message. Display the error in a dismissible inline banner above the form (do not silently swallow).

    Password reset: a secondary "Forgot password?" text button. On press, validate the email field alone (reuse `signInSchema.shape.email`); if valid, call `sendPasswordReset(email)` and show a confirmation message ("If that email exists, a reset link has been sent."); if the email field is empty/invalid, show the field error. Plain reset email only — no deep link handling (AUTH-04).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npx jest</automated>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/app/sign-in.tsx','utf8');['useForm','zodResolver','signIn','sendPasswordReset'].forEach(t=>{if(!s.includes(t)){console.error('sign-in missing '+t);process.exit(1)}});console.log('sign-in wired ok')"</automated>
    <human-check>On the EAS dev build: (1) submit invalid email → inline validation error, no Firebase call; (2) submit wrong credentials → "Incorrect email or password." banner; (3) submit valid trainer credentials → trainer shell appears; (4) tap "Forgot password?" with a real email → reset email arrives within 2 minutes.</human-check>
  </verify>
  <done>tsc + full jest suite pass; sign-in.tsx uses RHF + zodResolver, calls signIn and sendPasswordReset, maps Firebase error codes to readable banners, styled with Obsidian Performance theme; human-check confirms login routes to role shell and reset email arrives.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → Firebase Auth | Email/password fields are untrusted input crossing into the auth service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | sign-in form input | mitigate | zod schema (signInSchema) validates email format + non-empty password at the form boundary before any Firebase call (ASVS V5). |
| T-03-02 | Information Disclosure | auth error messages | mitigate | Wrong-password / user-not-found both map to a single "Incorrect email or password." message — does not reveal whether an account exists. Reset confirmation is generic ("If that email exists..."). |
| T-03-03 | Spoofing | password reset | accept | sendPasswordReset relies on Firebase Auth's built-in token generation/expiry/secure delivery; no custom token handling. Generic confirmation prevents account enumeration. |
</threat_model>

<verification>
- `npx jest` full suite green (authStore + auth.service + schema).
- `npx tsc --noEmit` passes.
- sign-in.tsx uses useForm + zodResolver, calls signIn and sendPasswordReset.
- PrimaryButton uses accent #00FF66; screen background #0E0E0E.
- Human-check (end-of-phase) confirms login routes to the role shell and reset email arrives.
</verification>

<success_criteria>
- Entering valid credentials signs the user in and lands them on the correct role shell (AUTH-01/AUTH-02) via the Plan 02 listener — no manual navigation.
- Validation and error states are visible and readable; no silent failures.
- Password reset email can be triggered from the screen (AUTH-04) with no deep-link config.
</success_criteria>

<output>
Create `.planning/phases/01-infrastructure-auth/01-03-SUMMARY.md` when done.
</output>
